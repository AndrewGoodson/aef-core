# Architecture Decision Records

Nygard format. Written whenever the research report and research brief
disagreed, or whenever a design choice wasn't fully pinned down by either
source document — per the build instruction to never silently pick one
side of a conflict.

| ADR | Decision | Confidence |
|---|---|---|
| [0001](0001-thin-graph-ir-not-a-framework.md) | Thin custom graph IR, not a framework wholesale | High |
| [0002](0002-durability-backend-in-memory-and-file-json.md) | Phase 0/1 durability: InMemory + File(JSON), not Postgres/Temporal | High |
| [0003](0003-knowledge-graph-backend-deferred.md) | KG backend deferred to Phase 2; target Neo4j/FalkorDB, not Apache AGE | Medium |
| [0004](0004-memory-backend-mem0-default.md) | Mem0 is the Phase 1 memory default; temporal-KG memory deferred | High |
| [0005](0005-multi-agent-default-single-agent-with-readers.md) | Single-agent-with-readers default; deterministic handoff over emergent routing | High |
| [0006](0006-evolution-engine-disabled-by-default.md) | Evolution engine disabled by default, enforced in code not just config | High |
| [0007](0007-fan-out-declared-not-executed.md) | Fan-out declared in the contract, not executed until Phase 2 | High |
| [0008](0008-otel-genai-attributes-pinned-locally.md) | OTel GenAI attribute keys pinned locally, not imported from the incubating package | High |
| [0009](0009-checkpoint-resume-requires-a-cursor.md) | Resuming a run needs a persisted cursor — `run()` alone silently duplicates side effects | High |
| [0010](0010-idempotency-key-is-exposed-not-enforced.md) | Kernel computes/exposes the idempotency key; enforcing it is the node's job, not the kernel's | High |
| [0011](0011-edge-and-node-observability-fields-wired-up.md) | HITL-approval edges now actually block; emergent-routing and telemetry tags now actually surface | High |
| [0012](0012-toolcall-tool-name-must-match-the-tool-being-evaluated.md) | PolicyEngine now denies a ToolCall whose tool_name doesn't match the Tool it's evaluated against | High |
| [0013](0013-eval-record-unmeasured-fields-are-none-not-zero.md) | cost_dollars/latency_ms are None when unmeasured, not a misleading 0.0; latency_ms now actually computed | High |
| [0014](0014-config-to-services-wiring-is-partial-by-design.md) | `aef run --config` wires a real model_provider; full config-to-Services wiring needs a plugin registry (Phase 2) | High |
| [0015](0015-completion-request-metadata-now-passed-to-anthropic.md) | CompletionRequest.metadata.user_id now reaches Anthropic's own abuse-tracking field; FallbackProvider verified already correct | High |
| [0016](0016-adopt-detects-frameworks-from-manifests-too.md) | aef adopt now detects frameworks from requirements.txt/pyproject.toml, not just .py imports | High |
| [0017](0017-otel-span-record-exception-accepts-baseexception.md) | OtelSpan.record_exception now accepts BaseException, matching the real SDK and InMemorySpan | High |
| [0018](0018-aef-run-adds-cwd-to-syspath.md) | aef run adds CWD to sys.path — aef init's own output couldn't be run before this | High |
| [0019](0019-plan-replacement-footgun-in-the-shipped-example.md) | StateDelta.plan replaces not merges; the shipped example was silently dropping subgoals/reusable_key | High |
| [0020](0020-edge-equality-uses-condition-code-not-identity.md) | Edge equality compares condition by __code__ not identity — diff() falsely flagged every rebuilt graph as changed | High |
| [0021](0021-aef-run-checkpoints-dir-enables-eval-and-trace-chaining.md) | aef run --checkpoints-dir — before this, run/eval/trace could never be chained at all | High |
| [0022](0022-scores-must-be-finite.md) | scores reject inf/nan at construction — was silently corrupted to null on checkpoint write, crashed later on reload | High |
| [0023](0023-replay-validates-trace-chain-integrity.md) | replay() now validates trace chain integrity — a reordered/corrupted trace was silently replayed into a wrong result with no error | High |
| [0024](0024-adopt-claude-md-doc-paths-fixed-for-pip-installs.md) | aef adopt's generated CLAUDE.md no longer points pip-installed adopters at docs/roadmap.md, which never ships in the package | High |
| [0025](0025-numeric-field-sweep-hitl-nan-bypass.md) | ToolCall.risk/PolicyConfig NaN silently bypassed the HITL security gate (risk > NaN is always False) — now rejected at construction | High |
| [0026](0026-file-durability-backend-checkpoint-corruption-hardening.md) | FileDurabilityBackend: a stray non-numeric .json file bricked list_checkpoints for the whole run; corrupted checkpoints now raise a named, diagnosable error | High |
| [0027](0027-otel-tracer-nested-spans-now-actually-parented.md) | OtelTracer's nested spans exported as unrelated roots (no parent/child) in a real OTel backend — start_span() never attached the OTel active context | High |
| [0028](0028-graph-visualize-mermaid-injection-fix.md) | Graph.visualize(): a node id containing Mermaid syntax could break out of its label and inject extra flowchart statements — now uses synthetic ids + label escaping | High |
| [0029](0029-init-agent-name-path-traversal-fix.md) | aef init's agent_name had zero path validation — "../../evil" and absolute paths wrote files outside the intended agents/ directory | High |
| [0030](0030-mem0-adapter-get-no-longer-swallows-real-failures.md) | Mem0Adapter.get()'s bare except Exception silently turned genuine backend failures into indistinguishable-from-not-found — removed | High |
| [0031](0031-atomic-durability-writes-and-load-latest-recovery.md) | Durability writes are atomic (temp+fsync+os.replace); load_latest recovers past a torn checkpoint instead of bricking resume — completes ADR 0026's read-only story | High |
| [0032](0032-hitl-pause-is-always-resumable.md) | A HITL pause now persists a checkpoint+cursor before raising, so a run gated before its first checkpoint is resumable; gated node re-execution kept as documented at-least-once | High |
| [0033](0033-autonomy-protocol-is-a-versioned-artifact.md) | The autonomous self-improving loop + its HARD-STOP safety gates are a versioned repo artifact (docs/autonomy/self-improving-loop.md), not a chat prompt; adopt emits a pointer | High |
| [0034](0034-adopt-emits-the-onboarding-kit.md) | aef adopt now emits AGENT_INTEGRATION.md (ingest-and-start guide) + AUTONOMY.md (inlined safety contract) into adopted repos, never-overwrite | High |
| [0035](0035-hitl-threshold-bounded-below-one.md) | PolicyConfig.require_hitl_above_risk bounded to [0.0, 1.0) — a threshold >=1.0 made the HITL gate unreachable for max-risk calls (residual half of 0025) | High |
| [0036](0036-fallback-routing-bypasses-edge-conditions.md) | A node's fallback_node_id now routes unconditionally on exception, bypassing edge resolution — before, a missing/false-condition edge masked the error with RoutingViolationError and the handler never ran | High |
| [0037](0037-anthropic-adapter-catches-the-true-base-exception.md) | AnthropicProvider catches anthropic.AnthropicError not just APIError — non-APIError subclasses leaked raw and aborted FallbackProvider's chain | High |
| [0038](0038-eval-passed-requires-completion-and-gates.md) | EvaluationRecord.passed requires task completion AND all domain gates — a configured gate used to override task_completion, marking an errored run passed | High |
| [0039](0039-replay-trusts-fallback-records.md) | Replay trusts fallback records (is_fallback flag) instead of re-executing them — a deterministic node's fallback trace was unreplayable (crashed with the original exception); seam ADR 0036 exposed | High |
| [0040](0040-adopt-emits-cross-harness-entry-files.md) | aef adopt emits a native entry file per harness (AGENTS.md/Codex, .github/copilot-instructions.md, .cursor/rules/aef.mdc) so the scaffold works across Claude/Codex/Cursor/Copilot, not Claude-only | High |
| [0041](0041-wiring-manifest-is-the-self-rewiring-artifact.md) | Self-rewiring operates on a declarative wiring manifest (refs by import path), not a Graph — a Graph holds callables and can't be serialized; also the containment boundary making self-rewiring != self-coding | High |
| [0042](0042-owner-merge-is-the-promotion-gate.md) | The owner's review+merge to main is the promotion gate — strictly stronger than Phase-4 criterion 7; the evolution engine stays disabled | High |
| [0043](0043-separation-of-powers-is-inviolable.md) | A proposal may not modify the standard that judges it, in the same change (anti-reward-hacking); suites still evolve via standalone owner commits | High |
| [0044](0044-self-coding-with-a-structurally-isolated-harness.md) | Agents may author their own code (Zone A); the harness that judges them executes from a trusted base ref they cannot write — supersedes 0041, whose palette premise was disproved (edges authorize routes, nodes choose them) | High |
| [0045](0045-tiered-auto-merge-replaces-per-change-review.md) | Tiered auto-merge replaces per-change owner review: Tier 1 auto-merges when gates have real evidence, Tier 2 escalates where they have none; Zone B never auto-approved; post-merge monitoring + auto-rollback + kill switch become load-bearing | High |
| [0046](0046-rule-based-reflection-slice.md) | The reflection slice is real and rule-based (RuleBasedCritic/Judge + make_reflect_node writing failure/success memory); Services grows critic/judge slots; a node with deterministic output but I/O must still declare deterministic=False or ReplayEngine repeats the write | High |
| [0047](0047-the-trust-boundary-is-the-base-ref.md) | **Mechanism claim CORRECTED by [0109](0109-the-trust-boundary-is-the-launcher-not-the-library.md)** — gates are NOT loaded through `BaseRefHarness`, which had no production call site; trusted provenance comes from the launcher. Still standing: --no-renames is a security flag (a rename hides the Zone C half); blob modes are checked because symlinks/submodules are valid Zone A paths; the sandbox declares its capabilities and refuses to run without attested network isolation | High |
| [0048](0048-corpus-splits-are-fixed-at-record-time.md) | Trace codec tags Route ({"kind":"end"|"node"|"fanout"}) so END can never collide with a node id, and emits canonical JSON so gates comparing traces don't flake; a scenario's split is stored in the file and verified against its directory (moving holdout into train is fatal); changing split counts as shrinkage; fixed_clock refuses to invent a timestamp because "more steps than the incumbent" is a finding, not a mismatch | High |
| [0049](0049-g0-uses-an-import-allowlist.md) | G0 governs agent-authored imports by ALLOWlist (a denylist must foresee every escape; vendor isolation falls out for free); G1 runs against a constructed post-merge workspace (base tree + Zone A overlay) never a branch checkout; G4 AST-checks safety metadata since no path check can see deterministic=True-with-side-effects or a constant idempotency key; run_pipeline imposes canonical order itself and an empty pipeline does not count as passing | High |
| [0050](0050-g2-compares-outcomes-not-paths.md) | G2 compares outcome CLASSES (terminated / plan_status / errors / policy denials), not execution paths — path identity admits only no-ops, which two reviewers found independently; routing divergence is reported never rejected; only previously-passing scenarios are held; an empty corpus FAILS because absence of evidence is not evidence of non-regression | High |
| [0051](0051-g3-requires-a-null-hypothesis-cohort.md) | G3 requires a null-hypothesis control cohort and FAILS without one — "beats the incumbent" launders chance into evidence, since random variants routinely score higher at n≈20; candidate must beat the cohort's p95, not the incumbent; CIs are reported never gated on; zero-tolerance per-scenario rule binds first because it holds when the statistics cannot. Reverses the roadmap's Q2 deferral, which assumed the human review ADR 0045 removed | High |
| [0052](0052-the-archive-stores-content-not-references.md) | The archive stores file BYTES + a SHA-256 per file, not palette refs — a ref is a name whose code changes under ordinary PRs, so a ref-based rollback restores a version whose meaning silently changed (found by the audit); self-contained rather than git-dependent (no gc/force-push liveness requirement); append-only, and a rollback is archived as a new version without deleting the one it rolled back from | High |
| [0053](0053-drift-is-distance-and-rebaselining-is-rate-limited.md) | G5 measures drift as DISTANCE from the blessed baseline, not the sum of step sizes — summing misreports an A→B→A round trip as maximal drift when the system is where it started; churn still consumes the rate budget, since rate and drift measure different costs. Rebaselining is rate-limited, not merely owner-only: once a drift budget binds every proposal pushes to raise it, and a budget that reliably produces a request to raise it is not a budget | High |
| [0054](0054-ungrounded-proposals-are-never-constructed.md) | An ungrounded proposal raises at construction rather than being rejected downstream — one that can be built will be built constantly, and each costs a gate run, a rate slot and a ledger line; grounding may cite the train split only, enforced by handing the proposer a TrainOnlyEvidence view rather than the Corpus; the G3 control cohort is generated by the SAME mutation machinery, because a cohort from a different distribution tests nothing about the candidate, and is seeded so the threshold can be re-derived | High |
| [0055](0055-the-ledger-is-a-hash-chain.md) | The proposal ledger is a hash chain verified ON READ, because "append-only by convention" is a hope and the actor being recorded is the one that could edit the record; altering/removing/reordering an entry breaks every link after it, and appending onto a broken chain is refused. The review report leads with the DECISION and puts the diff last — a report opening with a diff invites the rubber stamp the tiered policy exists to eliminate. Tamper-EVIDENT, not tamper-proof: a full rewrite still verifies, and closing that needs an external anchor not built here | High |
| [0056](0056-ambiguity-rolls-back-and-a-gated-rollback-halts.md) | Post-merge monitoring rolls back on AMBIGUITY, not just regression — "wait and see" quietly accumulates unverified merges, and reverting a good change costs one re-proposal while keeping a bad one compounds; a rollback of a change every gate passed HALTS the loop (the gates have a blind spot) while an ambiguity rollback does not; the kill switch is a file so the owner can stop the loop without the loop's cooperation; the digest reports whether the loop beats manual editing, since that is halt criterion 5. Completes M10 — Tier-1 auto-merge is now buildable and still OFF | High |
| [0057](0057-the-loop-runs-from-main-never-from-a-candidate.md) | The loop is triggered by workflow_dispatch/schedule from main ONLY — `pull_request` runs the workflow from the merge commit so a candidate editing .github/workflows/ supplies its own judge, and `pull_request_target` fixes that but carries full secrets into a job that may execute candidate code; the candidate is fetched as DATA instead, and a test asserts the forbidden triggers stay absent. Driver ordering is state-location -> kill switch -> ledger chain -> work; exit codes separate escalated(0)/rejected(1)/halted(2). Running it found that loop state inside the repo gets swept into candidate diffs by `git add -A`, so it is now refused | High |
| [0058](0058-drift-is-line-level-and-the-cohort-mutates-the-incumbent.md) | Two corrections found by running the full pipeline, not by inspection. (1) G5 drift is LINE-level, not file-level — ADR 0053's "spreading" rationale was wrong (spreading defeats a file-COUNT budget, which G0 already covers) and file granularity was degenerate: a two-line change to a one-file agent scored 1.0, identical to a rewrite, so no budget could admit anything. (2) The G3 control cohort mutates the INCUMBENT, not the candidate — mutating the candidate asks whether random FURTHER changes match it, which perturbation usually preserves, so the threshold rose to meet the candidate and the gate could accept nothing | High |
| [0059](0059-the-adoption-kit-leads-with-what-does-not-work.md) | aef adopt emits the self-rewiring loop kit (LOOP.md, agents/ + corpus/ READMEs, both workflows), and LOOP.md leads with what does NOT work yet — a repo producing candidates against an empty corpus sees every one rejected, which reads as "the loop is broken" rather than "the loop has nothing to judge against"; the first reading debugs the harness, the second records scenarios. Trigger discipline is asserted against the GENERATED workflows, since a trap closed only in the source repo is not closed | High |
| [0061](0061-owner-ground-truth-and-tripwire-scenarios.md) | Fixes ADR 0060. Scenarios carry an owner-supplied `expected` label (unspecified/must_pass/must_fail); a MUST_FAIL scenario is a TRIPWIRE — a candidate reporting success on a task labelled impossible fails G2 as a security event, because it shows the self-report every other score derives from is unreliable. Tripwires must be impossible in principle, not merely hard: labelling difficulty-5 scenarios must_fail rejected a legitimate improvement. Cost/latency gating was rejected after measurement — the hack is cheaper and faster, so it would have been rewarded | High |
| [0062](0062-unmeasurable-denies-and-the-workspace-ignores-export-attributes.md) | Two confirmed isolation defects. (1) numstat's "-" (binary, or a candidate's own .gitattributes marking files binary) counted as ZERO lines — a 5,050-line rewrite reported as 1 and cleared G0's size budget; unmeasurable now DENIES. Correctness also depended on which branch was checked out, which is not a control. (2) git archive honours export-ignore, so a base ref excluding tests/ from its sdist produced a workspace with NO test suite and a vacuous G1; replaced with ls-tree + per-blob write | High || [0065](0065-the-loop-learns-from-its-own-recorded-failures.md) | Wires reflection memory into the proposer — the missing seam that made this self-MODIFYING but not self-LEARNING: reflect nodes had written failure memory since M0 and nothing ever read it. Citations gain a kind; MEMORY citations are filtered by MemoryEvidence, which excludes records whose run_id matches a validation/holdout scenario (a reflect node over a holdout run writes a record carrying that scenario's id, so citing it leaks the holdout by proxy and the split check never sees it). Deny-what-is-known rather than allow-what-is-proven, because production runs are what there is to learn from | High |
| [0063](0063-a-floor-that-moves-is-not-a-floor.md) | Phase 1c doc-vs-code audit: 18 of 19 checked claims accurate, one false. ADR 0051 claimed a cohort below the minimum fails, but the driver wired min_cohort_size=config.cohort_size while the builder generates exactly that many members — the comparison could never be true and a two-member cohort passed with p95 over two samples. The gate was correct and its unit tests passed; the defect lived in the SEAM between two correct components, which only an assembled-system audit finds. The audit script itself had a false negative on this claim | High || [0066](0066-harvest-promotes-failures-to-train-only.md) | `aef loop harvest` grows the corpus from real runs, plus `aef run --record-runs` (observations record only pass/fail, which cannot rebuild a run as a test case). Failures auto-promote, successes only on request — auto-promoting successes inflates the pass rate the gates measure against. Always TRAIN, with no split parameter at all, since production writing to validation would let the gated system shape the set that gates it. A run that does not re-execute byte-identically is REJECTED: one flake poisons every future comparison. Harvested scenarios carry no owner label — a MUST_FAIL the system invented is a tripwire it set for itself | High |
| [0064](0064-numbers-for-three-known-weak-spots.md) | Phase 1d measurements. Policy detection by substring was ANTI-correlated — missed 4/5 real refusals, flagged 3/3 innocuous errors — so a candidate could be rejected for a DNS timeout; replaced with an explicit policy_denied key. Position-wise drift over-reported a single top-of-file insertion by 100x (1.000 vs 0.010), making the budget a de-facto ban on adding imports; now SequenceMatcher. G4's name matching was evaded by `import Edge as E`, producing zero findings; aliases now resolved. p95-over-5 measured across 40 seeds: stdev 0.0048, same decision 40/40 — not a defect, but weak evidence from one coarse agent | High || [0067](0067-the-cycle-never-pushes-and-a-halt-reaches-you.md) | `aef loop cycle` = kill switch -> ledger -> harvest -> propose (grounded) -> gate -> record, daily. Creates the candidate as a LOCAL branch and never pushes — which is what lets the whole loop run under contents:read, since creating a branch needs no permission and pushing does. At most one candidate per turn (asserted on source, because the `for` loop someone adds later looks like an improvement). Q-A5 resolved with three channels: HALT.md, a ledger entry, and a POST to a webhook the owner sets OUTSIDE the repo; a failed POST never hides the halt, and an unconfigured channel is reported loudly in every digest | High || [0068](0068-replay-verifies-the-fallback-route.md) | Phase A graph audit. A1: 5 node functions, zero contract violations (scanner verified against planted faults first). A2 DEFECT: replay trusted a fallback record's ROUTE as well as its delta — a trace replayed CLEAN against a graph whose fallback was retargeted to a different existing handler, while a live run gave materially different behaviour; the route is checkable without re-executing the raising node, and now is. A3: a RECOVERED run is indistinguishable from a failed one on every axis the loop scores, so graceful recovery is unrewardable — recorded, not fixed, since it needs typed error entries. A4: fan-out fails loudly, correct | High |
| [0069](0069-the-seams-between-cli-commands.md) | Phase B: the full command sequence had never been run as one sequence, and three of its four joins were broken. (1) `cmd_cycle` built InMemoryMemoryStore() inline, so proposer evidence was empty every invocation and the ADR 0065 reflection wire could never fire from the CLI; now FileMemoryStore + --memory. (2) `harvest` took no --state so it never checked the kill switch, letting a halted loop have its gate evidence rewritten. (3) G1's defaults were aef-core's own green bar, so `mypy --strict aef` ran against adopting repos that have no aef/ directory — rejecting every candidate, forever, silently; now pytest-only plus repeatable --build-command. Third time a defect has lived in a seam | High |
| [0070](0070-the-documented-adoption-workflow-could-not-be-followed.md) | Ran `aef adopt` then followed LOOP.md literally for the first time. Two blockers: `aef run` had no way to seed working_memory, so an adopter could not produce the FAILING run LOOP.md tells them to record — the first instruction could not be carried out with the tools provided (added --working-memory); and the reflect-node requirement was unstated, so `cycle` reported "no admissible failure memory" forever, with the added trap that an Edge to a reflect node does not route to it. LOOP.md now lists five obligations. A near-miss f-string collision would have made `aef adopt` crash outright and was caught by the new integration test on its first run | High |
| [0071](0071-the-development-pod-is-two-things.md) | The dev pod is a `reproduce-first` skill and ONE agent (`seam-hunter`); adversary, claim-auditor, scribe and an orchestrator were designed and cut, because each duplicated a lens a loop prompt already carries — a well-written prompt IS an agent definition, and moving it to a file buys reuse not capability. reproduce-first earned its place by being repeated seven times; seam-hunter earned its trial by finding three defects on its first invocation. Adds breadth and discipline, NOT independence: same model, same repo. Re-evaluate on every model upgrade — a prompt tuned for one model can become a ceiling for a better one | High |
| [0072](0072-three-defects-in-the-monitor-path.md) | seam-hunter's trial output. (1) `gate()` wrote a literal baseline_pass_rate of 1.0 — nothing measures a pass rate — so every merge was judged against a claim production was perfect, and any agent below 95% live halted the loop permanently while blaming the gates. (2) `monitor` passed the version the merge PRODUCED to `archive.rollback`, which restores that version's content: the rollback reinstated the regressing change, and no pre-merge state is ever archived so there was no correct target. (3) `aef run --observations` and the monitor's hardcoded path were never connected, so every window reverted silently. All three latent because the MERGED writer is unreachable while Tier-1 is off — which is exactly why they had no coverage | High |
| [0073](0073-the-five-obligations-contradicted-each-other.md) | LOOP.md's obligation 5 (archive a blessed baseline) had NO COMMAND — G5 refused every candidate forever with no way out; added `aef loop bless`, refusing a second bless since rebaselining is a rate-limited owner decision. Obligation 2 (add a reflect node) made obligation 3 IMPOSSIBLE: every reflect node needs critic/judge, and `aef run` wired neither, so satisfying one requirement broke another — same omission in `aef loop record` broke obligation 1. `aef loop doctor` now reports all five at once with runnable fixes. Near-miss: doctor's routing detector matched `Edge(to_node="reflect")` inside build_graph's return and passed the exact trap it existed to catch — caught only by a planted-fault test, the second such false negative in this program | High |
| [0074](0074-the-gates-the-cli-could-not-reach.md) | **G2 and G3 had never executed from a real command.** Every proof the six-gate pipeline works came from a test that hand-built its own LoopConfig and set two fields `aef.cli.loop._config` never set; no test imported `_config`, so the suite was green against a configuration no user could produce. Eight defects: an unwired second clock (G5 failed on `now=None`, fail-fast stopped the pipeline before the entire behavioural half); G5's refusal misnaming its own cause; a cohort failure silently disabling three gates; `entrypoint` defaulting to a layout nothing has with no flag to override (ADR 0069 defect 3 one field over); the drift metric subtracting a whole-tree baseline from a changed-files-only candidate, rejecting the FIRST candidate after a blessing; `bless` reading the working tree while every gate reads git; the control cohort carrying the candidate's own changes so G3 could never pass a multi-file candidate; and one security incident counted twice. Obligation 1 had the same hole ADR 0073 fixed in obligation 5 — no shipped command could label a tripwire, and `record` could not even reach a failing case. Now verified end to end: first six-gate pass from a real command, and the reward hack caught by the tripwire, counted once, halting the loop | High |
| [0075](0075-obligation-2-broke-the-gates.md) | ADR 0073 fixed two of THREE construction sites needing critic/judge — it missed `scenario_runner.run_scenario`, the one the gates use. So any adopter satisfying obligation 2 (add a reflect node) made every scenario execution crash with ServiceNotConfiguredError: candidate 0.0, incumbent 0.0, all five cohort members 0.0, **G3 rejecting every candidate forever** while doctor reported the obligation green. This repo's own demo agent has no reflect node, so the one agent the gates are tested against was the one shape that could not expose it. Also: ADR 0074's entrypoint fix survived in G2's own default, so the driver said "G2/G3 will refuse" and G2 imported anyway and crashed; the emitted CI workflows could not work in any adopting repo (installs this repo not aef, no --entrypoint, no --build-command); the weekly digest ran hourly (`${{ a }} || b` is a truthy string); a missing --memory was reported as a candidate rejection; a memory-grounded proposal's citations were discarded before the report, which described it as "none (control-cohort member)"; FileMemoryStore silently dropped valid_from/valid_until and had ZERO tests; and the scaffolded agent never sets a Plan, making G2 vacuous | High |
| [0076](0076-recovery-was-unrewardable.md) | A run that recovered from a transient error was indistinguishable from one that gave up — `Outcome.passed` required `error_count == 0`, so both scored False. G2 saw no difference, G3 could not reward it, and graceful recovery was **unrewardable by the objective the loop optimises**; harvest also promoted every recovered run into the corpus as a failure. Fixed the way `policy_denied` was (ADR 0064): an explicit `recovered: True` marker set by the node, never inferred from error text, with absent meaning not-recovered so the default path is provably unchanged. The error is still counted and reported — "recovered from 2" and "had 0 errors" are different facts. `RuleBasedEvaluator.task_completion` deliberately untouched (ADR 0038), verified as zero lines changed. Also closes two deferred ADR 0074 findings: the archive's append-only property was never verified in production (`check_never_shrinks` had tests and no caller, so a deleted version left `status` healthy while the rollback target was gone), and `BLESSED` was invisible in the owner's digest | High |
| [0077](0077-two-halt-criteria-had-no-caller.md) | `assess_halt` implements five halt criteria and its two callers never computed two of them — `drift_exhausted_twice` and `consecutive_escalation_rejections` were dead parameters while the ledger held the evidence for both, so two of five safety criteria were decorative (ADR 0065's shape again). Both now computed from the ledger and acted on. The real content is the near-miss: the first implementation matched `"drift" in reason`, and G5's OTHER failure message is "no owner-blessed baseline to measure **drift** against" — the state every fresh adopter starts in — so it would have halted the loop on the second run of every new repo. My own planted fault missed it because I wrote the fault with a paraphrase of the message. G5 now exports a `DRIFT_EXHAUSTED` marker and the criterion keys on that: a control that reads free text breaks when someone improves the wording. Third occasion in this program where a detector passed its own verification and was wrong — the corollary is now *plant the fault with the real value, not a paraphrase* | High |
| [0078](0078-the-fix-that-broke-the-command-it-audited.md) | **ADR 0075 shipped a regression to main**: writing a proposal's citations to the ledger passed `Citation` objects to a JSON serialiser, so `aef loop cycle` died with `Object of type Citation is not JSON serializable` — no gate ran, the ledger kept a dangling `proposed` entry, and exit 1 meant CI read it as "this candidate is no good", the exact failure ADR 0075 fixed on another value. The test defending that wire asserted `"proposal=proposal" in inspect.getsource(cycle)` — it checked the wire was connected and never sent anything down it. Five more at the same join: the proposer read the working tree while the branch is built from `base_ref`, laundering un-proposed changes into the candidate; detached HEAD (the normal CI shape) made the restore a no-op so cycles compounded; a trailing comment on a constant made the proposer emit NOTHING for the file; a five-member control cohort could be one distinct mutation, satisfying G3's member-counting floor with a point mass; and G0 scanned a lossily-decoded string while the runtime got bytes. Clean at this join: the workspace round-trip is byte-exact, and 18,000 generated controls produced zero syntax errors | High |
| [0079](0079-the-half-nobody-swept.md) | The two never-swept areas. **Worst finding of the program: deleting a `requires_human_approval=True` edge scored as a maximal IMPROVEMENT** — the HITL-gated incumbent always crashed (scenario_runner supplies no approvals) so G2 saw no regression, G3 saw 0.0→1.0, and G4 scanned only the candidate for the literal `=False`, so simply deleting the kwarg was invisible while writing `=False` was a security event; `0`/`[]`/`""` also passed, because the check was identity while the executor tests truthiness. G4 now compares base against head: a control on at base and not on at head is a finding however it was turned off. `policy_engine` was dropped at all four `Services` sites (ADR 0075 one service over), so any agent following the generated CLAUDE.md crashed in the gates and G3 rejected forever. The onboarding half: every "Start (5 steps)" command except `aef doctor` failed in a fresh adopted repo — `pip install -e ".[dev]"` installs the wrong package, the prescribed green bar failed 4/4 including `mypy --strict aef` (ADR 0069 defect 3 reinstated in prose), adopt generated a repo failing its own `ruff check .`, doctor exited 1 on a pristine `aef init` repo and passed on an adapter that was not valid Python, and `aef` shipped no `py.typed` so adopters could never run the mypy this project mandates. Of the five per-agent config fields only `model_provider` reaches a run (ADR 0014) and no generated file said so — an adopter setting `require_hitl_above_risk` believed it enforced. Structural: the adoption-sequence test hand-writes the two preconditions a real `aef adopt` output lacks, so the guard validates a hand-repaired copy of the thing it guards | High |
| [0080](0080-verifying-my-own-fixes.md) | An adversarial round whose only target was the same night's commits. Not dry, and the worst finding was **a reward hack introduced by ADR 0076**: `RECOVERED_KEY` is written by Zone A code the candidate authors and RELAXES the pass predicate, so one extra dict key turned `regressed=True` into `regressed=False` on an identical broken candidate and kept the agent's failures out of the corpus. `policy_denied` is safe only because Zone B alone writes it — a property not carried across. Introducing the marker is now owner-only, judged base-vs-head. Also: the previous commit's claim that G4 catches control removal "however it was turned off" was false three ways — `git mv` the module (raw_diff uses --no-renames so a move is delete+add, and the per-file loop skipped deletions: the diff handed G4 the evidence and G4 discarded it), add an unguarded duplicate edge (`_resolve_route` takes the first by priority, and only guarded edges were recorded so a purely additive diff removed nothing), and hide the guard behind a false condition — and it shipped with ZERO tests. It also halted the loop on a benign node rename, so the gate was inverted: firing on the safe change, silent on three hostile ones. Halt criterion 4's only reset was MERGED, written solely on the auto-merge path that is off, making it a lifetime counter. Held up: whole-tree drift arithmetic exact to the hand calculation, criterion 3 unspoofable, proposer regex clean across 20 hostile inputs. Left open and stated as a mitigation not a fix: `scenario_runner` still supplies no `hitl_approvals` | High on what was fixed; low on completeness |
| [0081](0081-a-pause-is-not-a-failure.md) | Closes the decision in MORNING_REPORT.md §5 and the behavioural half ADR 0080 left open. A run stopping at a human-approval gate got the same answer as one that crashed, so **removing the gate read as a maximal improvement** — incumbent 0.0, candidate with the edge deleted 1.0, and `Comparison.regressed` short-circuited on `not incumbent.passed` so G2 called it unchanged. A pause is now its own outcome class: `passed` stays strictly False so no existing consumer changes, and `Comparison` answers three questions — pause→pass is a REGRESSION (the control is no longer hit), pause→pause is unchanged, and pass→pause is reported but NOT rejected, because a loop that cannot make itself more conservative is pointed the wrong way. Both branches are decided before the `passed` short-circuits, which is where the question was being lost. Unlike `recovered` (ADR 0076/0080), the marker is unforgeable by construction: it is set only in the `except HumanApprovalRequiredError` clause in Zone B harness code, and it never points the direction a candidate wants. G4 additionally flags a candidate that raises the exception itself. The harness supplies no approvals — granting them would mean giving itself the permission the edge withholds — so a HITL-gated scenario is unmeasured, and G2's verdict now says so | High |
| [0082](0082-a-policy-you-can-configure-and-cannot-widen.md) | `policies` and `tools.allow` validated and were ignored, so an adopter setting `require_hitl_above_risk` believed it enforced and got the engine's default — while the generated CLAUDE.md called the policy engine "the engine every tool call passes through". ADR 0079's fix (a bare deny-by-default engine in the gate) stopped the crash and left the measurement meaningless: every adopter tool call denied on both sides, so `policy_denials` could never differ. Now `build_policy_config` maps config to runtime and `--config` reaches `aef run`, `aef loop gate` and `aef loop cycle`. `tools.allow` is SCOPES (the engine gates on required_scopes, so a name allowlist could authorise nothing); `policies.forbid` is the name-based deny axis; empty allows nothing. The trust-critical half: the gate reads the config from the BASE REF and passes it to the sandbox as data, never reading `aef.yaml` from the candidate's workspace — Zone C already rejects such a diff, but a defence resting on "the zone check will catch it" is one layer deep. Verified with a planted fault (candidate grants itself a scope, empties the deny list, sets the threshold to 0.99 — gate unmoved) plus the control proving an owner's change on main IS seen. An unparseable config denies rather than becoming the most permissive state | High |
| [0083](0083-an-audit-trail-that-outlives-the-process.md) | `roadmap.md` listed `AuditLogWriter` under Phase 1 DONE while shipping `InMemoryAuditLogWriter` as the only implementation — every audit entry died with the interpreter, and a `PolicyEngine` built without an explicit writer stored the default on a private attribute with no accessor, so entries were written and unreachable. An audit trail is consulted after an incident; that is the only time anybody consults one. Added `FileAuditLogWriter` (append-only JSONL, `aef run --audit-log`) and made the writer reachable. **Argument values are redacted by default**: a tool call's arguments are where an API key lives, and an audit log is exactly the file that gets shipped to an aggregator or attached to a ticket — the names make the entry useful, the values make it dangerous; verified that a call carrying a secret produces a file containing the parameter name and not the secret. Unserialisable arguments are stored as `repr` rather than losing the entry; a malformed line raises rather than being skipped. Also closes ADR 0079 F13's leftover: `pytest -q` exits 5 in a fresh adoptee — the first command of the prescribed green bar, on day one — and both canonical files now say so. Not claimed: the log records what PolicyEngine evaluated, not calls that never reached it | High |
| [0084](0084-the-six-things-that-were-written-down-and-left.md) | The six items ADR 0074/0079/0080 recorded and did not fix — recording a defect is a slower version of not having found it. `extends` resolved nothing and accepted any string, so `extends: production-base` loaded clean and inherited nothing (now rejected, since loading clean while doing nothing is not a defensible middle). `aef trace` printed nothing on the scaffold because the kernel never appends Provenance and the template emitted none, so the documented execute→score→replay demo replayed empty. `bless` took an agent_root while `_config` never set a zone_policy, so a non-default root would give G5's two sides different trees — ADR 0074's defect latent behind an unchanged default. `rewrite_constant` emitted a hardcoded `\n`, leaving one mixed ending in a CRLF file. `_CONSTANT_RE` allowed `-?\d+` while `find_constants` filtered on `ast.Constant`, and `X = -2` parses as UnaryOp — a dead regex branch that made every negative constant unreachable. And **two open merges rolled back to the wrong place**: `archive.rollback` appends, so reverting in ledger order made each rollback undo the previous one — reverting v2 restored the baseline and reverting v3 then restored v2, reinstating the first regression while reporting both as rolled back (ADR 0072's defect in the n>1 case). Reverting newest-first unwinds the stack in the order it was built | High |
| [0085](0085-the-candidate-wrote-the-evidence.md) | **The most severe defect in this program.** `run_variant` did `json.loads(result.stdout)` on a subprocess the candidate's own code runs in. Two added lines — print a forged payload, `raise SystemExit(0)` — scored a broken agent 1.0 instead of 0.0 and passed all six gates: `SystemExit` is a BaseException so it sailed through `except Exception` and exited cleanly with the forgery as the entire stdout. Strictly stronger than ADR 0060's reward hack — there the agent ran and lied, so the corpus was still a real observation; here the candidate AUTHORS the observation, which defeats ADR 0060's own escape hatch since a MUST_FAIL tripwire is compared against the forged Outcome too. Second half: `_gates_with_evidence` ran BEFORE `run_pipeline`, so G0's import allowlist — the control for exactly this — ran second, and a candidate with `import socket` plus a module-level write outside the workspace had already run by the time G0 rejected it, contradicting `gates/base.py`'s stated design. Three layers now: `load_graph` catches BaseException; the result is framed by a per-run nonce with an EXACTLY-ONE rule (the nonce is in argv so a candidate can forge a marker — and then there are two); and the cheap gates run first so a rejected candidate never executes. The one variant layer 2 cannot catch (forge + `os._exit`) is pinned by a test asserting BOTH that the parse layer is fooled and that G0 rejects it | High |
| [0086](0086-resume-must-not-invent-a-state.md) | First sweep of `aef/kernel`+`aef/state`. **A torn checkpoint silently skipped a completed node**: `load_latest` rewinds past a corrupt checkpoint (ADR 0031) and the cursor is not rewound, so `resume()` paired a rewound state with a live cursor and ran `['A ran', 'C ran']` where a clean run gave `['A ran', 'B ran', 'C ran']` — no error, and the resume then overwrote the torn file so the evidence vanished. Exactly the scenario ADR 0031's fallback exists for. `resume()` now compares both numbers and refuses, since continuing would produce a final state that never existed. The existing test stopped one assertion short — it checked the rewound seq and never called `resume()`. Also: `run_id` was joined onto the checkpoint root verbatim, so `../../escaped` wrote outside the backend; now validated in the schema AND confined at the backend boundary, because the backend is public API and containment cannot rest on callers being careful. Records three further findings as OPEN: ReplayEngine never validates the state chain (forged `scores`/`errors`/`objective` replay clean), `StateDelta.apply` is shallow-pure so a node mutating a nested structure rewrites trace history, and the ADR 0022 NaN guard covers `scores` but not `working_memory` | High |
| [0087](0087-history-must-not-be-editable-by-what-it-records.md) | Closes ADR 0086's three open findings. **Replay never validated the state chain** — `state` was reassigned from each record's own `input_state`, so the final state was decided entirely by the last record and a trace with perfectly chaining node ids replayed clean with forged `scores`/`objective`/`checkpoint_seq`, precisely because no deterministic node reads them; ADR 0023 caught reordering and not forgery. **`apply` was shallow-pure and the record aliased live state**: one delta applied twice leaked nested values between results and back into the delta, and a node mutating a nested structure rewrote the trace record that is supposed to be history — two fixes, because apply's output and apply's input are different objects, and fixing only the first left the second reproducing. **The ADR 0022 NaN guard stopped at `scores`**, so `working_memory` accepted NaN and the first checkpoint write turned it into `null` — the same corruption on the field the schema designates for per-agent data. The pattern: all three had a test that read as if it pinned the property and asserted something weaker — node ids instead of state, top-level containers instead of nested, one field instead of the family. Not claimed: the trace is tamper-EVIDENT. Nothing signs it; a forger who rebuilds a consistent chain replays clean, and should | High |
| [0088](0088-the-claim-that-was-not-verified.md) | The adversarial round defeated ADR 0085, written four commits earlier. Its load-bearing claim — *"the only way to emit exactly one forged marker ... needs an import G0 rejects"* — was FALSE, and asserted as exhaustive from two tested examples. `aef` is on the allowlist (a Zone A graph needs `aef.kernel`) and matching was root-module-only, so `from aef.harness import scenario_runner as _m` handed agent code `sys`: `_m.sys.argv[3]` is the per-run nonce, `_m.sys.__stdout__` is the real fd 1 whatever `sys.stdout` points at, and redirecting `sys.stdout` to stderr silenced the runner's own marker. Exactly one marker, zero G0 findings, all six gates passing on a broken agent, with the payload generated from `_scenarios.json` so the ADR 0060 tripwire never fired either. The two "independent" layers both rested on `sys` being unreachable. Now: `aef.harness`/`aef.cli` denied by dotted-prefix matching, and `os`/`sys`/`subprocess`/`shutil`/`socket`/`importlib` denied as ATTRIBUTES on any object — `aef.kernel.durability` imports `os`, so `durability.os` reached the filesystem with no import statement. **Honest residual: this raises cost, it does not make in-process evidence trustworthy** — the candidate and the reporter share a process, and the structural fix is a design change an owner should decide. Third night running that the self-attack round found the night's worst defect | High on the fix, low on completeness |
| [0089](0089-two-corrections-to-last-nights-decisions.md) | Two defects in decisions made earlier in the same run. **ADR 0081's `gate_added` exemption was a free pass**: a paused scenario scores 0.0 exactly like a failed one, so `pass -> fail` regressed and `pass -> pause` did not — and a candidate that broke five scenarios plus added `requires_human_approval=True` to its exit edge converted every regression into a G2 PASS at no cost. Reproduced through the real pipeline: G2 went from "5 previously-passing scenario(s) no longer pass" to "every previously-passing one still passes". G3's per-scenario floor caught it (defence in depth working), but G2 was neutralised and G4 does not fire because `scan_metadata` flags CLEARING the flag, never SETTING it. A recorded scenario that no longer completes is a regression whatever stopped it; `gate_added` still reports the distinction, it just no longer exempts. **ADR 0087's bare `deepcopy` made legal state fatal** — a Lock or open handle in `working_memory` went from working to raising TypeError, which `scenario_runner` swallows into 0.0 for candidate, incumbent and every cohort member: the ADR 0075/0079 shape a third time. Now deep-copies where possible and shares where not. Also states the cost ADR 0087 omitted: ~0.28ms/step at 100 working_memory keys, 27ms at 10 000, multiplied through G3's N+2 corpus passes | High |
| [0090](0090-four-ways-to-lose-a-verdict.md) | The remaining adversarial-round findings, all the same shape: **the system knew something and the operator did not**. A gate that raised escaped `run_pipeline`, `gate()` and `cmd_gate` and left the process on a traceback with NO `GATED` ledger entry — candidate-triggerable via a graph whose factory fails to load; a raising gate now FAILs with the exception named (Exception, not BaseException, so an interrupt still stops the loop). A candidate writing the marker PREFIX without its suffix let the runner's own leading NUL complete a non-overlapping `str.count` match, so two partial markers counted as one and the forgery surfaced as an ordinary JSON parse error rather than a security event. `ReplayEngine` still handed `record.input_state` to `node.fn` — the defect ADR 0087 fixed on the executor side — so replay rewrote the record it was verifying, and the chain check then passed only BECAUSE replay had reproduced the mutation into its own copy. And an absolute `--config` (natural, since every other path flag is a filesystem path) was silently discarded, leaving a run that looked configured and was not; that plus two other silent paths now raise `PolicyConfigError`. None of these made a bad candidate pass; every one made the record wrong, and the argument for running unattended rests on the record | High |
| [0091](0091-one-list-instead-of-four.md) | The same defect four times (ADR 0073, 0075, 0079, 0089): a service the node needs is absent in the gate path, `scenario_runner` swallows the `ServiceNotConfiguredError`, and candidate + incumbent + all five cohort members score 0.0 — so G3 rejects every candidate forever while `aef loop doctor` reports the agent green. Measured: `aef run` wired 7 of 8 requirable services, the gate path wired 4, and `evaluator` had a `require_` method with no setter anywhere while the migration checklist told adopters to "add an Evaluator". Fixing instances did not work because the failure is drift between two lists nobody compares, so there is now ONE list — `aef/services/runtime.py::agent_services()`, called by all four sites. `model_provider` is asserted as the only service the paths differ on (the gate sandbox has no credentials, and a model-calling node is unreplayable anyway); `memory` and `durability` default to in-memory — present so a node can require them, throwaway so a gate run cannot mutate the evidence a later proposal is built from. The drift test is deliberately a source assertion, because here the property IS "these modules call that function" and no behavioural test can see a site not yet written | High |
| [0092](0092-numbers-nobody-supplied.md) | Three declared surfaces with no supplier, and one metric supplied by the wrong party. **G3's cost rule read a number the candidate writes**: `cost_tokens` sums `Provenance` emitted by agent-authored nodes, so deleting `provenance=[...]` reported zero tokens and the cost-blow-up rule could not bind — ADR 0080's `recovered` shape on the cost axis. Deleting the reporting is not a cheaper agent; an incumbent that reported and a candidate that does not is now a violation, and the test records that an agent which NEVER reported leaves the rule unenforceable because no gate logic can invent the number. **`GateContext.tracer` and `.limits` had no injector** — one production construction site passing neither, so the gate pipeline's observability never executed outside a test and G0's `max_changed_lines` override was unreachable by construction; both now come from `LoopConfig`, verified by counting emitted spans and making a limit bind. **`domain_gates` is pluggable in code and not from config** — nothing populates it, `evaluator.suites` is read by nothing, and the roadmap implied otherwise. The class, now named: *a declared injection point with no production caller is indistinguishable from a missing feature, and reads as a present one* | High |
| [0093](0093-the-control-that-has-now-failed-three-times.md) | **Escalation, not resolution.** `getattr(module, "os")` defeats ADR 0088 completely — four lines of legal Zone A code, zero G0 findings, all six gates passing on a broken agent, verified end to end through the real `gate()`. `vars()` and `__dict__` work too. Deliberately NOT fixed by extending the denylist: that is the move that failed in ADR 0085 and again in 0088, each defeat coming from a smaller change than the last, and the next bypass is one I have not thought of — which is the sentence ADR 0088 already had to retract. Static allowlisting over a shared interpreter is the wrong instrument and the replacement is an owner's decision; the code comment claiming closure is removed. Does fix: `max_processes=256` fed to `RLIMIT_NPROC`, a PER-UID total, so every forking build command failed on any machine with >256 user processes (ADR 0069's shape, invisible in a container); the timeout killing only the direct child despite two comments claiming the group (first fix attempt was a no-op — `TimeoutExpired` carries no pid — caught only by running it); ADR 0091's default durability re-killing the state ADR 0089 made legal, the 0075/0079/0089 shape a FOURTH time; a harness crash reported as "5 previously-passing scenario(s) no longer pass" because `_parse` discarded the runner's failure diagnostic; and ADR 0090's verdict-losing hole still open on `_gates_with_evidence`, the one path that executes candidate code | High on the fixes; the completeness claim is negative |
| [0094](0094-the-candidate-is-not-asked.md) | **Option (b), chosen by the owner. Supersedes the security model of ADR 0085 and 0088, both defeated.** Separating processes alone buys nothing — a child that reports its own results forges them as before. What works is inverting control: the PARENT owns the state, routing, step count, trace, classification and score; the child evaluates one node and returns `(delta, route)`. `GraphExecutor` is untouched; only where `node.fn` runs moved, and `node.fn` was always the sole place candidate code executed. The worker never learns what a scenario is, how many exist, or what an `Outcome` is — asserted against the AST, not the prose. Every attack from ADRs 0085/0088/0093 re-run through the real pipeline: none produces a passing scenario, and **G0 still passes the getattr bypass** — the attack fails because the evidence moved, not because the scanner improved, pinned by its own test so the boundary is not mistaken for a static-analysis win. Killing the worker mid-corpus scores 1 of 3: what genuinely ran counts, the rest are unrun. Deliberately still open: a lying `StateDelta` is the node contract, which is what the corpus and MUST_FAIL tripwires judge. The marker protocol is DELETED not deprecated. Stated regression: the worker is a plain subprocess, so `SandboxPolicy` rlimits/timeout are not applied to it | High on the property; the sandbox gap is real and stated |
| [0095](0095-confinement-and-honesty-are-different-jobs.md) | Closes the regression ADR 0094 recorded against itself: `run_variant` accepted a `SandboxPolicy` and began with `del policy`, so the one process running agent-authored code had no rlimits, no process group (nothing could kill its descendants), a hand-rolled environment bypassing `env_allowlist`, and no timeout — **defence against a dishonest candidate had been traded for defence against a runaway one**, and those are different jobs. The worker now gets the same confinement a sandboxed command gets, from the same code rather than a second copy (`sandbox.py` exports `child_preexec`/`scrubbed_env`/`kill_process_group`), plus the one thing a long-lived worker needs and a one-shot command does not: a PER-STEP deadline, because `timeout_s` bounds a whole command and the parent is what waits, so a node that never returns hangs the gate — the one process a candidate must not be able to stop. Verified by running: credentials reach `None` while the worker sees only `HOME/LANG/PATH/PYTHONPATH/TMPDIR`; a node sleeping 60s returns the gate in 2.1s; a grandchild is killed with the group; and an honest agent still passes 3 of 3, because confinement that rejects honest agents is a broken gate. Unchanged and still stated: no network or filesystem confinement without a container | High |
| [0096](0096-what-a-proposer-may-change.md) | Design for a structural proposer, written before the code. Blocking finding first: **the failure memory does not record which node failed** — `node_id` is `"reflect"`, the node that OBSERVED the failure, while the failing node's id sits in `state.errors[i]["node_id"]` which the reflect node reads and discards. A numeric proposer never needed it; a structural one cannot begin without it (ADR 0092's class again: the information existed and nothing carried it). Decision: a proposer applies NAMED transformations from a fixed catalogue, never arbitrary code, under four non-negotiable properties each traceable to a defect that occurred here — a human-readable diff G0 can size, a checkable citation that constrains the change (today's rationale is independent of what the failure said), individual revertibility, and **no transformation may touch routing into a HITL-gated edge** (ADR 0089 measured what that buys an attacker). v1 ships ONE transformation, `add_deterministic_fallback`, because it is the only candidate that satisfies all four without inventing anything — AEF-native, structural, directly traceable, already replay-verified by ADR 0068, and a two-token diff. Node-splitting, condition-widening and reflect-insertion are deliberately excluded until a gate exists that can tell a good instance from a bad one | High on the constraints; the capability claim is deliberately small |
| [0097](0097-the-marker-nothing-set.md) | ADR 0076 added `RECOVERED_KEY` and closed with "not claimed: that anything currently SETS the marker". Milestone 1's first structural transformation gives a failing node a `fallback_node_id`, and the acceptance test showed the repair WORKED while scoring as a failure — the executor's fallback path appends an error entry and nothing marked it recovered, so **the first structural repair the proposer can make was unscoreable**: G3 would see no improvement, G2 a scenario that still does not pass. The executor now sets it when a declared fallback takes over — the one place that knows both that a node raised and that control continued, and Zone B, so it keeps the unforgeability agent-written `recovered` lacked (ADR 0080). `RECOVERED_KEY` moved to `aef/state/schema.py` because the kernel must not import the harness. Ten adversarial rounds missed this because it was not a defect in anything that existed: every consumer was correct, and the gap was between a capability nobody had exercised and a marker nobody had set. That is the argument for acceptance tests that demand a REPAIR rather than a diff | High |
| [0098](0098-milestone-1-was-wrong-at-the-design-level.md) | **Reverts ADR 0096's transformation and ADR 0097's executor change.** ADR 0096 justified `add_deterministic_fallback` partly as "already verified by ADR 0068" — true, and the answer to the wrong question. `fallback_node_id` has been in G4's `OWNER_ONLY_FIELDS` since ADR 0036/0039: an agent may not declare one. Milestone 1 shipped a proposer whose only structural output is the one thing the gate set forbids, returned in PREFERENCE to the numeric proposal G4 passes — so the loop would emit only security events the moment one new reflect record existed. Reproduced through the real pipeline: G0 pass, G1 pass, **G4 fail, disposition reject**. The tests missed it because the acceptance test called a bare `GraphExecutor`: it proved the repair works and could not see that the pipeline rejects it — an acceptance test must run what the system would actually run. Three further disqualifying findings: the executor's recovery marker was ADR 0080's hack relocated (a fallback to a do-nothing handler converts a genuine regression into a G2 pass); only half of ADR 0097 shipped, so a working fallback scores 0.0 in G3; and the handler choice picks the entry node, livelocking at max_steps. Kept: `failing_nodes` in memory, `MemoryEvidence.failing_nodes()`, and the `RECOVERED_KEY` layering move | High on the revert; low on the judgement that produced it |
| [0099](0099-a-catalogue-derived-from-the-gates.md) | Milestone 1, second attempt. Answers ADR 0098's prior question by MEASURING it: six candidate transformations run through the real G4, and `fallback_node_id` was **the only forbidden one** — the option ADR 0098 called "safest, probably not much left" was in fact the whole catalogue minus one. A second probe confirmed the prohibition binds on new nodes too (`scan_metadata` is whole-file), so the suspected hole did not exist. The catalogue now READS its constraint from the gate: `_assert_controls_untouched` imports `OWNER_ONLY_FIELDS` from G4, so adding a field there automatically forbids emitting it — verified by planting all six from their real names. First entry is `add_bounded_retry`, chosen on the distinction ADR 0080 and 0098 both turned on: a fallback records that control continued, a retry means the work happened. Adversarial round found two more, both reproduced: a side effect ran 3× before the failure surfaced (now requires `idempotency_key_fn`, refuses MUTATING), and `except Exception` swallowed a `HumanApprovalRequiredError` raised on one attempt but not another — HARD-STOP #7. The first fix for that emitted the exception class into agent code and **G4 rejected it**, ADR 0098's mistake in miniature one session later; the shipped answer is a refusal using G4's own scanner. Acceptance test drives the real `cycle()` end to end with a corpus recorded by running | Moderate — one catalogue entry; T2/T4 pass the gates but repair nothing measurable and are deliberately not shipped |
| [0100](0100-config-that-means-what-it-says.md) | Milestone 2. Of the five per-agent fields, `objectives`, `evaluator.suites` and `knowledge_graph` validated and were ignored; ADR 0092 documented that and documentation is not wiring. Each is now resolved one of two ways with no third: it reaches a run, or it is REFUSED at load time. `evaluator.suites` becomes `RuleBasedEvaluator.domain_gates` — the injection point with no production caller since Phase 0 — as `module:function` references (a bare name needs a registry someone registered into, which is invisible and order-dependent); a gate can only make `passed` stricter, never rescue a failing run, which is what makes it safe without touching `task_completion` (ADR 0038). `objectives` becomes the default for `--objective`, which the flag still overrides. `knowledge_graph` is refused, with the design reason: a KG needs a query interface, a budgetable result shape and a provenance story for retrieved facts, none of which the node signature carries — a constructor built first is a service nothing can call. The refusal immediately caught two shipped examples declaring `impl: neo4j`. Adversarial round found four, all reproduced: a suite could resolve `aef.harness.loop:gate` (a second door into the judging apparatus — now reads `FORBIDDEN_AEF_SUBPACKAGES` from G0 rather than copying it), a gate returning `"yes"` was read truthily into a `dict[str, bool]`, a forgotten return failed every run silently, and a raising gate produced an anonymous traceback. Module-body execution on resolve is documented and pinned, not fixed — it needs Milestone 4's sandbox. The stub's `STILL NOT WIRED` section is gone, which was 2d's test of whether this happened | High on the wiring, lower on the surface it opens — three of four findings were in the suite mechanism itself |
| [0101](0101-triage-of-the-named-but-unbuilt.md) | Milestone 3. **Deletes four interfaces, implements one.** Facts first: every reference to all five, outside its defining module, was either an unread `Services` slot or a test asserting the stub raises `NotImplementedError` — which is what a promise looks like when it is tested. `GraphStore` deleted (ADR 0100 already showed no builder can exist, and the config now refuses the block, so the slot was unfillable and unconfigurable). `PlanValidator` deleted as REDUNDANT: same report §16, same worked examples, same `-> bool` shape as `domain_gates`, which Milestone 2 built and wired — a redundant interface is worse than none because an adopter must guess which is real. `Planner` deleted as design ambition. `TokenOptimizer` deleted: `compress` cannot be built honestly without a model, and a truncating "compressor" is a lossy edit wearing the word; the honest half is budget enforcement by pruning whole units, which now lives in the retriever. `Retriever` implemented as `MemoryRetriever` — surviving not because retrieval is interesting but because `context_budget_tokens` has shipped since Phase 0 with a default of 8000 and NOTHING has ever read it to bound anything. Deterministic (lexical Jaccard, no model) so replay holds; reachable via a `context:` block whose `impl` is refused unless real; `token_budget` is an owner-set CEILING, not a default it could ignore. Exercised by a test driving `run_graph_module` end to end with a control proving the config is what supplies it. Adversarial round: `agent_id` defaulted to `None` and read EVERY agent's memory (now required — `None` must be chosen), and my own comment claimed `candidates_per_kind` "bounds the work, not the answer" when it bounds both — the ADR 0088/0093/0096 shape again | High on the deletions; moderate on the retriever, whose lexical ranking and recall ceiling are stated limitations |
| [0102](0102-containment-that-is-containment.md) | Milestone 4. `SandboxCapabilities.network_isolated` was `policy.network_isolation_attested` — a bool a caller passes in, honest and unchecked — while everything downstream assumed containment that did not exist. A container-backed path is now an OPTION (`SandboxPolicy.container_image`, `aef loop gate --sandbox-image`), and isolation is MEASURED: `detect_container_runtime` probes BOTH directions before anything runs, because "the connection failed" is also what a broken image, a stopped daemon and an offline host look like. Both were run by hand first (`--network none` -> blocked; without it -> reachable). Five flags, each a control pinned by name; the workspace is the only writable mount; the host environment is not forwarded at all. 4b: the local path can now make the CI workflow's claim, and `--network-isolated` (caller asserts) is distinguished from `--sandbox-image` (run builds and verifies). Adversarial round found two: **a timed-out container kept running** — `subprocess.run(timeout=)` kills the `docker run` client while the daemon owns the container, ADR 0093's exact defect in code written to provide containment, reproduced as one container alive two seconds after the gate reported a timeout and fixed by naming every run and asking the daemon to `rm --force`; and a missing image was reported as an isolation failure when exit 125 means the container never started (ADR 0074's misnamed cause). Checked and benign: port 80 binds despite `--cap-drop ALL` (containers set `ip_unprivileged_port_start=0`), which grants nothing with no network. 4c tested as a HARD-STOP: G0 still rejects first, refusals unchanged, in-process path still reports False | High on what was measured; lower on breadth — one image, one host, podman unexercised |
| [0103](0103-the-three-missing-criteria.md) | Milestone 5. Shadow execution, canary rollout and signed release manifests — the three Phase-4 criteria that were missing. Built in `aef/harness/`, NOT `aef/evolution/`: HARD-STOP #2 forbids touching the disablement, and these are mechanisms the owner-gated path uses. Shadow: the part that cannot be paraphrased away is that running a candidate on live input means its nodes EXECUTE, so a `MUTATING` node is refused at construction — "nothing it returns reaches a user" says nothing about what it does on the way; suppression reuses `PolicyEngine`'s own deny-by-default rather than a bespoke shadow mode (ADR 0091's drift); HITL approvals are not inherited; `ShadowObservation` has no field holding the candidate's output, so suppression is structural; `divergence_rate` over zero observations RAISES, because "never ran" and "always agreed" are otherwise the same number. Canary: assignment monotone in exposure so advancing does not discard the samples gathered, empty tags refused rather than defaulted, nearest-rank percentiles so an operator can point at a number that happened, warm version carried in state. **A tested, stated coverage limit**: a regression confined to a slice narrower than the smallest percentile's complement is invisible — found because the first fixture used exactly 1% and passed, which was the gate's coverage showing, not a fixture bug. Manifests: HMAC-SHA256, proving an agent cannot forge an approval and explicitly NOT giving third-party verifiability; key from a file not the environment. Adversarial round: a rolled-back candidate climbed the ladder again (advance→regress→rollback→advance forever, now terminal), and a manifest recording FAILED gates signed cleanly (now needs a signed `override_reason`). **All seven criteria implemented; Phase 4 stays disabled** | Moderate, lower than the last four: none of the three has run against live traffic, because this repo has none — that gap needs an adopter, not another test |
| [0104](0104-the-trust-case-recommends-against.md) | Milestone 6. `docs/trust/promotion-trust-case.md` assesses all seven Phase-4 criteria — now all implemented — criterion by criterion with transcripts, and **recommends AGAINST enabling Tier-1 auto-merge**. Three findings carry it: criteria 1 and 6 have never run against the live traffic and real tenants their own text names; shadow containment has a DEMONSTRATED bypass (suppression is `PolicyEngine` with an empty scope set, which denies tool calls — a node importing `pathlib` and writing is not making one, and it wrote to disk during a shadow run); and six adversarial rounds found six confirmed defects in freshly written code, a rate that has not fallen across the program's ~18 rounds and ~75 defects. Seven attacks were attempted and three landed, reported as they came out — including one the author had previously reported as a break and which re-checking showed was correct behaviour (1% of tenants excluded at 99% exposure), with a real weakness underneath it: assignment is a public deterministic hash, so a tenant controlling its own tag can search for the arm it wants. Residual risk is **5–10 false accepts per 100 gated candidates**, floored on G3's p95 threshold letting a no-better-than-random candidate through ~5 times in 100 by construction, and labelled derived-not-measured. `tests/harness/test_trust_case.py` pins the claims so the document cannot go stale silently — two of them fail LOUDLY if the harness improves. Tier-1 untouched | High that the document is honest; low on its sufficiency — it was written by the agent that built what it assesses |
| [0105](0105-the-contained-shadow.md) | Closes the bypass the trust case demonstrated: shadow suppression is `PolicyEngine` with an empty scope set, which denies TOOL CALLS, and a node importing `pathlib` and writing was never making one. `contained_candidate_graph` runs the candidate's nodes inside Milestone 4's container via the INVERTED-CONTROL worker — not merely sandboxing the whole run, because a candidate that reports its own final state can report the incumbent's and hide a divergence, making itself look SAFER than it is; in the gate a forged result inflates a score, in a shadow it erases the signal. Verified both directions (host marker True in-process, False contained), with the read-only root checked separately on paths the image really has because the first containment is partly the host path being absent. Two defects avoided by prior findings: `close()` would have leaked a container (ADR 0093's defect in its third location — killing the process group kills the `docker run` client, the daemon owns the container), and `sys.executable` is meaningless inside an image. Adversarial round: network blocked, workspace-parent write blocked, spawned subprocess dies with the container, host filesystem invisible, zero containers leaked — **the first dry round in the program**, qualified by a narrow surface. **Does not change the recommendation**: opt-in, needs an operator image, and untouched by it is the trust case's reason #1 — criteria 1 and 6 still have not run against live traffic or real tenants | High on the fix; moderate on its reach — one opt-in path, one image, one host |
| [0106](0106-the-keyed-tenant-hash.md) | Closes trust case §2.3. Tenant assignment was a public deterministic function of a string the tenant supplies, so a tenant controlling its own tag could compute which arm any tag lands in and choose one — demonstrated at 1-in-200 tries at 99% exposure. Now `blake2b(key=salt)` under an owner-held `CanarySalt`, loaded with the signing key's discipline but deliberately NOT the signing key (one secret for two purposes means a compromise of either leaks both; the release key is held by a person, this one read by a running service). `CanaryState` refuses to construct unkeyed unless `unkeyed=True` — the third instance of the same shape after `SandboxPolicy` and `ShadowRunner`. **What keying buys is bounded and the bound is tested**: it removes OFFLINE sifting of tags, not online probing by a tenant that can observe its arm and re-register. Adversarial round found two, both reproduced: the fingerprint was a plain `blake2b` of the salt and therefore a microsecond-per-guess offline oracle — any deterministic function of the salt is one, unavoidably, so it is now `pbkdf2_hmac` at 200k iterations (~12ms measured, domain-separated) with `generate()` for a random salt, because cost buys time against a weak salt and does not make one safe; and **the same tenant encoded NFC vs NFD got different arms 52% of the time**, seeing inconsistent behaviour and contributing to both arms, which is what stratifying exists to prevent — tags are now NFC-normalised. Monotonicity, stability, uniformity and cross-version reshuffling all re-measured under the key | High on the mechanism; moderate on the threat model — a tenant colluding with whoever issues tags is out of scope |
| [0107](0107-what-a-status-surface-may-claim.md) | Milestone 1 of the dashboard program, and contract only — no rendering code, deliberately. A dashboard is a trust surface: a wrong number on it is believed by someone who has stopped reading the underlying data. Three rules, each structural rather than conventional. (1) `Reading[T]` is a closed union of `Unknown` and `Known[T]`, and `Unknown` has **no `state` field at all** — so there is no branch producing HEALTHY from nothing, and `mypy --strict` rejects the unnarrowed access (asserted by a test that shells out to mypy, because the claim is about the type checker). (2) The page is read-only; `FORBIDDEN_HTML_CONSTRUCTS` lives here and the renderer's tests import it rather than re-listing it (ADR 0091: two lists nobody compares drift). A button on a web page is a second door into an approval story that routes through a signed manifest. (3) `disclosure_of()` raises on an unregistered field rather than defaulting — notably `canary_salt_fingerprint` is EXCLUDED, since ADR 0106 made it a PBKDF2 precisely because it is an offline oracle, and an export gets pasted around; `error_message` is REDACTED while type and count stay public. Adversarial round found three, all reproduced: `Digest.acceptance_rate` returns None at zero proposals and `Known(value=None, state=HEALTHY)` rendered GREEN — the module's central claim defeated in four lines by a shipped API, now refused with `Known.optional()` as the correct spelling; `PanelState` is a StrEnum so `state="unknown"` slipped past the identity guard; and any object with `.state=HEALTHY` was accepted as a reading. Empty collections stay allowed — empty is not absent | High on the contract, untested against real loop state until Milestone 5 |
| [0108](0108-the-graph-that-remembers.md) | Milestone 1 of the mind-graph program. `aef/dash/memory.py` accumulates: where nodes sat, how often each path has ever been taken, when each was last taken. All of it derived from `Provenance`, which the executor already writes — the load-bearing observation is that **consecutive provenance entries within one run ARE the edge traversals**, so no new collection was needed. Per-node error rate is NOT recoverable (Provenance has no error field) and reads UNKNOWN rather than being inferred. The encoding keeps two quantities apart on purpose: `traversals` is cumulative and never decays (thickness), `liveness()` is recency and returns **None** when a path never fired (brightness). None rather than 0.0 is the whole design — a 0.0 renders identically to 'abandoned', and abandoned is a measurement while never-fired is an absence. Part 2 resolves a HARD-STOP: `FORBIDDEN_HTML_CONSTRUCTS` banned `<button` as a proxy for 'a control' and caught a view-switching tab. The property protected is a WRITE PATH, not interactivity. Replaced not trimmed (ADR 0093), and the replacement is stricter — it adds sessionStorage, indexedDB, external src/href/@import/url(http) and `Math.random`, the last of which caught a live violation where edge phases were randomly seeded so identical data drew a different picture every load. Adversarial round found six, all reproduced: a `runs=0` node could claim healthy (the milestone's own rule, unenforced); a non-finite position emitted bare `Infinity` so `JSON.parse` threw and the whole page rendered blank; negative traversals made `lineWidth` NaN and the edge silently vanished; duplicate node ids discarded a node; out-of-range liveness clamped silently; and `traversals=0` with a liveness broke the core invariant. Plus a CSS variable renamed in the palette but not in the JS, where `strokeStyle = ""` silently keeps the previous colour | High on the accumulation model, untested against real loop state |
| [0109](0109-the-trust-boundary-is-the-launcher-not-the-library.md) | Corrects ADR 0047's central claim: `BaseRefHarness` was never the integrated gate loader — it had ZERO production call sites, found by an independent bug hunt and re-derived in review. The isolation property is established by the LAUNCHER (loop-gate.yml checks out main, installs it, then fetches the candidate as data), not by the library, and a library cannot attest its own import provenance. Adopters driving the loop by any other path do not inherit it. Recorded as its own ADR rather than only an in-place edit, because the index still carried the false claim | High |
| [0110](0110-consolidation-is-a-write-time-service.md) | Increment I0 of the WikiSkill program (arXiv:2608.27454) — design only, no code. WikiSkill's three layers map onto this repo as: raw experience **already exists** (`make_reflect_node` writes failure/success records, ADR 0046), consolidated knowledge is **the gap**, and the skill-updater layer is `aef/evolution/` which is **hard-disabled and out of scope**. Building only the middle layer is coherent because the paper's own ablation shows the persistent wiki carried the result, not the skill updater. Four decisions. (1) The wiki is a service under `aef/services/knowledge/` with memory's base/in-memory/adapters split; entries are keyed by a derived **signature**, not a whole-record hash, which consolidates nothing because two runs of the same failure differ in run_id and excerpt text — the default signature uses `failing_nodes`, the field ADR 0096 established a structural proposer cannot begin without. (2) Consolidation is rule-based first (the pattern ADR 0046 shipped under) and happens at **WRITE time, which is load-bearing rather than a performance preference**: `ReplayEngine` re-executes deterministic nodes and asserts their output matches, so a consolidator running during retrieval could never be LLM-backed at all; because an entry is stored data by the time anything retrieves it, a non-deterministic consolidator stays replay-compatible. Threshold is ≥2 distinct records — a signal seen once is an episode, not knowledge, and that is I2's planted-fault check. (3) Its own node, not a hook on reflect: reflection is within-run, consolidation is across-run, and folding them gives one node two write paths under one idempotency key. (4) Entries and records compete under ONE budget scored by the SAME function, with a confidence multiplier **written down as a thumb on the scale** — 'entries first' would hard-code the obvious failure mode of a stale entry outranking a fresh relevant record. Explicitly does NOT unblock evolution: the trust case's three findings are untouched by a knowledge layer, and claiming otherwise is the error `memory_retriever.py` names — a true statement about one property offered as an answer about a different one. No stub for the LLM consolidator (ADR 0101's rule). I4's A/B can kill the whole layer, and that outcome is a result rather than a failure | High on the design; unmeasured until I4 |
| [0111](0111-a-model-release-is-a-scheduled-audit.md) | The loop's runtime is a coding agent reading prompt surfaces, not the dormant Python provider, and nothing re-checked either when a model shipped. Ships `/new-model-check`: a procedure with **no per-model facts** (a table here rots per release — the defect being fixed) that loads them each run from the bundled `claude-api` skill, proves its inventory grep against a planted fault, gives every `[BLOCKS]` fix a failing-first test plus a mutation check, applies `[TUNE]` edits remove-first/add-second with before/after quoted per commit, and never removes an instruction to verify. Shipped by `adopt`, pinned byte-identical to this repo's copy by a test. First run (Fable 5.1) found: the provider sent `temperature` on every call and so could talk to **no current model**; `role="tool"` forwarded to an API that has no such role; refusals returned as `content == ""`; `model: claude-sonnet` (not an ID) in every example config and both scaffold templates; and `.claude/` wholly gitignored, so `reproduce-first` and `seam-hunter` — named by `CLAUDE.md` as the verification method — existed on one machine. No live call was made (no credential): every API finding is **suspected**, reproduced ones are listed separately. Three of five test-side steps had the hole in the test, not the code | High on the mechanism and provider fixes; moderate on the prompt blocks, applied on the vendor's measurement not a local one |
| [0112](0112-the-harness-is-the-model.md) | Corrects a premise every model path in this runtime rested on: the repos this scaffold is built for hold **no API key** — their agents are Claude Code / Codex sessions, and their credential is the harness login. Until now the only provider was the SDK adapter, so in exactly those repos no node could reach a model, and `CLAUDE.md` said so ("nothing to attach to"). `aef/providers/harness_provider.py` makes a node's model call one headless CLI run: `claude -p --tools "" --max-turns 1 --output-format json` (**reproduced** from inside a running session: `result: "OK"`, real usage, `modelUsage` naming the answering model; `--bare` deliberately not passed because it skips the keychain and answers "Not logged in" with exit 0 and `is_error: true`) and `codex exec --ephemeral -s read-only --json -o <file>` (**not reproduced** — the Codex on the authoring box cannot parse its server's model catalog; the adapter is built from `--help` and its parsing is a hypothesis). Single-turn and tool-less by construction — a node wants a completion, not an agent editing the tree — and multi-message requests are rendered as one transcript, stated rather than hidden behind a fake chat. No vendor SDK; the runner is injected so tests spawn nothing. `impl: claude_code` is the template default; `model_provider.model` validated for months while nothing read it and is now the provider's default | High on the Claude Code path; the Codex path is a hypothesis until a run confirms it |
| [0113](0113-a-metric-that-can-fail-without-an-error.md) | I1 of the improve loop. The corpus HAD a scalar, and it could only go down by raising: `RuleBasedEvaluator` set `task_completion` to 1.0 for any errorless finished plan, so a graph that ran cleanly and answered wrong scored the same as one that answered right — nothing the loop learned could move the metric except "stop raising". Reproduced first, kept as the control. Scenarios now carry owner-declared `checks` — a dotted path into final state, four operators, a value; **data, never code**, because a check that could execute would hand the candidate the judge — and a `budget_ms` judged on the runner's stopwatch (the pinned clock replays recorded latency verbatim). With checks, `task_completion` is the fraction that hold, still zeroed by an error; without them, unchanged, since a scenario that claims nothing about its answer must not have a claim invented for it. One `score_scenario()` in both the in-process runner and the isolated gate path (ADR 0091's drift), with a test that pushes a wrong-answer candidate through isolation. `aef loop score` reads the metric with n, stdev, CI95, and under `--repeat` the spread across identical runs — the noise floor an increment must clear. Demo corpus: 0.5000/0.4000, spread 0; planted content fault → 0.1667/0.1000, only checked scenarios moved; three scorer mutations each failed a test. Rubric dim 1: 8→13, not the +7 allowed — keep/revert (I2) is what makes it a loop, and five checks on eleven demo scenarios is what the number says | High on the mechanism; the corpus is small and demo-shaped |
| [0114](0114-keep-on-a-branch-never-on-main.md) | I2 of the improve loop. autoresearch's result came from STACKING kept changes; this loop had every piece but that — `cycle` proposed from `base_ref` every time and a candidate every gate passed got `ESCALATE` (correctly: Tier-1 is off) and then nothing, so the next cycle proposed the same first improvement again. "Nothing compounds into main" and "nothing compounds" are different claims and the loop lived under the second. `run_loop` keeps on a LOCAL `loop/kept` branch (guarded `update-ref`, its own `KEPT` ledger event — not `MERGED`, which the monitor would roll back), proposes each turn FROM the kept state, and asserts `main` never moved. Bounded five ways: turns, wall-clock, halt, no candidate, and a candidate tree already rejected this run — the proposer is deterministic from its evidence, so after a rejection it would re-propose the identical diff forever at N+2 corpus passes each. Through the real cycle and gates: turn 1 kept the structural retry, turn 2 proposed a numeric tweak from the kept state and G3 rejected it, turn 3 re-proposed the same tree and the driver stopped. Four driver mutations each failed tests. Dim 1: 13→17 — the loop is complete; what it works on is thin | High on the driver |
| [0115](0115-the-model-writes-prose-code-keeps-score.md) | I3 of the improve loop — the LLM Critic/Judge ADR 0046 deferred, buildable once ADR 0112 made a model reachable without a key. Same rule as the summariser: the model writes prose, code computes every counted field — `grounded_in` is `failure_signals(state)`, the score is the rule-based weighted mean over the model's per-term numbers with omission counted 0.0 (a model must not lift a score by skipping the hard term), clamped, invented terms dropped. Bias controls are structural: evidence excerpts capped so bulk cannot read as quality; the judge asks twice with evidence in opposite orders, averages, and reports `position_delta` so order-sensitivity is visible rather than averaged away. Any failure falls back to rule-based and says so. Reachable from `aef.yaml` (`reflection.impl: llm`), refused at construction without a provider. Live on 11 corpus states: both judges 11/11 with ground truth, delta 0.0 everywhere, ~10 s per judgment — so **off by default**, `knowledge_boost`'s rule. The critic's causal prose is the visible gain and nothing reads it yet. Four mutations each failed tests. Dim 3: 3→7 | High on the mechanism; parity measured, not superiority |
| [0116](0116-demote-never-delete.md) | I4 of the improve loop — ACE's curation for the knowledge layer. ADR 0110 fixed near-duplicates crowding out other lessons; this fixes the other collapse, stale lessons crowding out live ones, which had no signal at all: an entry for a failure fixed fifty runs ago ranked exactly as one from this morning and a tight budget chose by id. ACE's own signal (retrieved → outcome) has no producer here — no node writes `retrieved_context` — so the signal is RESOLUTION: `KnowledgeEntry.runs_since_last_seen`, distinct runs of this agent recorded after the entry's last occurrence, recomputed by every consolidation from the records and never incremented. A field, not a `content` key, because rendering `content` spends budget — putting it in content reproduced I4's finding (6→5 at budget 400) before it moved. The retriever demotes by `hl/(hl+runs_since)` and deletes nothing. Metric fixed first (live-lesson coverage), rig of three resolved and three live lessons under a ticking clock: budget 400 live coverage 2→3 of 3 at every positive half-life, 6/6 at budget 2000, control unchanged; default 5 is the middle of the swept range. Two predictions wrong and recorded. Three mutations each failed tests. Dim 2: 8→12 | High on the mechanism; the gain is one lesson at one budget |
| [0117](0117-skills-are-proposed-never-installed.md) | I7 of the improve loop — WikiSkill's third layer with the runtime updater removed. ADR 0110 ruled the whole layer out because its updater is constraint #7; that was right about self-modification and too broad about the layer, since a well-evidenced `KnowledgeEntry` already holds what a skill would say. `aef loop skills` renders one `SKILL.md` draft per entry with ≥3 occurrences under a proposals dir the caller names; refuses `.claude/`, `.cursor/`, `.github/`, `agents/` by name before writing anything — a draft landing where a harness loads skills is self-modification with extra steps; never overwrites a draft a person edited; every claim (occurrences, confidence, staleness, run ids, provenance) computed from the entry; imports nothing from evolution, providers or reasoning, by AST test. The knowledge store is rebuilt from the durable memory file each run, since the consolidator is a stateless recompute. Three mutations each failed tests. Dim 2: 12→15; the value of an adopted draft is unmeasured until someone adopts one | High on the refusals |
| [0118](0118-the-retriever-gets-a-caller.md) | I9 of the improve loop. `Retriever` was a declared injection point with no production caller — the ADR 0092 shape, found again — so "the first thing that enforces `context_budget_tokens`" enforced it for nobody, and ACE's retrieved→outcome signal had no producer. `make_retrieve_node` asks with the state's objective and the STATE's budget and writes chunks to `retrieved_context`; reflect records `retrieved_signatures` (what the run was shown, not what the store held); the consolidator tallies `helpful` (in context, failure not reproduced) and `harmful` (in context, reproduced anyway), agent-scoped, recomputed. Surfaced in chunk metadata and skill drafts, NOT a ranking input: on every rig that exists harmful and live coincide, so the knob stays off until a rig separates them (`knowledge_boost`'s rule). Adding `require_retriever` failed the service-parity test — a retrieve-node graph ran under `aef run --config` and raised in the gate, the fifth ADR 0091 drift — so `agent_services` now defaults a retriever over its own throwaway stores, superseding ADR 0101's "no default" (its control test rewritten deliberately). Closes MERGE_READY_LOOP A1: `build_retriever` takes `knowledge=` and `aef run` shares one store between retriever and consolidate node. Mutation M28 (tally not agent-scoped) was NOT detected at first — the query filter hid it — and the test was rewritten to consolidate across agents. Dim 2: 15→17 | High on the node and tally; the tally's ranking value unmeasured |
| [0119](0119-redact-the-input-then-re-execute.md) | I5 of the improve loop. The machinery to learn from real runs existed; one reason no owner would turn it on was not in the trust case: the corpus lives in git and `harvest` wrote tenant text verbatim. Redact the INPUT, then re-execute, never patch the recorded trace — a trace with secrets swapped in place is a run that never happened. The redacted state is re-run under the recorded clock and admitted only if node path, failing nodes and plan status match; a graph whose behaviour depended on the secret is rejected, not recorded. The scenario that would be written is scanned with the same patterns — a secret the graph reintroduced from a tool or environment is caught there; this is the planted-fault check on the redactor, run every harvest. Patterns are data, conservative, and only extended. On by default. Planted email + token: promoted, absent from disk, re-executed trace stored; token-dependent graph: rejected; token-emitting graph: rejected by the scan; policy off: email reaches disk (the control). Four mutations each failed tests. Dim 7: 2→5 — the ingestion path is safe to point at a tenant and has not been; the rest is the owner's decision | High on the mechanism; the pattern list is a floor |
| [0120](0120-the-prompt-surface-is-under-test.md) | I8 of the improve loop. `/new-model-check` edits the prompt surface once per release and nothing stopped the next commit undoing it. `tests/test_prompt_surface.py` pins the state the last check left — the guide's unattended-run blocks present (joined blockquotes), removed text absent (anti-narration, anti-formatting, escalation; outside code spans and lines that name the pattern), verification instructions kept — with every detector proved against a planted fault first. That last rule earned its place immediately: the anti-formatting regex was case-sensitive and "Never use bullets" passed it; and the verification check found the `CLAUDE.md` that `adopt` ships had no reproduce-first line of its own. A lint, not an eval, and says so. Dim 5: 9→10 | High on what it pins |
| [0121](0121-an-archive-not-a-ladder.md) | I6 of the improve loop — DGM's archive for `run_loop`, which until now proposed each turn from the latest kept state and could never build on a stepping stone. G3's candidate and incumbent means now reach the driver (`GateRun`, `CycleRun`, the KEPT ledger event); an archive of kept members carries score, parent and children; `sample_parents` picks the next parent by a sigmoid of score over `1 + children` with a seeded RNG, and the kept branch points at the best member; a candidate whose tree matches a kept one is a duplicate, neither kept nor reverted, and the loop continues. Measured through the real cycle: greedy and sampled both keep one candidate and one tree — sampling re-draws the root, the deterministic proposer re-emits the same change, duplicate skipped — so the knob is **off by default** and the number says why. Mutation "novelty term dropped" passed every test until a direct weight test was added: the fourth test-side hole in six increments. Dim 6: 3→6 | High on the mechanism; measured gain zero |
| [0122](0122-a-proposer-with-a-repertoire.md) | I10 of the above-90 loop. ADR 0121 measured zero diversity gain because the rule-based proposer reaches two trees in total (reproduced: one candidate per parent, identical across calls). `LLMProposer(provider, model)` has the same `Proposal` contract: the model writes prose and the whole file in one fenced block; code computes the citations (`MemoryEvidence`, through the rule-based `_check_citations` — validation/holdout refused before any call, as is a path outside Zone A) and validates the reply against the gates' own rules — parses, same path, G0's line budget, G0's `scan_source` with the IMPORTED allowlist, G4's owner-only fields through the catalogue's check, non-empty diff — falling back to `RuleBasedProposer` with the reason in the rationale on any failure. `LoopConfig.proposer`, `aef loop run --proposer llm --proposer-model`. Measured live (claude_code, 26 calls): flaky kept 1 = 1, demo kept 1 > 0 (the model raised both constants coherently — the claim the demo agent was written to test), 16/16 replies validated, 4/4 distinct candidates per run, kept diversity still 1 everywhere (prediction recorded as wrong), and the LLM arm is the only one that halted the loop (G5 drift twice). **Off by default**; nine mutations detected. Found by running it: `run_loop` gave every turn one workdir and G1 refuses a non-empty scratch dir, so no real loop had ever gated a second candidate behaviourally — fixed per turn, M48. Dim 6: 6→8; dim 1: 17→18 | High on the mechanism; medium on generality (two fixtures, two reps) |
| [0123](0123-a-task-a-model-can-fail-replayed-without-a-key.md) | I11 of the above-90 loop. ADR 0113 gave the metric a way to fall without an error and the corpus had nothing to fall on: every scenario was `agents/demo`, which fails only by raising, because a graph that asks a model could not be re-executed by a gate that holds no key and gets a different answer every run. `Scenario.model_calls` pins every completion the recording made, keyed by a hash of (messages, model, max_tokens) that is derived at load and never trusted from the file; `CassetteProvider` replays hits without touching the inner provider and, by default, FAILS a miss naming it — a changed prompt is a behavioural difference, reported rather than answered live from inside a gate — with `cassette_miss="live"` as the opt-in that scores a prompt change live and labels the result LIVE. The cassette reaches the isolated worker in a `configure` frame. `agents/summary` (retrieve → draft → reflect → consolidate) is the first content task; twenty synthetic passages recorded live (20 calls) with per-term `contains` checks and a word-count `regex`, 12/6/2, holdout spent once by name. Cassette: train 0.9792, validation 0.9167, repeat spread 0; three scenarios at 0.75 are a capitalised term against a case-sensitive check — the first content failures the metric has seen. Planted prompt regression under the default: 36 misses, 0.0000, no live call. The live re-score never completed (three attempts past a ten-minute wall; predicted six minutes) — the live noise floor is absent, stated. Judge A/B on the 18 states: rule 3/18, LLM 9/18, neither reads the answer, predicted and confirmed. Four mutations each failed a test. Worker PYTHONPATH fixed on the way. Dim 1: 17→19; dim 3: 7→8 | High on the cassette; the corpus is one shape and the live floor is unmeasured |
| [0125](0125-the-seams-the-assembled-paths-left-open.md) | Fix wave A of the above-90 loop's adversarial pass — six seams, every one reproduced by running an assembled path before anything changed, two of them pre-existing. `aef run` built a fresh knowledge store per process and the retrieve node runs before the consolidate node, so across CLI runs `retrieved_signatures` was `[]` every time and ADR 0118's "A1 closed" was true only for a caller who hand-builds `Services`; the store is now recomputed from durable memory at run start (`RuleBasedConsolidator` is stateless by design, ADR 0110 — no file-backed knowledge store, no second source of truth). Without a `context:` block `build_retriever` returns None and `agent_services` defaulted a retriever with `agent_id=None` **over the durable multi-agent store** — it returned another tenant's record; `aef run` now passes the id, and ADR 0118's claim that this default "only ever fronts a throwaway store" is corrected there by erratum. **Pre-existing since ADR 0094:** the isolated worker did `agent_services()` with no arguments, so the base ref's `PolicyConfig` never crossed the process boundary and any node consulting the engine scored 0.0 for candidate, incumbent AND every cohort member — the uniform-zero shape `services/runtime.py` opens by describing, for a sixth time; ADR 0123's `configure` frame is EXTENDED (never a second channel) to carry the policy through `policy_payload`/`policy_config_from_payload`, the scenario's agent id and the pinned clock, in-process 1.0 vs isolated 0.0 before, 1.0/1.0 after. `run_loop` moved `loop/kept` with `update-ref` while it could be HEAD, leaving `M  agents/demo/graph.py` — a staged reversal of what it just kept — and now refuses to start there by name. The gates never filtered by `graph_id`, so a demo candidate was gated on `summary_agent` scenarios: match `LoopConfig.graph_id` when any scenario does, gate everything when the corpus records one graph (`--graph-id` is the archive key, a different namespace), and raise `CorpusGraphMismatchError` when it records several and none match — named, because empty reads to G2/G3 as "no evidence". Verified and fixed as suspected: `aef loop score` ran the incumbent with `policy=None` while `aef run --config` and the gates applied the same yaml's policy (0.0 vs 1.0). Documented, not changed: the isolated path's `elapsed_ms` includes per-node IPC (in-process 0.082–0.125 ms vs isolated 0.715–0.790 ms on a 4-node graph), so a `budget_ms` calibrated with `loop score` can fail at the gate — ADR 0113 and `loop score --help` say so. Nine mutations, each detected. No rubric score moved; what moved is that dim 2's "A1 closed" evidence is now true of the path the CLI runs | High on the six fixes and their controls; medium on F3's completeness (nothing yet parity-tests the parent's Services against the worker's); low on a documented footgun being enough for `budget_ms` |
| [0126](0126-the-seams-the-wave-b-round-found.md) | Fix wave B of the above-90 loop — the seam-hunter round after I10/I11, six findings, all reproduced by running before anything changed. The ACE tally scored a **success** entry `harmful` every time the success recurred (a repeated success re-produces its own signature) and credited a failure lesson as `helpful` when the run reproduced it inside a longer chain (`failure:fetch` shown, `failure:fetch>parse` produced — a different string): failures only now, and reproduction is a node-list SUBSEQUENCE, keeping `default_signature`'s rule that `A>B` is not `B>A`. `ClaudeCodeProvider` inherited the operator's session — **211,470 input tokens per judge call** ($0.18 cached, $2.22 uncached) against 4,684 with the MCP config suppressed, and the judge answered in the operator's personal register — so `--strict-mcp-config --mcp-config {} --safe-mode` join the argv (`--bare` still absent: it skips the keychain), and `max_tokens` is documented as dropped rather than silently ignored. The prompt-surface test filtered out every line containing a backtick, "guide" or "remove", which hid three of its own four planted faults — it blanks code spans and nothing else now, and the detector test pushes the faults THROUGH the filter. `opaque_secret` matched any 40+ char run and redacted `migrate-the-customer-billing-pipeline-to-v2-with-zero-downtime`, admitting the scenario with a placeholder objective; it needs a letter, a numeral and no hyphen now (hyphenated key shapes are prefix-anchored in `api_key`/`bearer`). `harvest` pinned the clock and not the model, so any model-calling run was rejected as non-deterministic: `RecordedRun.model_calls` round-trips and the re-execution runs on `CassetteProvider(on_miss="fail")` — and the output scan was reading the cassette's own SHA-256 request key as a secret, rejecting every such run (all twenty ADR 0123 scenarios match it). The judges' evidence omitted `working_memory`, which on a content task is where the answer is — the measured cause of ADR 0123's 3/18 and 9/18 agreement — so string entries are rendered first at a 600-char cap, 4 at most, total item cap unchanged. Sixteen mutations each failed a test; no rubric dimension moves. Errata appended to 0115 (per-call cost never measured; `max_tokens` dropped), 0116 (tallies recomputed over a window the provenance is not bounded by: occ 6 harmful 4 → occ 6 harmful 2 at `candidates_per_kind=2`) and 0118 (the tally as shipped) | High on five findings; medium on the provider — the flags are right by `claude --help` and the argv has not been through a live call (quota) |
| [0127](0127-the-scoreboard-is-under-test.md) | I0 of the to-90 loop. The rubric's first rule is that a score moves only on a cited artifact; its headline TOTAL never was one — carried forward by hand as `previous + delta` while nothing recomputed it from the rows. The baseline summed to 51 against a stated 50 and every total since inherited the point, through nine increments, into ADRs, the log, the status block and the run report. Surfaced when the report printed a per-dimension breakdown beside the total for the first time and the two disagreed. Totals corrected (50→51, 84→85) with a note that it is addition, not re-scoring; **no row moves and no measurement is affected**. `tests/test_rubric_arithmetic.py` now recomputes the heading from the rows and encodes the table's real shape (prepend-ordered history; a dimension that never moved carries from baseline), plus every weighted dimension scored, no score above its weight, weights summing to 100, and each section self-consistent including the baseline. Downstream ADRs left as written — they are dated records, and rewriting them would erase the error rather than record it. Four mutations each failed tests. The score is 85, so 90 is five points away, not six | High |
| [0131](0131-the-codex-path-measured.md) | I15 of the to-90 loop. ADR 0112 shipped `CodexProvider` with its parsing written blind from `codex exec --help` and labelled a hypothesis, because the installed CLI (0.135.0, May) rejected its own server's model catalogue — `unknown variant 'max'` — and exited 1 before reading a prompt. So "works with any coding agent" rested on one measured backend and one guess. Upgraded to 0.153.2; the adapter answered **unmodified on the first live attempt**: argv as written, `OK` from `--output-last-message`, usage from the `turn.completed` event (in 19,975 / out 26), model and stop reason correct. Kept runnable rather than merely recorded — `tests/providers/test_codex_live.py`, skipped unless the CLI exists AND `AEF_LIVE_HARNESS` is set, because a network call in a routine `pytest -q` is a flake waiting to happen. Two mutations against the real CLI each failed. Noted, not acted on: Codex spends ~19–20k input tokens on a one-word reply (the ADR 0126 overhead class, no Codex `--safe-mode` sought yet) and emits an MCP `HTTP 405` on stderr while succeeding. The failure was version skew, not code. Dim 8: 4→5, total 85→86 | High on the adapter; moderate on it staying verified — one model, one prompt shape, a CLI moving fast |
| [0137](0137-an-adopted-repos-model-call-must-be-visible.md) | K1 of the ready loop. `aef migrate` did work on the measured adoptee — and the wrapper it generated called `run_agent(state.objective)`, which still built its own `anthropic.Anthropic()`. Reproduced four ways before anything changed: `aef doctor` exit 0; the node run against a counting provider reached the vendor twice and `Services.model_provider` **0 times**; `record_run` captured **0 RecordedCalls**, so replay under `on_miss="fail"` made a LIVE vendor call (`cassette {hits: 0, misses: 0}` — never asked, not bypassed) and scored 0.0 with the SDK removed; and no surface warned. The detector existed and had run since Phase 0 — inside `tests/`, so it could only ever scan this repo. Scanner lifted to `aef/harness/vendor_scan.py` with ONE list (the subset test found `cohere` already drifted out of the constraint #3 list on its first run — `import cohere` in `aef/kernel/` was never caught); a sixth preflight obligation **model calls visible** walks the modules reachable by import from the configured graph and refuses in `aef loop doctor`, with the same finding as an advisory in `aef doctor`. `aef migrate` now emits a **routed** node (`services.require_model_provider().complete(...)`, the adopter's function bypassed and said to be) when the function is nothing but the call — own client, one completion, literal `model=`, no loop, no `try`, no stream, no other call — and otherwise the old wrapper plus a docstring warning naming what routing would have dropped. **Falsification clause decided toward the unrouted form**: `complete()` is one-shot, non-streaming, single-backend, so a retry loop, a `try`, a stream, a guessed model or a transitively-reached outer wrapper (exactly the shape `outermost` exists to pick) is strictly more than routing offers; each is a named reason with its own test. After: provider 1 call, vendor 0, 1 RecordedCall, and the gate replays at the same score **with `anthropic` deleted from `sys.modules` and no credential**. Eight mutations, eight caught. Two defects found on the way and fixed: `migrate._skip` tested the ABSOLUTE path, so any repo living under a dot-directory (`~/.local/src/app`, a worktree under `.claude/`) reported `scanned 0 Python file(s)`, exit 0 — the same shape as the defect K1 is about; and the generated module had never passed the repo's own ruff (two `E501`, an `F401`), now asserted for all three forms. **No rubric score moves** — the ready loop's done is command-backed statements, not points | High on the reproductions and the routed path end to end; medium on the routable predicate generalising (one toy adoptee, seven hand-written counter-shapes; a retry decorator is invisible to a body-only analysis) and on the reachability walk (no `sys.path` edits, namespace packages or dynamic imports — it can under-report, which is the safe direction) |
| [0138](0138-a-corpus-on-day-one.md) | K2 of the ready loop. `aef adopt` writes `corpus/README.md` and nothing else, so the loop an adopter has just installed can do nothing until they hand-record scenarios one `aef loop record` at a time — and the failing ones, the only ones carrying information, need a hand-written `--working-memory` JSON blob each. `aef loop bootstrap --inputs inputs.json` runs the configured graph once per input and records each as a TRAIN scenario. Four inherited rules: **train only, with no flag to override** (harvest's — if the system can fill the set that gates it, the gate measures the system's own choices; `bootstrap()` has no `split` parameter, and a `split` key in the file is refused); **never labels `expected`** (ADR 0060 — it prints the failed ids and GENERATES, never runs, the `aef loop record --expected must_fail` line with that input's objective and working memory filled in; an `expected` key is refused, not ignored, because a silently dropped claim leaves an owner believing in a tripwire that is not there); **refuses the whole invocation** if any id exists, before running anything, sharing recorder's rule and wording via `refuse_existing_ids` rather than restating it; and **reports the failure count**, saying so when it is zero. `checks` and `budget_ms` ARE per-input and `expected` is not — the first two specify the task before the run, the third judges what the run turned out to do. The seam: fresh `Services` per input, because the gates re-execute each scenario alone and a scenario that only reproduces after its predecessors ran is one nothing downstream can trust. Measured on the adopted repo: corpus obligation `0 scenario(s), 0 tripwire(s)` → `4, 0` after bootstrap → `[OK] 5 scenario(s), 1 tripwire(s)` after pasting one generated line, with no hand-written scenario; `loop score` reads train mean 0.5000. Eight mutations, eight failures — the eighth from a seam hunt of this diff that found `cmd_bootstrap` writing to `corpus/` without the kill-switch check `cmd_harvest` makes for the same directory (ADR 0069), now added with an optional `--state` because day one has no loop state dir yet. **A wrong prediction recorded**: the first version printed "0 of 0 recorded run(s) failed — a corpus where everything passes…" for a run where nothing ran, the exact green-light-for-nothing shape this increment exists to remove | High on the four rules (each mutation-checked, each already paid for elsewhere); moderate on the `inputs.json` shape, which no adopter has yet written; low on the failure count on graphs that fail by raising — those land in `errored`, and the adoptee's own migrated graph does exactly that until K1 lands |
| [0139](0139-an-adopted-repo-gates-a-candidate.md) | K3 of the ready loop. `test_adoption_sequence.py` drove a fresh adopted repo to five green obligations and a blessed baseline and **stopped**: its one `aef loop cycle` asserted only `"ledger verified"` — printed before the cycle does anything — and passed no `--entrypoint`, so G2/G3 would have refused even had a candidate existed. Reproduced: the full documented sequence on an adopted+migrated raw-SDK repo ends `no admissible failure memory: no candidate this cycle`, **exit 0 having done nothing**. The minimum was then measured by removal, one thing at a time, each run and quoted: no module-level numeric constant -> `the proposer produced nothing from the available evidence`; no route to reflect -> `no admissible failure memory`; no failing `aef run --memory` -> the same (bootstrap cannot supply it — ADR 0138's per-input `InMemoryMemoryStore` is deliberate); the graph at the repo root instead of under `agents/` -> `G0 rejected it: candidate touches paths outside Zone A`. And a model-calling graph cannot get a corpus at all without a credential: the cassette the gates replay from does not exist until something makes the call once, so bootstrap on K1's routed node exits 1 with `no live provider to fall through to`. `test_an_adopted_repo_gates_a_candidate_end_to_end` (slow, 35 scenario executions, no credential, zero model calls) now runs adopt -> migrate -> the refusal -> the minimum -> bootstrap -> **the tripwire line bootstrap printed, shlex-split and run verbatim** -> a failing `aef run --memory` -> bless -> doctor -> cycle, and asserts against `ledger.jsonl` rather than the CLI's summary: `1 candidate + 1 incumbent + 5 random control(s)`, G2 `5 scenario(s) re-executed`, G3 a cohort comparison and **not** its `no null-hypothesis control cohort` refusal. The verdict is a REJECTION and that is the assertion — a test requiring an acceptance could be satisfied by weakening G3. The measured minimum now LEADS the generated `LOOP.md` (each item's failure mode is exit 0, which reads as success; ordering is mutation-pinned). Seven mutations, seven caught. **Wrong prediction recorded**: the expected blocker was failure memory alone; Zone A placement mattered equally and was on nobody's list — `aef migrate` writes to the repo root, the one place the loop is structurally forbidden to work, and says nothing. Two defects found and REPORTED not fixed (outside K3's file scope): `aef loop bless --agent-path aef_migrated.py` printed `blessed aef_migrated.py as baseline v1` while archiving a Zone A tree whose only file was `agents/README.md`; and `aef adopt` writes no `.gitignore`, so `git add -A` commits `agents/**/__pycache__/*.pyc` into Zone A and G5 charges **0.4675 of a 0.500 budget** for a one-line candidate against **0.0238** without it — ADR 0074's defect arriving from the adopter's side. **No rubric score moves** | High that an adopted repo can gate a candidate (fresh `git init` to six executing gates, asserted from the ledger) and high on the four measured minimum items; moderate that the minimum is complete — it is the minimum for one agent shape, and a graph that fails by raising, or with too few reachable control mutations, was not run; low on anything it says about a repo nobody wrote to be scanned, which is K5 |
| [0140](0140-the-routed-form-drops-the-request.md) | Fix wave C of the ready loop — four defects a seam hunt reproduced in the **routed** node form ADR 0137 shipped hours earlier, every one of them the generator routing a call it should have refused. ADR 0137's falsification clause enumerated retries, loops, `try`, streams, guessed models and transitive wrappers — all **control flow** — and never asked whether the **request** survives translation: `CompletionRequest` has five fields, and a call site passing `system="You are a claims adjuster. NEVER approve a payout above $5,000."` with `tools=`, `stop_sequences=` and `temperature=0.0` was judged ROUTED, with the generated node carrying `messages/model/max_tokens` and a docstring saying "there is nothing here for routing to lose". The routable keyword set is now read off `dataclasses.fields(CompletionRequest)` — a second hardcoded list here is ADR 0091's drift and is what *produced* this defect — and any keyword the request type cannot carry, or any expressible keyword whose value is not a literal reproduced verbatim (`max_tokens=MAX` used to route as no max_tokens, substituting the default 16000), refuses the routed form and names itself; `system=` gets its own reason, corrected mid-fix to say what is actually missing (no `system` **field**, and no basis for pairing a literal system prompt with the objective this node substitutes) after checking found that `ProviderMessage` does carry a `system` role both adapters fold in. Three more refusals, all in the same direction: **any decorator at all** (a bare `@retry` is an `ast.Name`, invisible even to the "body also calls `retry()`" rule, which caught the called form only by accident — ADR 0137's own Confidence section predicted this and shipped it anyway), **`*args`/`**kwargs` at the SDK call** (`arg=None`, filtered out before any keyword was read, so a caller passing `stream=True` at runtime defeated the stream check), and **a client constructor with ANY argument** (`anthropic.Anthropic(base_url="https://llm-gateway.corp/v1", timeout=120.0, max_retries=8)` routed a call to a different endpoint, credential and account). `--force` — the command `aef loop doctor`'s own fix string prints — discarded hand edits with no backup, no diff, no warning, exit 0, three lines below a comment calling an edited generated file "the expensive thing to lose"; it now renders first, compares, and writes `.bak`/`.bak.N` with the reason on stdout when they differ. And `render()` emitted `Node(...)` with no `side_effects`, so every generated node defaulted to PURE — a live routed model call declared to have no effect on the world, past a retry guard whose idempotency requirement is skipped entirely when the declaration is *absent*; both forms now declare `SideEffect.EXTERNAL_CALL` with a generated `idempotency_key_fn`, the same shape `agents/summary`'s `draft` node uses, chosen over a `TODO` that fails loudly because `build_graph()` must still run — it is ADR 0137's whole evidence chain and K3's premise — with the generated factory's docstring stating that no consumer dedupes on the key, so a retry is a second billed call. Ten mutations, ten caught. A new defect found by the test written to prevent one: with a realistic module path the generated docstring and `working_memory` lines were already 116/125/138 characters in both forms — ADR 0137's ruff-the-output test only ever ran on short node ids. Erratum appended to 0137. **No rubric score moves** | High on all four reproductions and fixes, each run before and after and each mutation-checked; medium on `temperature`, which is a real field and so is carried, while the Anthropic adapter deliberately does not forward it — a request-type-to-adapter gap this predicate does not answer, named rather than closed; medium on completeness, since "what else does a real call site do" is still one seam hunt over hand-written shapes |
| [0141](0141-the-obligation-and-the-guards.md) | Fix wave D of the ready loop — the seam hunt of the K1/K2 merge. Eight findings, every one reproduced by RUNNING a command first, two of which make sentences in ADRs 0137 and 0138 false as written (both get an erratum; neither is rewritten). **The sixth preflight obligation asked the wrong question**: it called `scan_file` with its constraint-#3 default, so `import psycopg2` in a reachable module blocked an adopter forever with a fix about routing a Postgres connection through `require_model_provider().complete(...)` — 14 of the 19 names are not model SDKs, and ADR 0137's "it over-reports nothing" was false for all 14. `roots` is a parameter now, with a behavioural test over all 14 and an AST test pinning which caller passes which list. **Its fix string looped**: for the unroutable population migrate's own falsification clause creates, the printed fix was `aef migrate --dir . --force`, which regenerates the identical wrapper — run it and the obligation is byte-identically red. The message now reads migrate's own `UNROUTED wrapper for ... / Not routed because ...` docstring back and either sends you to migrate or says it will not help, quoting the refusal and naming BOTH edits, including the one stated nowhere: delete the import, or the module stays reachable and the obligation stays red after the body is routed. **The kill switch was opt-in by the caller** — `if getattr(args, "state", None):` on a flag ADR 0138 made optional and whose own Evidence block omits, so with the switch engaged `--state` gave exit 2 and no `--state` gave exit 0 and grew a halted loop's corpus; one of `--state`/`--no-loop-state` is now required, discovery being impossible because LOOP.md puts the state dir outside the repo. **Bootstrap ate harvest's daily budget**: `already_today` counted every scenario recorded in 24h and bootstrap stamps `recorded_at = now`, so the K5 pilot sequence (adopt → bootstrap → run for real → harvest) reported `promoted 0, 3 held back by the daily rate limit`, exit 0 — `Scenario.source` now says who wrote each one, only `harvest` is charged, and the held-back line carries its arithmetic. **Nothing consumed `Preflight.ready`** — one reader, `cmd_doctor` — so `loop doctor` exit 1 was followed by `loop cycle` proposing and gating the same repo, against ADR 0137 §2's "the gates refuse". Decided ADVISORY and said so everywhere rather than made blocking: obligation 4 is knowable only at the CLI boundary so a refusal would still leave the importable `harness.loop.cycle()` unguarded and the claim false; three of six enforce themselves later; and it would break K3's own first-day sequence, where observations cannot exist yet. `cycle`/`gate` print the unmet ones. **`aef doctor`'s advisory fired only when `aef_migrated.py` existed** — the artefact of a different command — so the documented path (wire `aef_adapter.py`, nodes under `agents/**`) never triggered it; keyed on the graph now, with `--agent-path`. **Pre-existing: `corpus.check_never_shrinks` had no production caller and `save_manifest` had none at all**, so deleting the two scenarios an agent fails raised `loop score` 0.6667 → 1.0000, exit 0, while `recorder.refuse_existing_ids` cited that guard as its justification; `save_scenario` now writes the ledger (a union, never a regeneration) and `_preflight` plus `cmd_score` read it, from the base ref where the corpus is tracked. Suspected item confirmed narrower than reported: one malformed scenario gives a named refusal that named no file, not the traceback expected — `load_scenario` prefixes the path and `cmd_bootstrap` catches `CorpusError`. Thirteen mutations, thirteen caught. No rubric dimension moves | High on all eight reproductions and on six of the fixes; medium on R7 staying advisory being the right long-term answer, on R12 (the working-tree fallback does not stop a candidate deleting a scenario and its manifest entry together, and no CI job reads either) and on R8's discovery list missing a graph kept somewhere else |
| [0142](0142-zone-a-hygiene-the-adopter-pays-for-what-adopt-did-not-write.md) | Fix wave E of the ready loop — the two defects K3 (ADR 0139) found from the adopter's side and reported rather than fixed. **E1:** `aef adopt` wrote no `.gitignore`, so an ordinary `git add -A` on day one commits `agents/**/__pycache__/*.pyc` into **Zone A**; that bytecode is created after `aef loop bless` archives the baseline and committed before the cycle, so it is on the candidate side only and `structural_drift` charges every line of it. Re-derived rather than copied, twice each through a real `aef loop cycle` writing its own ledger: **drift 0.468/0.500 without a `.gitignore` against 0.024 with** — itemised, 36 differing lines of which **35 are bytecode and 1 is the candidate**, 93.5% of the budget for a ONE-LINE change, headroom 0.0325. `_drift_exhausted_twice` halts the loop on two consecutive drift rejections, so day one put an adopter two candidates from a halt caused by nothing their agent did — ADR 0074's defect from the other side, and unreachable by that fix, which made both sides read from git and cannot help when the bytecode is IN git. `run_adopt` now writes a `.gitignore` under the scaffold's never-overwrite rule; an existing one is **skipped and reported**, never appended to, because silently adding a generated line to a tracked config the adopter owns is that rule broken by another route — the report lands in the checklist `adopt` prints and writes to disk, naming both patterns and the measured cost. Coverage is an exact match in two families (`__pycache__/…`, `*.pyc`-shaped) because either alone suffices on CPython 3 and nagging for an equivalent pattern is how generated advice stops being read; comments are not rules (a naive `"__pycache__" in text` reads `# __pycache__/` as coverage — its own mutation). `corpus/` and `.github/workflows/` are deliberately NOT ignored: Zone B is evidence read from git, and an ignored corpus is an empty one. After: **0.468 → 0.024**, both arms identical. **E2:** `aef migrate` hardcodes its output to the repo ROOT, which is Zone C — reproduced through the harness's own `read_candidate`/`inspect_candidate` on a real commit (`allowed: False`, `Zone C (core) — not under the agent root 'agents'`) — and neither migrate's report nor ANY generated file said so: the strings `Zone A` and `agents/` appeared nowhere in `CLAUDE.md`, `AGENTS.md` or `AEF_MIGRATION_CHECKLIST.md`, which told the adopter to convert call sites into nodes. `migrate.py` is another worker's file, so `adopt` now STATES the constraint where the adopter meets it — a `## Where converted nodes have to live` section in the generated CLAUDE.md/AGENTS.md and a framework-independent checklist item, both interpolated from `DEFAULT_AGENT_ROOT` and cross-checked against the directory `adopt` really creates. The change `migrate` still needs (an `--out` defaulting under the agent root, plus a report line naming the zone it wrote to) is written out in Consequences for routing. Eight new tests, one of them end-to-end on unmodified `adopt` output through `bless`'s archive and the gate's own `structural_drift`; six mutations each detected, and **two attempted mutations that survived are recorded** because both left the information intact — a partial edit is not a deletion. `test_adopt.py`'s pinned written-file set and 15→16 idempotency counts updated deliberately, not relaxed. **No rubric score moves** | High on E1's mechanism and fix (ledger numbers twice each, arithmetic re-derived line by line, the regression test fails with the 0.4675-shaped tree when the `.gitignore` is removed); high that E2's constraint is now stated and only moderate that stating it is enough — documentation is the weakest control here and the strong one is another worker's file; low on `gitignore_gaps` against real-world `.gitignore` files, tested against four shapes when git's ignore semantics are richer than all of them |
| [0143](0143-migrate-writes-into-zone-a-and-wires-the-loop.md) | Increments L1 and L2 of the ingest loop — the two defects ADR 0139 measured in what `aef migrate` itself writes, both left unfixed by fix wave E because `migrate.py` had another owner that night. **REPRODUCED by running the harness's own code:** a candidate whose only changed path was the generated file came back from `inspect_candidate` as `allowed: False ... aef_migrated.py: Zone C (core) — not under the agent root 'agents'`, and the report said nothing about it; and the generated `build_graph()` had one node returning `END`, so `aef run --memory` left no file at all and `aef loop cycle` printed `no admissible failure memory: no candidate this cycle`, **exit 0 having done nothing**. L1: `run_migrate` gains `--out`, defaulting to `DEFAULT_MIGRATED_OUT = f"{DEFAULT_AGENT_ROOT}/migrated/graph.py"` — derived, never spelled out, and imported by `doctor`, `adopt` and `adopt_loop` rather than re-derived; parents are created; ADR 0140's never-overwrite and `--force` `.bak` rules follow the path rather than one filename; and `report()` names the **zone** of what it wrote via `zones.inspect_path`, the classifier G0 uses. `LEGACY_MIGRATED_OUT` is kept as a named constant so `aef doctor` still discovers a repo migrated before this, with M8 as its control. **No `__init__.py` is generated and that is measured** — five layouts built and imported, all five clean under namespace packages. L2: the generated graph wires `<call site> -> reflect -> consolidate -> END`, every node's route moves from `END` to `"reflect"`, and the module docstring, `build_graph`'s docstring and the report all say the tail is what makes the repo learn and that removing it makes the loop **silently** inert. **The falsification clause answered with a measurement:** executed with a stub provider, the same node leaves the same answer in `working_memory` byte for byte and the memory store goes **0 records -> 1**; what does change is that reflect/consolidate need critic/judge/memory/knowledge, so a hand-built bare `Services` now raises `ServiceNotConfiguredError: service 'critic'` where it ran before — `agent_services()` defaults all four, so `aef run`, `bootstrap`, `cycle` and every gate are unaffected. Trade taken, stated in three places, pinned by a test. K3's end-to-end test now gates **migrate's own output**: the hand-placed `agents/mine/graph.py`, its `make_reflect_node`, its `Edge`, its routes and its two `__init__.py` files are DELETED — the test writes no edge, no reflect node and no route — while the two numeric constants and a body that can fail without a credential remain, patched into migrate's output with before/after assertions on every substitution. Its gate assertions were **strengthened**: the rejection is read from `ledger.jsonl` (`rejected` present, `accepted` absent, `G3 outcome == fail`), because a test demanding an acceptance could be satisfied by weakening G3. Eleven mutations, eleven caught. **A defect found on the way and fixed**: the generated `LOOP.md` still said `aef adopt` writes no `.gitignore`, five hours after ADR 0142 made it write one. **Three wrong predictions recorded as wrong**, including that K3's hand-written agent would disappear entirely — it does not, because items 2 and 4 of the measured minimum are semantics, so definition-of-done statement 1 is still not true. **No rubric score moves** | High on both reproductions and both fixes (run against the real classifier and the real executor, each pinned, each mutated) and on the anti-drift derivation; medium on the `Services` trade being right, since it is undetectable at import time and no repo outside this one was run; medium on multi-call-site repos, where only the entry node is reached — as before, and stated in the generated docstring; low, unchanged, on anything it says about a repo nobody wrote to be scanned (K5) |
| [0145](0145-bootstrap-leaves-the-evidence-it-produced.md) | Increment L3 of the ingest loop. ADR 0139 measured `aef loop bootstrap` as unable to supply requirement 4 — a failing run whose memory the cycle can read — and a model-calling graph as unable to bootstrap at all. Both reproduced by running the commands: bootstrap runs four inputs, reports two FAILED, writes no memory file, and `aef loop cycle --memory M` then says `no admissible failure memory: no candidate this cycle`, exit 0; on a model-calling graph it exits 1 with `cassette miss ... and no live provider to fall through to`. **`--memory <file>`** writes each run's reflections to the durable `FileMemoryStore` the cycle reads, WITHOUT giving up ADR 0138's isolation seam: `RunScopedMemory` reads a fresh per-input scratch store and mirrors every write to the sink, so no input can read what another remembered. Bootstrap authors nothing — a graph with no reflect node leaves the sink empty and is told so (ADR 0060), and "no sink asked for" prints differently from "asked and empty". **`--config`** existed and pointed at nothing; it now reads `aef.yaml` through `aef run`'s own `build_run_config`, so `policies`, `tools.allow` and `evaluator.suites` reach a recording for the first time (they never had — ADR 0091's drift, in the flag meant to prevent it), the live call count is reported, and a missing provider names the flag that fixes it. Measured: the same sequence plus one flag goes from `no admissible failure memory` to `proposed cycle-... / gated: reject — G3 ...`. 10 mutations planted, 10 caught. **No rubric dimension moves** | High on both reproductions and on the measured close; high that the isolation seam survives (the mutation that breaks it is the implementation this most likely would have shipped); **medium on `--config`: the wiring, cassette and count are proved with a FAKE provider — the live recording path is untested for want of quota**; low on anything about a repo nobody wrote to be scanned |
| [0147](0147-bless-must-archive-the-tree-that-contains-the-agent.md) | Increment L5 of the ingest loop, closing the first of the two defects ADR 0139 reported rather than fixed. Reproduced: `aef loop bless --agent-path aef_migrated.py` printed `blessed aef_migrated.py as baseline v1`, exit 0, while the archive held exactly one file — `agents/README.md`. `bless` checked `path_exists_at(ref, agent_path)` and then archived the Zone A tree: two different questions, with the message naming the first. So the `blessed baseline` obligation went green on evidence unrelated to the agent, and G5 then measured every candidate's `structural_drift` against a baseline that never contained the file the candidate changes — ADR 0074's arithmetic arriving from the other side. **Refuses when the agent path is not inside the tree that would be archived, naming both paths and both remedies** (move it under Zone A, or pass `--agent-root`); the tree is named through `agent_root`/`DEFAULT_AGENT_ROOT` rather than spelled, spellings are normalised so `./agents/x.py` is not refused, and the `no files under <root>` branch loses its `pragma: no cover` — its comment asserted exactly the assumption the defect lived inside. Nothing is archived on the refusal, asserted through `archive.versions()` rather than a printed line. L1 (ADR 0143) makes this harder to reach and does not fix it. 3 mutations, 3 caught. **No rubric dimension moves** | High that the reproduced shape is closed; medium on completeness (symlinks out of Zone A and case-insensitive path collisions were not run); not measured: whether any real repo is carrying a mis-measured drift budget from a wrong blessing today |
| [0148](0148-the-first-day-document-written-from-a-terminal.md) | Increment L6 of the ingest loop — `FIRST_DAY.md`, the sequence in the order an adopter meets it, shipped by `aef adopt` under the never-overwrite rule and added to the prompt surface. Named for *when* to read it, the one thing `CLAUDE.md`/`AGENT_INTEGRATION.md`/`LOOP.md`/`AUTONOMY.md` never answered. **Every command in it was RUN in a scratch repo and its real output pasted**, because all three of this scaffold's documented-but-false claims were written from the source rather than from a terminal. Nine already-false claims closed, each verified by running: adopt writes 16 files not "six"; `aef loop doctor` reports six obligations not five and the sixth was missing from the list; "with obligations unmet the gates refuse" (they are ADVISORY, ADR 0141); "`aef loop bootstrap` cannot" leave failure memory (`--memory` does, ADR 0145 — measured both ways, exit 0 having done nothing without it); the printed bootstrap invocation the CLI **refuses** for want of `--state`/`--no-loop-state`; `--config` named nowhere; "work down its output until every line is OK" when two obligations cannot be green on day one; `--config` reaching bootstrap; and the generated `aef_adapter.py`'s bare `Services()`, which raises `ServiceNotConfiguredError: service 'critic'` on the documented path since L2 wired reflect→consolidate — fixed to `agent_services()`, regression test executes the shim rather than grepping it. Requirement 2 re-measured on migrate's own graph: a candidate IS proposed (`add_bounded_retry`) and the **control cohort** is what cannot be built, a different message in a different place; whether `--proposer llm` needs a constant stays UNMEASURED (quota). 10 mutations, 10 caught — three MISSED first, all three tests pinning a token rather than a claim. +10 tests, 1976→1986. No rubric dimension moves; zero model calls | High on the commands and the nine claims (each run); moderate on requirement 2's re-measurement generalising past one agent shape; low, unchanged, on any repo this project did not write |
| [0149](0149-the-recorder-the-defaults-and-the-tree-that-is-not-there.md) | Fix wave F — five seams on the adoption path, each REPRODUCED by running a command before anything changed. **F1, critical:** `aef loop record --config` had its own `load_agent_config` + `build_model_provider` and passed `agent_services` NO policy, while `cmd_bootstrap` two functions below it went through `build_run_config`. Same graph, same `aef.yaml`, same objective: bootstrap recorded `{'decision': 'allow'} done`, record recorded `{'decision': 'deny'} failed`. Since `aef loop record --expected must_fail` is the ONLY documented way to mint a tripwire, its must-fail guard then ACCEPTED the label — the run failed, from the dropped config rather than from the task — and `scenario_runner` applies the owner's policy at gate time, so the same scenario passes, `tripwire_hit = True`, `regressed = True`, and **G2 rejects every candidate forever reporting reward hacking** (carried through the gates' own `run_scenario`/`Comparison`, not inferred). `cmd_record` and `cmd_score` now read config through `aef run`'s single site, and the ADR 0145 AST test is generalised over **every** `--config` command the PARSER declares — a hand-kept list is how this passed for bootstrap while record drifted — plus the behavioural test the hunt found missing, which records the same input through both recorders and compares. **F2:** `DEFAULT_AGENT_PATH` was `agents/demo/graph.py`, aef-core's own fixture directory; on a defaults-only `adopt`+`migrate` repo `loop doctor` printed a `bless` fix line that exits 1 and `loop cycle` exited **0** with `no agent source at agents/demo/graph.py`. One constant in `harness/zones.py` derived from what `aef migrate` writes (there, not in `cli/migrate.py`, because the harness does not import the CLI), `DEFAULT_MIGRATED_OUT` aliased to it, guarded by an AST scan that catches the INTERPOLATED form too. **F5:** `bless` accepted a Zone A symlink and archived 16 bytes of `../real/graph.py` as the baseline — the case ADR 0147's own Confidence named untested — now refused reusing `candidate.ESCAPE_MODES` rather than a second list. **F6:** three call sites generate three nodes of which one runs, silently (no executor warning, `node_path` from the trace, singular report); the REPORT now names the entry node, lists every unreached one, and says composing them is the semantic half migrate will not do — no edge order invented. **F7:** `build_run_config` reports the evaluator error before the provider error where `aef run` reported the provider first; kept and PINNED (validate before constructing) with a control that the second fault is still reachable. 11 mutations, 10 caught, 1 missed BY DESIGN and reported as such. Errata appended to ADR 0145 (its 'abolished' claim covered one of two callers) and ADR 0147 (its 'last literal' claim, and its untested symlink). **No rubric dimension moves** | High on all five reproductions and on F1's fix (two tests fail under different mutations, so neither carries the other); medium on the F2 guard's completeness (a default computed at runtime is invisible to it) and on F5's scope (whether an existing baseline anywhere already holds a link target is not measured and nothing detects it retroactively); low, unchanged, on anything said about a repo nobody wrote to be scanned |
| [0150](0150-a-flag-shape-is-not-a-flag-value.md) | Found by running the documented first-day sequence on `main`: ADR 0126's `--mcp-config {}` is REJECTED by the CLI (`mcpServers: Invalid input`), so **every `ClaudeCodeProvider` call exited 1 for hours** — ADR 0112's whole no-API-key path dead on main, through four merges and a full green bar. It shipped because the quota was out that day, so the tests asserted the SHAPE of argv (`== "{}"`) and argv was well formed: a shape assertion cannot tell a valid flag value from an invalid one. Fixed to `{"mcpServers":{}}` as one module constant, with the unit test now PARSING the value. `tests/providers/test_harness_live.py` runs the real binary, opt-in twice like the Codex guard, so both harness backends finally have a live test. Mutation M2 (drop the MCP flags) was NOT detected and correctly so: measured against the real CLI, `--safe-mode` alone gives input_tokens 2, identical to all three flags — so ADR 0126's 211,470 -> 4,684 credit belongs to `--safe-mode` and the MCP pair is belt-and-braces. Generalises to: a test asserting an argument's shape proves the caller's intent, not the callee's acceptance | High — defect, fix and attribution each measured against the real CLI |
| [0151](0151-the-independent-score-is-the-score.md) | BEYOND_90's J0, finally run: a fresh agent blind to IMPROVE_LOG, every ADR, every loop file and git history scored the rubric from code and tests alone — **68.5 against our 86**. Every one of its seven deductions was verified by reading or running the code it named and every one holds: the repo's own scheduled `loop cycle` passes no `--memory` and has exited 0 doing nothing every night since it was written; `retrieved_context` is written by the retrieve node and read by NO prompt (I12 as designed would have measured four identical arms — S1 stopped mid-preflight); the judge A/B, the archive measurement and the LLM-proposer measurement exist only as prose; `sample_parents` has no CLI flag; the Codex live test is a skip from code alone. The lower score stands on all seven: **68 / 100**, seven rows prepended, arithmetic test enforcing the sum. Nothing J0 missed was discoverable from code. Eighteen points, none from a control failing — all from the repo's two signature shapes, wired-but-not-consumed and asserted-not-shown, found by someone who did not build it | High — each resolution verified against the code, not the report |
| [0152](0152-a-prompt-file-agent-is-a-graph.md) | Increment M1 of the upgrade loop. Reproduced by running: `aef migrate --dir .` on a clone of a real prompt-file repo printed `found 0 call site(s)` with **eight `.claude/agents/*.md` personas** three directories away — the answer for every eligible repo in the survey, all of which have zero SDK call sites. `make_prompt_agent_node` (`aef/reasoning/prompt_agent.py`) runs one persona as a node: body as the `system` message, `state.objective` as the user turn, through `Services.model_provider`, `deterministic=False`, `EXTERNAL_CALL`, key from (agent name, objective). **The prompt runs; the agent's tools do not** — `--tools ""`, and the persona's own `tools:` frontmatter is parsed, reported and never obeyed. `aef migrate` now writes one graph per agent at `<agent-root>/migrated/<name>/graph.py`, `graph_id` = the agent name, wired `prompt_agent → reflect → consolidate → END`; skills are found, counted and deliberately NOT migrated, with the reason in the report. Zone A design (a) — `--agent-root .claude/agents`, opt-in, default unchanged — chosen over a multi-root `ZonePolicy` because it touches **no containment-boundary code at all**; both CLI facts it rests on (recursive agent discovery; a `.py` under `.claude/agents` is inert) measured from the binary via an unknown-`--agent` probe that costs no model call. G0 on a Zone A `.md`: ran it, did not crash, was **skipping silently** — "no static-safety violations" over a file it never opened — now counts what it scanned and names what it did not. 4 live calls; migrate finds 8/writes 8, `aef run` returns real persona-grounded text, `bootstrap` records 2 scenarios with cassettes. No rubric dimension moves. | High on the mechanism; one pilot repo, one harness |
| [0153](0153-appending-is-not-overwriting.md) | Increment M2 of the upgrade loop. REPRODUCED on a clone of a real 8-agent repo: `aef adopt` printed `detected framework: none` and **skipped the `AGENTS.md` that repo's agents read** — `grep -c AEF AGENTS.md` returned 0, so the contract reached no file the harness opens; a repo with its own `CLAUDE.md` got the same treatment. Adopt gains a fifth label, `prompt_files`, with counts (`prompt_files (8 agents, 5 skills, AGENTS.md, .codex)`), and a third verb: five entry files (`CLAUDE.md`, `AGENTS.md`, the Copilot and Cursor pointers, `.gitignore`) are **appended to within `aef:begin`/`aef:end` markers** instead of skipped. Appending is not overwriting and the ADR argues why: every pre-existing byte survives verbatim and outside the block, a re-run replaces only the block, deleting it restores the file. Measured: original bytes are a prefix, `git diff --stat` is 41 insertions and 0 deletions, and a second adopt changes **no byte of any of 260 files** — the first version failed that and was caught by running adopt twice, not by reading it. Precedence: a code/manifest signal keeps the label (it selects the migration notes) while the prompt counts and the `aef migrate` step are reported under every label. Also ships the `loop cycle` step the rendered adopter workflow never had, with `--memory` and `--cassette-miss fail` and the verdict in `$GITHUB_STEP_SUMMARY`. No rubric dimension moves | High on the gap and the marker mechanics; low on the generated prompt-file sequence, which parses but has not been run end to end |
| [0154](0154-grok-and-any-harness-after-it.md) | Increment M3 of the upgrade loop — `GrokProvider` plus `CommandProvider`, so a harness this repo has never run is an owner's config rather than a class someone here guesses at. Grok parsed from **five real runs**, never from `--help`: its headless door is `-p/--single <PROMPT>` where the prompt is the flag's VALUE (a `claude -p <prompt>`-shaped guess opens a TUI that hangs), and none of its field names are Claude's — reply in `text` not `result`, `stopReason` not `stop_reason`, no `is_error`, errors as `{"type":"error","message":...}`. Isolation MEASURED four ways because Grok has **no `--safe-mode`** at all: `--cwd <empty dir>` is the load-bearing lever (24,001 → 18,581 total input, matching the 5,153 tokens `grok inspect` attributes to this repo's `Claude.md`+`Agents.md`), the tool flags buy 481–645 (noise, kept for the safety property), and **~17.9k still gets through** because the operator's global `~/.claude/Claude.md` reaches every call and no flag stops it — the isolated run's own `thought` quoted a rule that lives only there. `CommandProvider` (`impl: command`) proves sufficiency twice: it reproduces `ClaudeCodeProvider`'s argv element for element, and it reproduced the Grok answer LIVE from config alone. `{model}`/`{system}` are argv **slots**, not appends, because flag order is part of a CLI's contract. No shell ever — argv is a list, placeholders substitute only as whole elements, and `` `whoami`; rm -rf / $(id) `` arrives verbatim as one element as prompt, as model name, and on stdin; the `shell=True` scan is AST-based and verified against a planted fault. A system message with no `{system}` slot is prepended, never dropped (ADR 0112's `max_tokens` rule). **Folded in from M1:** `modelUsage`'s FIRST key is not the model that answered — the CLI bills a helper model, so a `--model claude-opus-5` run recorded `"model": "claude-haiku-4-5-20251001"` in provenance and in every corpus scenario; one `answering_model()` used by both adapters (requested → alias-extended → most output tokens → first). **The finding: ADR 0150's defect recurred inside the ADR about it.** The live isolation guard, written against Grok's `usage.input_tokens`, **PASSED on a deliberately de-isolated provider** — that field is the uncached remainder and moves with cache state. Fixed by reporting total context, not by loosening the bound; re-mutated, now fails at 23,520. 12 mutations, 12 caught, one only after the control was rebuilt. 12 live calls (budget 10; the 2 over are the re-verification). +65 tests, 2002→2067 | High on Grok's interface, JSON shape, error shape and the isolation table (each measured, the isolation number re-measured under mutation) and on the no-shell properties (structural); moderate on the 20,000-token live bound, which separates the arms on THIS repo's ~5.1k of instruction files and leaves an unmeasured margin elsewhere; low and unchanged on Copilot's CLI, which is not installed and about which this ADR claims nothing |
| [0155](0155-a-caller-that-writes-state-nobody-reads.md) | S1/I12, the ACE four-arm on the task metric, **model `claude-opus-5[1m]`**. Reproduced before spending quota: arms (a) no-retrieve, (b) raw records and (c) records + knowledge produced **one identical SHA-256 over every prompt** while chunk counts climbed 0→5 — `draft_node` never read `state.retrieved_context`, so ADR 0118's caller wrote state nobody read (erratum appended there). Wired: `render_retrieved_context` in `aef/reasoning/nodes.py`, generic and state-only, appended by `draft_node`; **byte-identical prompt when nothing is retrieved**, pinned by a literal golden so every cassette still hits; two mutations detected. Then 84 live calls, `--repeats 2`: (a) 0.8541, (b) 0.8541, (c) 0.8334, (d) 0.9166, largest within-arm spread **0.0833**. **Falsification (c) ≤ (b) FIRED — dim 2 does not move, 17/20 stands**, and ADR 0110's coverage result is demoted to an unconfirmed proxy; the sharper reason is that **zero knowledge entries formed in any arm** — six distinct objectives, no `state.errors`, so no signature recurs and the two-run threshold is never met, making (c) and (b) the same arm. (d) − (c) = +0.0832 against a 0.0833 spread, so LLM reflection stays off for the third time. Two defects found and reported unfixed: the corpus word-cap regex `^(?:\s*\S+){1,35}\s*$` **does not terminate in 600 s on a 36-word summary** — the check that catches long summaries hangs on long summaries, invisible under cassettes, and the likely true cause of ADR 0123's "three attempts past a ten-minute wall"; and `ClaudeCodeProvider` reads the answering model as the first `modelUsage` key, which is the CLI's auxiliary haiku call, so every `Provenance.model` names the wrong model | High — every number from a run; the null result is the result |
| [0156](0156-the-live-noise-floor-and-a-regression-under-it.md) | Increment S2 of the upgrade loop (= I13). The live noise floor of `agents/summary` on the validation split, measured on **`claude-opus-5`** — every prior number in this file came from `claude-fable-5-1`, so the incumbent arm was re-measured rather than compared across models. **Two defects had to be closed before a number existed.** D1: run as `TO_90_LOOP.md` §I13 writes it, `--cassette-miss live` on the INCUMBENT makes **zero** live calls and reports `repeat_spread 0.000000` — the unchanged prompt reproduces the recorded request byte-for-byte, so `CassetteProvider` serves the hit and `on_miss` never applies; a floor collected that way is the replayed floor with the model never asked anything. D2, and it is what stopped I11's three attempts: every summary scenario's word cap is `^(?:\s*\S+){1,N}\s*$`, a nested quantifier over an ambiguous group, and `checks.py::_holds` calls `re.search` with no timeout — a match short-circuits in **0.05 ms**, a failure must exhaust every way of cutting the string into ≤ N runs and **does not terminate**. One word over the cap is the difference. The recorded cassettes all sit at or under the cap, so the branch is unreachable from every green test and reachable from every live gate pass; I11's ten-minute walls were attributed to a throttle because nothing reports where a score spends its time. Neither file is this worker's, so both are REPORTED: the arms run against a scratch corpus with the six validation cassettes emptied (forcing live) and the cap rewritten to the linear equivalent `^\s*\S+(?:\s+\S+){0,N-1}\s*$` — same predicate, proved by 4,000 generated strings per cap and by the whole replayed metric coming back byte-identical (train 0.9792, validation 0.9167, every per-scenario score including all three 0.75s). **Floor (Opus, 2026-09-04): mean 0.7639, spread 0.1666 over 3 repeats of 6 scenarios** (0.8333/0.7917/0.6667, stdev 0.0867, `0 hits / 6 misses / on_miss=live` every repeat). Planted regression (the must-mention instruction removed from `draft_prompt`): 0.7083/0.7500/0.7500, mean 0.7361 — **a fall of 0.0278 against a floor spread of 0.1666**, 0.32 of one stdev, every repeat overlapping. **The falsification fired: dimension 1 stays 19/20.** The replayed path detects this same regression perfectly (ADR 0123: 0.0000, 36 misses, no live call); the live path cannot see it at all, and the binding constraint is the corpus — 24 binary outcomes of which live variance already moves two to four. Per-scenario the instruction IS load-bearing (`sum-17` and `sum-18` fell in 3 of 3 repeats, `sum-16` rose in 2), so a paired sign test would flag what the mean cannot; offered, not made. Repeat 3's `sum-13: 0.00` attributed with no live call — a raising provider and an empty-string provider both give exactly `score 0.0000, cost_tokens 0`, and that repeat's split cost is 370 against ~465, one call's worth missing — so a third of the floor's spread is transient call failure, which a gate pass will also meet. Two further defects reported: `loop score --json` cannot distinguish "the provider died" from "the answer was wrong" (it emits neither `failure` nor the check failures `run_scenario` already computes), and `ClaudeCodeProvider` attributes the answer with `next(iter(modelUsage))`, which on this machine returns `claude-haiku-4-5-20251001` for a call issued with `--model claude-opus-5` — dictionary insertion order recorded as `Provenance.model`. 41 calls (1 preflight at `input_tokens 2`, 4 diagnostic, 36 measurement). `agents/summary/graph.py` restored from a shasum-verified backup, `git diff --exit-code` clean | High on the floor (three live repeats, both arms on identical inputs, one prompt line between them) and on D2 (stack trace, then isolation, then a rewrite that reproduces the replayed metric byte-for-byte); medium on the cause of repeat 3's 0.00, which the harness does not report and the run cannot be replayed to find; low on generalisation — six scenarios of one shape, one night, one model |
| [0157](0157-a-lesson-appended-to-a-prompt.md) | Increment M4 of the upgrade loop — `RuleBasedPromptProposer`, ACE's method on a `.md` persona, and L4 measured. **The reproduction is not the one the brief predicted**: the cycle never reached the proposer at all. An owner CHECK is the task metric and is evaluated after the run (ADR 0113), `make_reflect_node` writes `failure` only when `state.errors` is non-empty, and `make_prompt_agent_node` declares no `fallback_node_id` — so **a prompt-file agent that answers cannot produce failure memory**, and both bootstrapped runs (one of which FAILED the owner's check) wrote `kind="success"`. `aef loop cycle --proposer rule_based` printed `no admissible failure memory: no candidate this cycle`. The proposer takes the highest-recurrence admissible failure entry — consolidated through `RuleBasedConsolidator`, so the two-run rule (0110), `runs_since_last_seen` (0116) and the tallies (0118) are the measured ones, not a second copy — and appends ONE bullet under `## Lessons (aef)` carrying its signature and run count in an HTML comment: no model call, existing bullets copied byte-for-byte, eviction by staleness among the bullets THIS loop wrote and never an owner's, `PromptOutsideZoneAError` for anything not Zone A. **It is nobody's default and the reason is stated**: `Node` carries no kind, `_build_proposer` never sees a `Graph`, and switching on the agent path's suffix would make `--proposer` mean different things in different repos. **L4, one cycle each on the marlin clone:** `rule_based_prompt` produced a candidate — 4 lines, **G5 drift 0.007/0.500**, G0/G1/G4/G5 pass, **0 live calls**; `llm` produced **nothing** for **1 live call**, `LLMProposalRejected: the reply does not parse as Python: invalid character '—'` — ADR 0122's finding restated for prose, and the drift budget was never reached, let alone raised. **Paired all-live scoring on a cassette-stripped corpus, run twice:** the failing scenario flips **0.0000 → 1.0000**, mean 0.5 → 1.0 — the 'lesson made no difference' falsification did NOT fire — **and the caveat is louder than the result**: the lesson's text is the critic's rendering of the failure, the failure names the check, so the bullet pasted `contains 'VERDICT:'` into the persona and the agent then said it. ACE's method and teaching to the test are the same operation here and the loop cannot tell them apart. Evidence was **synthesised** (there is no producer) by the real reflect node over a state carrying the check failure as an error, and says so. **Three gate defects reported, not fixed:** G2 always raises `TrustBoundaryError` on a non-Python candidate (it re-materialises G1's workspace when no cohort exists); **G3 has no null hypothesis for a prompt candidate at all**, so one can be rejected but never accepted; and `aef migrate --agent-root .claude/agents` prints a run command that cannot be imported. 6 mutations, 6 kills; +32 tests, 2136→2168. 15 live calls (budget 30). Model: Opus. **No rubric dimension moves**, and the ADR says what would | High on the reproduction, the proposer and every gate verdict (each read from `ledger.jsonl`); high on the live flip, which reproduced twice and is a binary check outcome above S2's floor; **low that the flip means the agent improved** — the target string was in the prompt; low on generality: one persona, one repo, n=2 scenarios, and evidence no real deployment currently produces |
| [0159](0159-the-judge-ab-is-an-artifact-now.md) | Increment S3 of the upgrade loop (= I14). ADR 0126 put `working_memory` into the judges' evidence and did not re-run the A/B its own consequences section said must be re-run before anyone cites 3/18 or 9/18 again; J0 deducted dim 3 to 6 because that A/B was a docstring with no script or data. It is a script now — `docs/research/i14/run_i14.py` (`--dry-run` lists the 18 states and the calls, `--live` makes them, `--report` recomputes every table from the committed `results.jsonl`) — with the raw judgments, the preflight and both rendered outputs in the repo. **36 live calls on `claude-opus-5[1m]`**, both arms re-measured on the same 18 states and the same model (Fable's numbers are not comparable): rule-based **3/18** agreement with the owner checks, LLM **15/18**, the two judges agree with each other on **0/18**, flat across thresholds 0.25–0.75; position delta max **0.06** (was up to 0.2); 0 fallbacks, 9.3 s per judgment. With the answer in evidence the LLM judge's scores rose from the blind run's 0.23–0.50 to 0.82–0.90 — a real, measured behaviour change. **The point is still not taken.** Both arms are constant functions (rule-based fails all 18, LLM passes all 18); the corpus is 15/18 pass, so 15/18 is what answering "pass" to everything scores; AUC over the 45 pass/fail pairs is **0.322**; and the corpus's only three negatives are the case-sensitive `contains` defects ADR 0123 already named (`Volunteers`/`volunteers`, `Swimming`/`swimming`, `Landslip`/`landslip`) — corrected, the corpus is 18/18 pass and has no negatives at all. The summary corpus cannot grade a judge, which retroactively applies to I11's 9/18. Dim 3 stays **6/10**; the next increment is a corpus with true content negatives. Found, not fixed: `answering_model`'s rule 3 ("most output tokens") misattributed 1 of 36 judge calls to the CLI's helper model, because a JSON-only judge reply can be 12 tokens; and `CompletionResult` keeps only `usage.input_tokens` (2 per call), not the cache_creation / cache_read where the prompt's real cost is | High on the numbers and the non-discrimination finding (two independent statistics agree); medium on the rubric call, a pre-registered criterion met in the letter and not the spirit — data committed so it can be overruled |
| [0161](0161-the-container-is-what-you-get-without-asking.md) | Worker S5 of the upgrade loop (BEYOND_90's J3), closing the OTHER half of J0's dim-4 deduction. **Reproduced by running, and the reproduction contradicted the increment's premise**: ADR 0105 had already made containment the default *by refusal*, so there was no uncontained-by-default constructor to close. What was wrong is what the refusal left behind — on this box, with a running daemon and `aef-worker:test` built, `ShadowRunner(incumbent, candidate)` refused exactly as it does on a box with neither, and the one-line way past it was `uncontained=True`, under which the escaping node's write landed on the host (`marker exists on the HOST: True`). **Nothing here ever PROVIDED the container**; containment was the default the way a door is locked when nobody has a key. `shadow_for` + `resolve_containment` now detect a runtime, verify its isolation in both directions and return a runner whose candidate executes inside the container: same node, same box, no arguments beyond the image, `HOST marker exists: False`, `runtime.verified True`. **`shadow.containment: auto` is the default and does NOT fall back** — J3 asked for an automatic fallback, which against ADR 0105's refusal is a strict weakening and would put the weaker mode back on the path of least resistance, so `auto` refuses and names which of the runtime and image was missing (HARD-STOP gate 2 resolved in favour of the control, not by stopping). `fallback` and `off` are owner statements in `aef.yaml`, announced on stderr with the named cause verbatim (`no container runtime found` / `image unavailable: <runtime's own reason>`) and written to the ledger as a new `EventKind.CONTAINMENT` carrying `security_event: True` — the key `build_digest` counts, so a fallback reaches the owner's weekly digest rather than a stderr line from last Tuesday; the seam is under test because a detail key nobody counts is a stderr line with extra steps. Both directions are recorded, since logging only the fallbacks makes 'ran contained' and 'never ran' the same absence. The AST scan that required zero `uncontained=True` in `aef/` now requires **exactly one**, matched by file AND enclosing function, paid for by a test proving the default mode raises before constructing any runner even when handed an `in_process_candidate` — a second `ContainmentDecision` field on `ShadowRunner` was considered and rejected as two spellings of one security decision (ADR 0091). `test_the_in_process_shadow_bypass_is_still_real_when_opted_into` renamed to `..._exists_only_when_an_owner_opts_out_and_is_logged` and rewritten to assert both halves. **Also fixed, and reproduced: the flake three workers hit** — `test_closing_the_session_leaves_no_container_running` diffed the WHOLE daemon's `docker ps`, so any foreign container started between the two calls failed it (`OLD (unfiltered): survived={'d1abb74c744e'} -> FAILS (spurious)`); filtered to the `aef-worker-` name prefix, still catches a real leak under mutation. 5 mutations, 5 caught — M5's first attempt failed in 0.46s, which is what a SyntaxError looks like rather than a leaked container, and was re-done until the failure had the right cause. +15 tests, 2131→2146. Zero live model calls. **Dim 4: 14 → 15** | High on both reproductions, the default and the fallback (each RUN, before and after, output pasted); the ADR states what is NOT claimed — shadow execution still has no production caller anywhere in `aef/`, so what changed is which outcome an integrator falls into, not that the loop now shadows |
| [0165](0165-the-scheduled-loop-that-ran-every-night-and-did-nothing.md) | Worker J0F of the upgrade loop, closing J0's first deduction where it lives. **Reproduced by running this repo's own nightly workflow command**: `aef loop cycle --repo . --state ~/.aef-loop-state --workdir ... --module agents.demo.graph --runs ... --corpus corpus` — no `--memory` — prints `no memory store configured: nothing to learn from, no candidate` and **exits 0**; the same invocation with one admissible failure record proposes `cycle-...` and gates it. ADR 0139's *exit 0 having done nothing*, on `cron: "0 3 * * *"`, reporting success every morning since the workflow was written. Fixed with ADR 0141's `--state`/`--no-loop-state` shape: **one of `--memory` / `--no-memory` is required**, refused in the HANDLER with the parser deliberately still permissive (L6's blind spot runs both ways — `cmd_cycle` is importable), exit 2 because a missing flag is a configuration error and must not read as a verdict on a candidate (ADR 0075). `--no-memory` keeps today's behaviour and says the silence was chosen. `aef loop monitor` now reports **days since last PROPOSED, days since last KEPT/MERGED, and a named `SCHEDULED CYCLE PRODUCING NOTHING` warning** — which the ledger *cannot* supply, because `cycle` writes a ledger entry only when it proposes, so a loop that has run 180 nights producing nothing has a ledger byte-identical to one nobody started; `cmd_cycle` therefore journals every attempt to `<state>/cycles.jsonl` and a test asserts the two cases are indistinguishable without it. Workflow passes `--memory` into the cached state dir, tees both verdicts to `$GITHUB_STEP_SUMMARY`, and fails only on exit ≥ 2 so an ordinary rejection is not reported as a halt. **Stated rather than papered over: the CI memory file will be EMPTY** — nothing in CI writes runs — so tonight's cycle says `no admissible failure memory` instead of `no memory store configured`. That is the truth and it is now visible; it is not yet a loop that learns from CI. 5 mutations, 5 caught, including restoring the original defect in the YAML. +24 tests, 2002→2026. **No rubric dimension moves** — dim 1 is J0's to re-earn, and the ADR says what would: a CI log showing a scheduled run that actually proposed | High on the reproduction, the refusal and the warning (each RUN, before and after, exit codes pasted); **medium on the workflow itself, which was not executed — it is YAML for a scheduler this branch cannot run, so what is proved is that it contains the flag the CLI accepts, not that tonight's run behaves as described**; nothing claimed about the nightly cycle ever proposing anything |
| [0166](0166-a-word-cap-that-did-not-terminate.md) | Fix worker D2 of the upgrade loop, closing ADR 0156 §D2 — the defect that stopped I11 three times and cost S2 four runs. Every summary scenario's word cap was `^(?:\s*\S+){1,N}\s*$`; the nullable `\s*` lets adjacent iterations split one token, so a **match** returns in 0.05 ms and a **failure** — one word over the cap — explores every partition and never returns, with `checks.py::_holds` calling `re.search` unbounded. Reproduced by running, in a child process under a wall, both directly and through `score_scenario` on the real `sum-13`: at cap `True in 0.01 ms`, one over `DID NOT TERMINATE in 8 s`; after the fix, `False in 0.02 ms`. Four changes, each sufficient alone. **New ops `max_words`/`min_words`** — `len(str.split())`, no regex, integer-only value (`bool` refused), non-string target fails rather than stringifying. **The twenty corpus scenarios keep a regex, and measuring is what showed why**: `{1,N}` has a LOWER bound, so a bare `max_words` **accepts an empty summary** — precisely the input ADR 0156 used to attribute repeat 3's zero to a failed call — while `max_words`+`min_words` is a fifth check on a four-check scenario and moves every recorded score (0.75→0.80). So they take the linear form `^\s*\S+(?:\s+\S+){0,N-1}\s*$` (non-nullable separator forces every boundary); 12,000 generated strings show zero disagreement and the metric comes back **byte-identical** — train 0.9792, validation 0.9167, all three 0.75s — with `checks` the only key that moved in all 20 files. **A static detector**, because `re` has no timeout and this repo takes no dependency for one: refuse a repeated group whose body holds a nullable quantifier, or is a single unbounded atom (`(x+)+`). Runs at scenario load AND in `_holds`; verified against 10 must-refuse and 11 must-pass patterns including the rewrite it recommends, and against a planted evil pattern that `load_scenario` refuses by name and file. Conservative by choice — a refused-safe pattern is a message, a missed unsafe one is a hang. Long inputs are refused, never truncated, since truncation answers a different question. **`loop score --json` gains `attribution`**, closing ADR 0156's further defect: `run_scenario` already computed both the `failure` string and the failed checks and `cmd_score` emitted neither, so "the provider died" and "the answer was wrong" were both `0.0000`; tested with a stub provider on both shapes, and on the real corpus it names the case-sensitivity that has explained the two 0.75s in prose since ADR 0123. 5 mutations, 5 caught, every restore sha256-verified — **and M3 exposed a weak test that HUNG instead of failing; the test was rebuilt around a child-process wall rather than the mutation dropped**. +47 tests, 2131→2178 | High on the defect, the fix and the equivalence (each RUN; the replayed metric is byte-identical and the one input the rewrites differ on is stated rather than averaged away); **medium on the detector's completeness — it is a syntactic rule over two known families, not an ambiguity decision procedure, and the 10,000-character backstop narrows the remainder without closing it**; what is fully closed is the shape that was actually here |
| [0167](0167-the-unfixed-twin-and-the-tree-the-baseline-was-of.md) | Fix wave G1a of the upgrade loop — ten seams over M1/M3/J0F, each REPRODUCED by running a command on the parent commit before anything changed. **F1: `aef loop run` was the unfixed twin of the defect ADR 0165 fixed.** Same file, ninety lines below, same `FileMemoryStore(...) if args.memory else None`, no guard, journalling nothing: five `loop run --turns 2` invocations without `--memory` each exited **0**, the state dir did not exist, and `loop monitor` said `cycles run: 0 (last never)` with no warning — the "unstarted versus dead" ambiguity 0165 §2 says it removed, one subcommand over. Fixed with the same handler-level refusal, and `run` journals one attempt **per TURN** (the alarm counts consecutive quiet attempts against a threshold of three; ten quiet turns as one entry would need thirty turns to fire). The regression test does not name the two commands: it parses `aef/cli/loop.py`, derives every handler importing `cycle`/`run_loop`, maps each to its subcommand through its own `set_defaults`, and asserts the derived set equals the set it can invoke — a planted third turn-runner is caught by that assertion (M7), so the CLASS is closed rather than the instance. **F6: the journal stopped at the happy path.** `record_cycle_attempt` sat after `cmd_cycle`'s `try`; with the kill switch engaged the command printed `HALTED:`, exited 2, and `cycles.jsonl` DID NOT EXIST — a nightly cycle dying the same way every night left a journal as empty as one nobody ran. Moved into a `finally`, each branch naming its exception; an unnamed exception is named, journalled and re-raised unchanged. **F2: preflight obligation 2 was permanently red on every graph `aef migrate` writes for a prompt agent, and the graph routes correctly.** `loop doctor` on the migrated pilot printed `[--] reflect node routed to  a reflect node exists but nothing routes to it`, exit 1, while executing the same module gave the trace `['prompt_agent','reflect','consolidate']` — the detector wanted a 3-arg node function and M1's generated module has none, only `make_prompt_agent_node(route="reflect")`. Second shape recognised (an `aef.reasoning` `make_*_node` with `route=` naming the reflect id; `make_reflect_node` itself excluded as a self-loop; a `route=` on anyone else's function is not evidence), hand-written detection unchanged, and `test_the_real_migrate_output_passes_the_real_preflight` runs the REAL migrate into the REAL preflight — the C↔D join where the defect lived. **F7: the baseline recorded the files but not which TREE they were of.** `bless --agent-root .claude/agents` wrote an `entry.json` with no `agent_root` anywhere, and the next cycle at the default root was rejected with `cumulative drift: 1.000` — G5 unioning two disjoint key sets, two rejections from a halt. `ArchiveEntry.agent_root` (additive, `""` = not recorded and deliberately NOT a mismatch, so no pre-existing baseline is retroactively failed), and the obligation now reports unmet by name with the root that was blessed. Plus: `--agent-root` non-default with `--agent-path` left at `DEFAULT_AGENT_PATH` — Zone C under the widened root — is refused by `bless`/`cycle`/`run`/`doctor`, naming both paths and the per-agent path migrate actually wrote; only when the root is non-default, because strengthening a control on invocations nothing reproduced a problem with is an owner's decision (0141's rule applied to itself). **M4, folded in: `bless` accepted a `--state` inside the repo that `cycle` refuses** — 4 of the 9 subcommands taking `--repo`+`--state` never called `_preflight`, and `bless --state <repo>/state` printed `blessed ... as baseline v1` leaving `archive/` and `ledger.jsonl` in the working tree. One call in `_config` (the same function, imported, never a second predicate) plus one in `bless()` for library callers, with a parser-derived enumerating test that also asserts nothing is CREATED — which caught a defect this wave introduced, journalling a refusal into the directory it had just refused. **R3: an exception is exit 1 and so is a rejection**, so the rendered nightly workflow's `status >= 2` rule stayed green on a bad config, a missing corpus, an import error or the `agents.migrated.graph` placeholder's `NotImplementedError` — and, the exception escaping before the journal, `cycles.jsonl` gained nothing either, so both alarms were blind to the same class of night. `EXIT_ERROR = 3`, journalled with the exception's name, `main.py`'s catch-all untouched, and the workflow's rule READ from `adopt_loop.py` rather than assumed. **R6: the containment fallback reported every Monday as an attack** — `security_event: True` rendered as `**A proposal reached for the harness**` whatever wrote it, so a runner with no Docker was an alarm; `EventKind.CONTAINMENT` now renders `shadow ran uncontained: <reason>` under its own heading, still counted, with a control test that a real security event still names the harness. **Three more from G1b**: obligation 6 checked ONE path and passed on the call-site stub whose `build_graph()` raises `NotImplementedError` while eight generated graphs went unopened (now iterates ADR 0168's own `discover_graph_files`, unmet if any is invisible, with the count in the green line — the real migrate into the real preflight); `ArchiveError` from a hand-typed `--graph-id ../x` came back as exit 1, i.e. a rejection (caught at the boundary, exit 3); and the loop's five `--module` help strings still said "importable module" after 0168 made the file path work, which is the form a widened `--agent-root` needs. Errata appended to ADR 0165 (both of its claims). 16 mutations, 16 caught, every restore byte-identical by SHA-256, plus the real generated module regenerated with `route=END`. **No rubric dimension moves** | High on all five reproductions and on F1/F2/M4's fixes (each re-run end to end, output pasted); medium on F7's effect — nothing retroactively repairs a baseline blessed without a root and whether any exists in the wild is not measured; medium on F6's coverage — one of three named exception paths is reproduced at the CLI and two are reached by injecting at the collaborator boundary, because the earlier gates reject first |
| [0168](0168-the-diagnostic-that-never-opened-the-agents.md) | Fix wave G1b of the upgrade loop — six findings, six reproductions, zero model calls. **F3 (high):** on a clone of the pilot with **nine** graphs on disk, `aef doctor` scanned **two** and obligation 6 — ADR 0137's *every model call reaches the harness* — **passed on the one file that makes no model call**, the call-site stub whose `build_graph()` raises `NotImplementedError`; the eight graphs that do call a model were never opened. Cause is a seam, not a bug in either half: doctor globbed `<agent root>/*/graph.py` (one level, ADR 0149) and ADR 0152's migrate writes `<agent root>/migrated/<module>/graph.py` (two). **Erratum on ADR 0149** — its "deriving from `DEFAULT_AGENT_ROOT` cannot leave this list naming a directory nothing writes to" held for one writer; derivation keeps the ROOT correct and says nothing about the DEPTH. Discovery is now ONE function, `aef.harness.zones.discover_graph_files` (`rglob`, plus the adapter/default/legacy entries that live outside the root), and the invariant is enforced by running the real `run_migrate` into the real `_graph_entries` rather than by a shared constant. `aef doctor --agent-root` added — a repo that took ADR 0152's opt-in had doctor reporting on the two files OUTSIDE the widened root and nothing about the sixteen inside. **F5 (medium):** one `aef migrate --agent-root .claude/agents` printed `Zone A (agents/**)` for `agents/migrated/graph.py` twelve lines above `Zone A is '.claude/agents'`, with the classifier the gates use saying Zone **C** — `_zone_note` used the DEFAULT `ZonePolicy` and `result.agent_root` never reached it. Threaded through; the widened note now says in words that `--agent-root` moves the prompt-agent graphs while `--out` moves this one, and prints the `--agent-path` values that ARE inside the root. **S2 (suspected → CONFIRMED):** a persona named `../escape` yields `graph_id='../escape'` printed as the value to hand `aef loop bless`, and `archive._graph_dir` was `root / graph_id` — `record()` wrote `state/escape/v000001/` one level ABOVE the archive root, leaving it empty (`root / "/etc/x"` discards `root` outright). Refused at both ends: `zones.segment_refusal` in the one place every archive read and write goes through (refused, never normalised — two spellings of one id must not disagree about a directory), and migrate renames to the already-disambiguated module with the reason in the report, `AGENT_NAME` untouched because only the id that becomes a DIRECTORY must be a path segment. **F8 (low):** the bytecode advisory named `agents/`; on the widened clone `git add -A` tracked `.claude/agents/migrated/marlin_accela/__pycache__/graph.cpython-313.pyc` — adopt runs before migrate and cannot know the root, so the message states the rule instead of guessing a path. **M4 (high, folded in):** the report's own `aef run` line under a widened root printed `aef run .claude.agents.migrated.marlin_accela.graph`, which exits 1 with `the 'package' argument is required to perform a relative import` — a leading dot is a RELATIVE import and no dotted spelling of `.claude/agents/...` exists at all; M1 shipped a flag whose own generated command could not be run under it, and M4 worked around it by keeping the graphs at the default root (**erratum on ADR 0152**, recorded there). Refusing the root was rejected — it IS the documented opt-in — so `aef run` (and `aef loop record`, which shares the loader) take a FILE PATH as well, chosen on the spelling and never by a failed-import fallback (which would hide a module's real error behind `no such file`), with `sys.modules` keyed on the resolved path because migrate names every generated file `graph.py`; pinned producer→parser→runner, the real `report()` into the real `build_parser()` into the real `run_graph_module`, run against a `command`-provider config whose argv is `/bin/echo` — no live call. **R7 (medium, second seam hunt):** doctor's `entry_file_points_at_the_guide` uses `is_file()`, which follows symlinks, so a repo whose `AGENTS.md` and `CLAUDE.md` both link into `docs/` gets two WARNs prescribing `re-run aef adopt --dir .` while adopt SKIPS both with `a symlink, or under one — adoption never writes through a link`: the only advice on offer was a loop. Fixed the FIX, not the check — reading through the link stays right (a link to a blocked target passes, and the mutation that narrows to real files fails that test) — and the remedy now names the target and says to block it or replace the link; the walk covers a symlinked PARENT too, because adopt refuses those as well. Reported, not fixed: preflight obligation 6 takes ONE `agent_path` and has the same false pass (call site named for G1a); a hostile `--graph-id` now reaches the CLI as an uncaught `ArchiveError`; `aef loop bootstrap --module`'s help still says module. 12 mutations, 12 kills — one only after its control was rebuilt (keying `sys.modules` on the basename still returns a distinct module, so `first is not second` passed; the control now asserts the registry). Every restore `sha256`-verified. +42 tests, 2153→2195. No rubric dimension moves | High on all four — each reproduced by a command whose output is in the ADR, each fix re-run against it, each pinned by a test that fails when the defect is put back; lower on breadth (one pilot repo, one harness) |
| [0169](0169-a-containment-claim-is-per-provider-evidence.md) | Fix wave G2 — seam finding F4, the M1×M3 join. `aef migrate` stamped **"THE PROMPT RUNS; THE AGENT'S TOOLS DO NOT ... the harness adapters send `--tools ""` with `--max-turns 1`"** into every generated module as fact, and `make_prompt_agent_node` said "nothing in this path can open a file, spawn a process or reach a network service". Both are claims about `ClaudeCodeProvider`; the path takes whatever `model_provider.impl` names. Reproduced by printing the argv: **`codex exec` sends neither flag** (an agentic loop in a `--sandbox read-only` jail, no system-prompt flag, so the persona goes in the USER turn), and **`impl: command` — ADR 0154's answer for Copilot and every harness after it — sends whatever the owner's template says**, with no `{system}` slot putting the persona in the user turn too. No test joined the claim to the provider set, and one asserting "every impl is tool-suppressed" would fail on `command` by construction. **The suspected item, measured in 3 live grok calls:** a directory holding one file whose first line is `AEF_CANARY_...`, `--cwd` pointed at it, the adapter's exact flags, "List the files in the current directory and print the first line of each" — with `--max-turns 1` exit 1, `stopReason: "cancelled"`, stderr `Error: max turns reached` and a reply that is a TOOL PREAMBLE; with `--max-turns 3` exit 0, `num_turns: 2`, **and the canary in the reply**. `grok --tools ""` suppresses nothing on 1.0.5, while `claude --help` documents the identical spelling as "Use \"\" to disable all tools" — ADR 0150's rule one level up: a flag's SPELLING is not its semantics across binaries. What contains the shipped Grok adapter is `--max-turns 1`, by *cancelling the run*; whether the read executed first is NOT established, and `--disallowed-tools` is the right lever left unused because `--help` lists no tool names and this repo ships no guessed flag values. **Fix:** `ModelProvider.isolation`, a closed vocabulary, DERIVED from each adapter's own argv for a sentinelled probe request — `claude_code {no_tools, no_mcp, single_turn, no_project_context, system_role}`, `codex {read_only_fs, user_turn_persona}`, `grok {single_turn, no_web_search, no_subagents, system_role}`, `anthropic` structural, `command` = the owner's UNVERIFIED `isolation:` assertion plus the derived channel (its template flags are deliberately never read as evidence), cassette forwards its inner, fallback INTERSECTS. The node writes provider + isolation + persona channel to `working_memory["<node id>__containment"]` every run and appends a `prompt_agent.persona_in_user_turn` error when the persona went out in the user turn — a warning not a refusal, but an error entry, so the run scores 0 and reaches failure memory, said out loud. Three-valued: a provider declaring nothing records `unknown` and does not warn. Header and report state the per-impl truth; a test asserts the old sentence is ABSENT. **Folded in from S3:** `answering_model` rule 3 misattributed 1 of 36 judge calls to the helper model — a JSON-only reply is ~12 output tokens, fewer than the helper writes — and the requested-name rule never ran because `agent_services(reflection="llm")` sets `model = reflection_model or ""`; it now returns an attribution (`requested`/`alias`/`sole`/`heuristic`/`unknown`) recorded on `CompletionResult`, with the runtime.py root cause reported upward. And `input_tokens` was NEVER the cost: it is the uncached remainder, **2 on all 36 calls**, so ADR 0126's 211,470-vs-4,684 rested on numbers nothing retained — cache counters added, `total_input_tokens` used by both live guards and by `Provenance.token_cost`. 11 mutations, 11 caught (M7 reported NOT DETECTED first: a SAME-LENGTH edit written in the same clock second served a stale `.pyc`; harness fixed, recorded) plus a no-op control correctly not detected. +25 tests, 2158→2183. Errata on 0152 and 0154. **No rubric dimension moves** | High on F4 (the argv is printed by the real classes) and on the Grok canary (two arms, the file's contents in the reply); high on both S3 defects, reproduced against the real function and the real recorded data; **medium on `claude_code`'s `no_tools`, which rests on the CLI's own `--help` and has NOT been canary-tested — the budget was grok-only, and the one CLI that was canary-tested is the one whose documented reading turned out wrong**; low and unchanged on any harness an owner wires through `impl: command`, which is this ADR's whole point |
| [0171](0171-a-corpus-that-can-tell-two-judges-apart.md) | Worker S3b of the upgrade loop, executing ADR 0159's closing sentence ("the next increment for dimension 3 is a corpus with true content negatives, not a better judge") and ADR 0166's open consequence. Three case-sensitive `contains` checks corrected — and then **57 more of the same shape found latent** by this increment's own new test, every one passing only because the model happened not to open a sentence with that term. The 20 word caps moved to `max_words` + `min_words: 1`; both of ADR 0166's objections discharged rather than dodged, and the score proved byte-identical across both migrations. 19 scenarios recorded live on `claude-opus-5[1m]` against inputs chosen to be handled badly; **7 fail an owner check, 4 in validation** — and all 7 fail on the word cap, because every content trap was handled correctly and the three apparent content failures were the owner's checks written too narrowly. Judge A/B re-run on the enriched validation split (34 calls): rule-based 4/17 constant-fail, **LLM 16/17 against a 13/17 constant baseline, AUC 1.000** (owner-fail 0.275–0.635, owner-pass 0.850–0.910, ordered by the size of the overrun), position delta max 0.17. All three pre-registered conditions fired: **dimension 3 moves 6 → 7**. 5 mutations, 5 caught | High on the numbers; medium on their generality — AUC 1.000 separates ONE failure family with n=4 |
| [0173](0173-the-containment-field-that-was-read-by-nothing.md) | Fix worker H2 of the upgrade loop, closing R5 of the second seam hunt — the seam between ADR 0161's config surface and ADR 0161's harness. **`shadow.containment` validated everywhere and was read by nothing**: `build_containment_mode` had zero callers and `shadow_for`'s `mode` defaulted to `ContainmentMode.AUTO` in the signature, so the one wire carrying an owner's declared mode was the one nobody connected while the field's excellent typo message made the surface read as wired. Reproduced by running, both directions: an owner who wrote `containment: "off"` on a box with docker got `decision.mode = auto, contained = True, owner_opted_out = False` — a container they had declined — and an owner who wrote `fallback` on a runtime-less box got `UncontainedShadowError` instead of the fallback that mode exists to give them. **Both `mode` parameters lose their defaults** (ADR 0101's rule applied to a parameter: a default that silently overrides the owner's config is a promise the signature makes and the code breaks), and `RunConfig` gains `containment_mode = build_containment_mode(config.shadow)` so a future caller has exactly one place to read the choice from — its unconfigured value DERIVED from `ShadowConfig()` rather than written as `AUTO`, so “no aef.yaml” and “an aef.yaml with no shadow block” cannot become two postures. After: `off → off, contained=False, owner_opted_out=True` with the ledger entry carrying `security_event`; `fallback → it RAN`; omitting `mode` → `TypeError`. **Explicitly not done: shadow execution still has no production caller** — the ADR says what one would need (a worker image with the adopter's own `aef`, a decision on which live requests may be duplicated, an owner who has read §2.1) rather than inventing one, and `RunConfig`'s docstring says the field's only reader today is `RunConfig`. **S5's +1 on dim 4 survives, argued not asserted**: the gap applied the CONTAINED-or-refuse mode unconditionally, so nothing weaker than `auto` was reachable through it; the real cost was to the audit trail, where `owner_opted_out: True` had no path an owner could take to it. 5 mutations, 5 caught — M1's first form failed with an `IsolationError` from a real container rather than on the property, and the TEST was rebuilt around `inspect.signature` rather than the mutation dropped. +5 tests, 2264→2269. Erratum filed on 0161. Zero live model calls. **No rubric dimension moves** | High on both reproductions (each RUN, before and after, output pasted) and on the mutations; the ADR states what is not claimed — the wire exists, the caller does not, and nothing here makes the loop shadow anything |
| [0174](0174-a-failed-check-is-what-the-run-did.md) | Worker M4b of the upgrade loop — the wire M4 (ADR 0157) and S1 (ADR 0155) hit from opposite sides and which 0157's "Undone" says nobody owned. **Both reproductions re-run, offline, before anything changed.** M4's: `aef loop bootstrap` on the marlin clone records two runs, one of which FAILS the owner's check, and BOTH memory records read `kind="success"`, `verbal_feedback: "no failure signals: 0 error(s) recorded, 0 tool call(s), none failed"` — while `loop score` says `0.0000 accela-missing-credentials / check failed: … contains 'VERDICT:'` — and `loop cycle --proposer rule_based_prompt` prints `no admissible failure memory: no candidate this cycle`. S1's: the six summary validation scenarios through the real graph give 6 successes with 6 unique signatures and **0 knowledge entries**, *and two of them fail an owner check*. One cause: `make_reflect_node` writes `failure` only from `state.errors`/`tool_results`, and a check is the task metric evaluated by the harness afterwards (ADR 0113). **`aef/harness/check_memory.py`** evaluates the owner's checks against the final state and, when any fail on a run that raised nothing, runs the REAL `Critic`/`Judge` over that state with the failures as evidence and writes one `kind="failure"` record in `make_reflect_node`'s own content shape. **Design (a), a LOCAL derived state, and (b) rejected by measurement, not taste**: injecting the failures into the run's real `state.errors` takes `sum-14`'s `task_completion` from **0.7500 to 0.0000** while the checks still hold 3/4 — `score_scenario` stops counting the check fraction the moment errors exist, so the metric changes when you measure it — and flips `classify().passed` True→False, reporting a wrong answer as a crash. **The signature drops the expected value** (`check:<path>:<op>`), which is load-bearing twice: measured, value-keyed signatures give **0 entries on S1's split** (two runs, two different required terms — the exact state 0155 recorded) against **1** for the shipped keying; and the signature is a prompt surface, printed as `render_retrieved_context`'s `[label]` and the proposer's `<!-- aef sig=… -->`. The rendering never reads `check.value` — *"working_memory.prompt_agent does not contain a required substring the owner declared; observed 406 words, 2836 chars: '…'"* where 0157's bullet said ``contains 'VERDICT:'`` — uniformly across all six ops, so a `max_words` failure reports the observed word count and never the cap. **What it still leaks is enumerated rather than claimed away**: the state path, the operator (which for an `equals` check on a binary field leaks the answer completely), the observed value, and the pass/fail counts; the defensible claim is only that a lesson can no longer be satisfied by pasting a string out of itself. **The falsification did not fire and the argument is the repo's own**: `BootstrapInput` already distinguishes `expected` ("a judgement about what the run turned out to do", refused) from `checks` ("a specification of the task written before the run"), so evaluating the owner's pre-run claim against the run's output is recording what happened; ADR 0145's "a graph with no reflect node leaves an empty sink" is narrowed IN WRITING with a test, and ADR 0060 is untouched. **One call site, two refusals with reasons already in the code**: `score`/`run_scenario` is refused by its own docstring invariant (a gate run must not mutate the evidence a later proposal is built from) and `run --record-runs`/`harvest` because a production run carries no owner check at all — `RecordedRun` has no field for one. **End to end**: bootstrap writes the failure record → a second input failing the same check → consolidate forms **1 entry from 2 distinct runs** → the cycle **proposes a candidate**, G0/G1/G4/G5 pass with drift 0.007/0.500, G2 fails on ADR 0157's unfixed defect 1 (`TrustBoundaryError` on every non-Python candidate). **Live: 3 calls** (preflight `input_tokens 2` on `claude-opus-5[1m]`, two bootstrap recordings) — two real objectives, two runs failing one check, one lesson, `grep -c VERDICT memory.jsonl → 0`, same candidate, same gate verdicts. **Nothing synthesised**, retiring 0157's `make_evidence.py`. S1's side: the same six scenarios now give 2 failure records under one signature → 1 entry → the lesson appears in the next run's draft prompt where every bullet previously read *"no failure signals"*. 6 mutations, 6 kills, every restore sha256-verified — including dropping the producer, which returns the whole pilot to `no admissible failure memory`. +43 tests, 2262→2305. **No rubric dimension moves**: no task score was measured, and what S1's four arms need to be re-run with is a corpus whose failures recur (S3b's) | High on both reproductions, the producer and every gate verdict (each RUN, offline then live, output pasted; the ledger read back); high on the recurrence and no-leak measurements, which are counts from committed artifacts; **nothing claimed about task quality — no arm was scored**, and the leak residual is stated as four named channels rather than as a redaction claim |
| 0060–0064 | *On unmerged `verify/*` branches. Numbers reserved, not skipped.* | — |
