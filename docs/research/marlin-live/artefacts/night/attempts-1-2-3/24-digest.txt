<frozen runpy>:128: RuntimeWarning: 'aef.cli.main' found in sys.modules after import of package 'aef.cli', but prior to execution of 'aef.cli.main'; this may result in unpredictable behaviour
# Self-rewiring digest, 2026-08-30 to 2026-09-06

- Proposed: 5
- Merged: 0 (acceptance rate 0%)
- Rolled back: 0 — net accepted 0
- Rejected: 5
- Escalated to you: 0
- Security events: 0
- Halts: 0
- Scenarios added to the corpus: 0
- Drift from the blessed baseline: 0.000
- Out-performing you editing code directly: n/a — nothing to compare yet
- Production runs recorded: 10
- Halt channel configured: yes — /bin/sh (3 argument(s))

**10 recorded, 0 admitted to the corpus.** Recording is not harvesting: a run becomes a scenario only if `aef loop harvest` re-executes it identically and its behaviour survives redaction. Which of those it was is in that command's own report line — run `aef loop harvest <graph> --runs <dir> --corpus <dir>` and read it. Two answers are ordinary — every run passed (harvest promotes failures unless `--include-successes`), or the daily limit held them back — and one is not: `REJECTED, did not re-execute deterministically` on every run means the ingestion path is broken, not quiet.
