# Self-learning loop rubric — and the baseline score

A score is only honest if the scale is written down before the scoring and
each dimension moves only on evidence. This is that scale. `RESEARCH_LOOP.md`
re-scores it against the external field; `IMPROVE_LOOP.md` works the gaps.

**A dimension's score changes only with a cited artifact** — a test, a
measured A/B, an ADR, a file — never with a sentence saying it improved.
A self-graded number is exactly the failure the trust case warns about.

| # | Dimension | Weight | What full marks means | Field reference |
|---|---|---|---|---|
| 1 | Closed measurement loop | 20 | A fixed task benchmark, one scalar metric, a time budget; every change is kept or reverted on the metric, automatically, with holdout separation | autoresearch (keep/revert on one metric, ~100 runs/night); DGM (SWE-bench 20→50%); SICA |
| 2 | What gets learned | 20 | Experience changes behaviour at the strongest safe level: raw records → consolidated knowledge/playbook → skills/tools → harness code. Each level measured against the one below | ACE (+10.6% agents, playbook not summary); WikiSkill (wiki ablation load-bearing); Voyager/AgentFactory (skill library); DGM/MOSS (code) |
| 3 | Reflection quality | 10 | LLM critic + judge over recorded signals with position/verbosity/self-preference bias controls, measured against the rule-based baseline | Reflexion; ExpeL; ACE reflector/curator |
| 4 | Safety, containment, governance | 15 | Deterministic replay, deny-by-default policy, HITL, gated promotion, containment, adversarial rounds, an owner-readable trust case | Almost no research loop has any of this |
| 5 | Evidence and honesty | 10 | Every claim of improvement has a measurement; every knob's default has a reason; negative results recorded | Statistical limits of self-improvement (2510.04399) |
| 6 | Open-endedness / archive | 10 | Candidate lineage kept; parents sampled for diversity, not just the current best; stepping stones survive | DGM archive; SWE-Exp |
| 7 | Real-signal ingestion | 10 | Learns from live runs and real tenants, not a synthetic corpus; telemetry closes the loop | Arize "closing the loop"; Live-SWE-agent (on-the-fly) |
| 8 | Adoptability / harness-native | 5 | Drops into a repo whose agents are coding-agent sessions; no key; cross-tool | — |

## Current — 2026-09-08, J0b's re-score (ADR 0188) over J0's base (ADR 0151), plus the above-95 and to-95 loops' increments — **92 / 100**



*The base is J0's independent score (68, ADR 0151). Every row above J0's is a
self-graded increment measured after it — each names its ADR and the artifact,
and each is exactly the kind of row a future J0 should re-check. A heading that
said "after J0" over a table containing such rows was mislabelled (second seam
hunt, S2).*

*Corrected 2026-09-04 (ADR 0127): every total in this file up to and
including this one was carried forward by hand as `previous + delta` and
was one point low, because the baseline's own rows summed to 51 against a
stated 50. **No dimension's score or evidence changes** — only the
addition. `tests/test_rubric_arithmetic.py` now recomputes the heading
from the rows, so a total nobody checked cannot recur.*

| # | Score | What moved and the artifact |
|---|---|---|
| 7 | 7/10 | Q1 (ADR 0204): the loop installed into **`/Users/raptor/marlin`** — a Florida permit-data pipeline with eight personas its owner writes and uses — and left to run. **(a) Real objectives from this repository's own work entered the corpus through `harvest`**: ten questions whose every fact is read out of `pipeline/jurisdictions/*.yaml`, `AGENTS.md`, `docs/COUNTY_ONBOARDING.md` and the commit history, answered live by `claude_code`; corpus **`{"harvest": 5}`, bootstrap 0, record 0**. **(b) The redaction control passed**: 0 substitutions over ten real runs, **10 of 10** planted shapes caught, 2 of 3 secret-shaped keys dropped, the residual named (`opaque_secret` fires on a public ArcGIS endpoint URL), and marlin's Azure subscription id checked **by value** in every committed artefact and absent from all of them — five of the eight personas carry it and the one this pilot ran does not, so the zero is a property of the persona and is stated as one. **(c) A candidate was proposed and gated on that evidence**: both `grounded_in` ids are check failures of harvest-sourced corpus scenarios, `live_model_calls: true`, six gates, 28 scenario executions, drift 0.007/0.500. **The night kept nothing, so this is +2 and not +3** — and the reason it was right not to is measured rather than assumed: three arms over the same five scenarios, one variable each, incumbent **0.6667** (cassette), a placebo `## Lessons (aef)` bullet **0.8667** (live), the loop's own bullet **0.4000** (live), with two 1.0000 scenarios collapsing to 0.3333 and 0.0000. The harm is the bullet's text, not the excerpt (already removed by ADR 0180) and not the section header. **Held against this row, in it: G3's verdict was `could not judge` — 4 dead calls of 4, the operator's quota having run out mid-night — so a reader who requires a completed G3 should score this +1.** Five defects found by running (F-Q1-1 a persona written for a tool-using agent hallucinating its own tool results under `no_tools`; F-Q1-3 `bless --graph-id` vs `run`'s `default` archive key; F-Q1-4 a single-use `--workdir` the generated `LOOP.md` names twice; F-Q1-5 an unlaunchable provider reported as a behavioural regression; F-Q1-6, fixed on the trunk), plus F-N7-1, F-N7-2 and F-P1-2 reproduced on a second repository. **The last three points stay unclaimed and the reason is not a missing measurement: marlin is the owner's own repository, not a third party.** |
| 1 | 18/20 | P1 (ADR 0200): J0b's dimension-1 deduction was three clauses and this answers all three. **(a) It has now closed unattended.** A scheduled multi-turn run on a copy of the peptide pilot, started and left — 262 s, 2 of 3 turns, 48 live calls, exit 0 — proposing from harvested production evidence, gated live (`live_model_calls: true`), **rejected both times by G2**, stopped on its own repeated-tree rule, kept nothing, moved neither `master` nor `loop/kept`, and left a ledger, a lineage, a journal, a digest and a status a person read cold: `docs/research/night-1/MORNING-REPORT.md`, with the two failed starts reported rather than tidied away. **It found a defect nothing else could**: `aef loop cycle` has derived the evidence graph id from its corpus since ADR 0176 and **`cmd_run` never called `resolve_graph_id_from_corpus` at all**, so the multi-turn driver dropped every memory record as another graph's and printed `no candidate this cycle` **at exit 0** on any adopting repo whose graph id is not the literal `default` — ADR 0165's shape a third time, reproduced side by side at zero live cost (`24-run-vs-cycle.txt`) and fixed. **(b) A turn may try several candidates.** Reproduced first — the rule-based proposer offers three distinct trees and `cycle` built ONE branch (`proposals[0]`, with a comment calling it deliberate). `--candidates N` gates each on its **own independent pass** in its own workdir and keeps the best that PASSED; the gates never see a set, and a `CANDIDATES` ledger entry records every candidate tried, not only the winner. **Cost measured, not asserted**: `cohort+2` corpus passes per candidate, i.e. 7 passes / 28 scenario executions / 24 live calls / 131 s per candidate on this pilot — linear, with no shared work, which is why the default stays 1. **(c) A held-out set is read by an automated comparison the loop cannot choose.** The decision is argued both ways in the ADR and the owner's holdout **stays the owner's** — n=2 here and n=0 on the pilot, and a cadence conditioned on the loop's own keeps turns it into a second validation split. `--audit-slice N` holds N train scenarios back instead, drawn by `audit_slice(corpus, at, size)` — a function whose GUARANTEE is the absence of any other argument, pinned by a signature test — rotating daily, held back from the gates AND from the proposer's evidence, and read afterwards as an advisory comparison recorded with `advisory: true`. Wired into this repo's nightly and the rendered adopter nightly; a test asserts no workflow names the holdout flag. 12 mutations, 12 caught, restores shasum-verified. **Remaining, and why this is 18 not 20**: nothing was kept on the night, so the audit has never fired on a passing candidate; `--candidates 2` was configured and the prompt proposer offers one lesson by construction, so no second candidate existed to try; and two turns in four minutes is not ~100 runs a night — the gap is the proposer's repertoire, not the driver |

| 2 | 17/20 | P2 (ADR 0201): the increment ADR 0193 named — *"this corpus cannot produce a second knowledge entry, so everything about ranking between entries is unmeasurable here"*. **Three lessons now form**, from three distinct signatures: a word cap (7 distinct runs), a new owner rule alone (4), and both at once (6). The rule — *an attributed claim stays attributed*, one constant regex — was **screened against the 39 existing recordings before it was written** (six candidates, `screen.py`; this agent had never once carried an attribution through a summary, 9 of 9) and then applied to **none** of them: it is carried only by fourteen scenarios whose checks were committed before their first live call, so ADR 0193's baseline and control arms are untouched. That the rule was chosen because it fails is disclosed, not buried. Four arms over 21 validation scenarios, 10 negatives, **120 live calls** on `claude-opus-5[1m]`: (a) 0.9000 ×1 · (b) **0.9107** ×2, spread 0.0024, byte-identical prompts · (c) ranking OFF = **(b) byte for byte** (one sha256 over 21 prompts, offline, matched live — 0 calls) · (d) **0.9547** ×2 at the boost that engages all three. Pre-registered rule, evaluated by script: (d) − (b) = **+0.0440** against a 0.0191 bar (2.30×) and **+0.0975** on the negatives. **The mechanism is legible rather than inferred**: word-cap failures go 8, 8, 6 across the control repeats to **0 and 0** with the lessons in the prompt. **And the knob is a switch, not a ranking control** — at boost 16.0 the three lessons hold ranks 0, 1, 2 on all 21 objectives in both repeats, and the same order holds at every boost from 0.0 to 16.0 without one inversion; between-lesson order belongs to staleness and confidence. **The pre-registered trigger for moving the shipped default fired and the default did not move**, stated as a departure: the engaging value was 8.0 for one lesson and is 16.0 for three, so it is a function of store size and staleness rather than a constant, and a scalar cannot express "engage the layer". A harm is reported (`sum-48` 1.00 → 0.75, a scenario that had been passing for the wrong reason). ADR 0180's 12-character excerpt scanner is shown to have a false-positive mode (4 raw hits, all `'er than the '`, from the harness's own fixed phrase) and now separates it; 0 windows of any run's own output in 105 live lesson blocks. Registered in `make measure` as `i12e-seed` + `i12e-arms`, 13 rows, zero live calls. Remaining: **two** repeats per arm, not three; the delta clears this rig's bar and not S1b's 0.0857; the Rule A lesson is close to unactionable because ADR 0174 strips the requirement from the text, which is why arm (d) still failed it 5 and 3 times; and `render_retrieved_context`'s constant five is still the real budget — with three lessons competing, they take three of the five bullets and the raw records get two |

| 3 | 10/10 | P3 (ADR 0202): this judge had only ever been graded where a **word count** would have scored as well — ADR 0159's 15/18 against a 15/18 constant baseline (AUC 0.322), then ADR 0171's AUC 1.000 over four negatives that were all word-cap overruns. So a set a word count cannot grade: six passages from `corpus/`, each contributing a **matched pair** — the agent's own recorded summary verbatim from the cassette, and that summary with ONE minimal owner-authored edit, the judged state differing by that string and nothing else. Six content families fixed before any was written: wrong unit (`340mm`→`340cm`), superseded figure (£2.4m/£3.1m swapped), swapped entities (Hessle **Low**/**High** Mill), fabricated reason under a right conclusion, dropped condition (the refusal RATE fell while the count rose), unsupported claim (a date the source withdraws). **Every case within its cap**, refused by the builder otherwise, so pass-everything, fail-everything and a word-count-only judge each score exactly **6/12**. Result: `LLMJudge` on the session default **12/12, AUC 1.000 over 36 pairs, 6/6 matched pairs the right way round, max position delta 0.100** — and the corpus's own regex checks, the non-model instrument a judge replaces, score **9/12**, missing exactly the three failures that look most correct (swapped mills, invented cause, swapped costs; judged 0.065, 0.150, 0.175). **Self-preference on the GRADING path +0.025** against ADR 0162's +0.260 on the comparing path, measured on identical cases with `claude-sonnet-5` as the disinterested judge. **The choosing-path control landed**: `PairwiseRanker` picking which summary to keep — both judges 6/6, 0/6 position-inconsistent — and the shipped guard refuses on that act both ways (0 calls declared, 1 call on this repo's own `model: ""`). 75 live calls of 90; 5 mutations, 5 caught; 18 tests; `make measure` row `i14b-hardset`, 8 pinned numbers. Three defects reported and not fixed, one of which corrects ADR 0171's stated mechanism (`max_words` never reaches the judge's evidence; the cap arrives through the objective). Remaining, and it is the honest weight of this row: **the negatives are AUTHORED** — ADR 0171 tried to record content negatives and got none, so this grades the judge and is never an agent failure rate; n=12 on one task family; the disinterested judge BREACHED the pre-registered 0.20 position bar at 0.550 and its one error is that instability, which the ADR discloses along with the reading under which this increment is +1 rather than +2; and nothing in `run_loop` calls the ranker — what that would take is written down in four steps |
| 6 | 10/10 | P4 (ADR 0203): **a kept candidate descends from a candidate the gates rejected** — the sentence ADRs 0121, 0160 and 0198 each looked for and none could write. ADR 0198 had the diagnosis exactly right and it was read as a dead end: `cycle` takes `proposals[0]` and `find_constants` returns constants in source order, so the rule-based proposer walks ONE axis deterministically, and `agents/demo`'s ladder is two-dimensional by construction. **The fix is not a better proposer; it is a ladder on the axis the proposer already walks.** `agents/ladder` — new, with `agents/demo` untouched so ADR 0198 still re-derives — puts `BATCH_SIZE` first in the file between a floor (the item count) and a ceiling that is a literal in the body, so neither proposer nor cohort can tune it. Staircase MEASURED through `aef loop score` and published **before any arm ran**: 3→**0.5556**, 4→**0.5556** (neutral, so G3 rejects it and it becomes a stone), 5→**0.7778**, 6→**0.0000**. `coerce_value` steps 3→4 and 4→5, so greedy — whose parent is the kept ref, which never moves — proposes 4, is rejected, proposes 4 again and stops: **greedy cannot reach 5, as a proof rather than an observation.** Seeds 0–9, both arms, **0 live calls** under a counter capped at 0: greedy **0/10** kept, sampling **8/10**, every keeper at 0.7778 from a parent at 0.5556 marked `reject` — the rung the probe predicted, which was a pre-registered clause so a lucky keep could not be counted as a climb. The two that did not climb drew the ROOT at turn 2, at the ≈0.28 the pre-registration computed from `_parent_weight` beforehand; the cohort blocked none, stated as luck in the measurement's favour. `seed0` shows the other half in consecutive turns: rung 5 kept, rung 6 caught by **G2's zero-tolerance regression rule** — sampling widens a search and does nothing to make it safer. **A defect found by running**: the first sweep reported 0 in BOTH arms because `python` was not on `PATH`, G1 rejected everything before it could be scored, and `_parent_weight`'s zero for an unscored reject collapsed sampling onto greedy — a null result from a broken harness is indistinguishable from a null result about the subject at the headline, and separable only in the turn log. **`sample_parents` stays OFF and is NOT deleted**, and ADR 0198's own deletion condition is settled by this run rather than deferred again. 5 mutations, 5 caught **on the second pass** — M4 escaped because the test pinned `RuleBasedProposer.step` to a copy of itself, M2 on a stale `.pyc` (same byte length, same second), ADR 0198's trap in a third place. 13 tests; `make measure` row `j2c-staircase`. Remaining, and deliberately low: a fixture built to have a staircase has one; one proposer, one corpus, a landscape whose shape was chosen. The number that would move this row again is **how often a real agent's tunables have that shape** — which `--probe` answers in zero calls |
| 7 | 5/10 | N7 (ADR 0192): the pilot on **`peptideindex`** — a production repo with no agentic surface at all and a `master` default branch. All three pre-registered clauses fired. **(a) Real runs entered the corpus through `harvest`**: five objectives drawn from this repo's own `price-index.json`, its vendor docs, its `price_outlier_cap_views.sql` and the freshness fix that is its HEAD commit, answered live by `claude_code` (`claude-opus-5[1m]`, containment recorded on all five: `no_mcp, no_project_context, no_tools, single_turn, system_role`), then admitted by `aef loop harvest` — corpus `{"harvest": 5}`, **bootstrap 0, record 0**, against ADR 0163's `0 promoted, 5 REJECTED`. Redaction on, counts quoted (`redactions 0`, `refused 0/0`) with the control run: 5 of 5 planted credential shapes caught, 2 of 3 secret-shaped working-memory keys dropped, and the residual named (vendor domains and prices deliberately unmatched; the repo's Azure subscription UUID unmatched, the same gap as marlin). **(b) A candidate proposed FROM that evidence** — ADR 0164's unclaimed clause, met for the first time in this program: both `grounded_in` ids are check-failure records whose `run_id`s are harvest-sourced corpus scenarios, followed link by link in `21-grounding-chain.txt`. **(c) The gates reached a verdict live**: `live_model_calls: true`, 35 scenario executions, G0/G1/G2/G4/G5 pass and **G3 fail — 1 previously-passing scenario now scores below 0.5**, drift 0.057/0.500, EXIT 1. Owner checks are three rules written once with values computed by arithmetic, failing 2 of 5 on a real disagreement (the reviewer preferred its own clock to the date the objective stated, and said so in the answer). **Two defects found and reported, not fixed**: F-N7-1 (HIGH) — the determinism re-check hard-codes an empty memory store while ADR 0118's `retrieve` node puts what it found into the user turn, so **only the first production run against a given `--memory` is ever harvestable**; isolated to one variable, 1-of-5 → 5-of-5. F-N7-2 (MEDIUM) — `digest`'s `Scenarios added to the corpus` is a constant 0, so ADR 0190's "the ingestion path is broken" warning fired on a day harvest had admitted all five. Qualifications inside the +2: four of the five runs were re-recorded without `--memory` to route around F-N7-1, and the two grounding records share one check signature on one failure family. **The last five points stay unclaimed, and the reason is not a missing measurement: peptideindex is the owner's own repo, not a third party.** **Stated against this row's own interest:** F-N7-4, found at commit time — ADR 0197's new `uuid` pattern refuses a run for carrying its own id, so on the trunk this corpus harvests 0 of 5 and the evidence behind this +2 cannot be rebuilt until that collision is settled. |

| 6 | 8/10 | N8 (ADR 0198): J0b's two dimension-6 deductions, answered separately. **The listing exists**: `aef loop lineage list` prints every member with its parent, score, gate verdict, children count and whether the sampler may build on it — `sampleable` is `_parent_weight` itself plus `_resume_lineage`'s ref check, not a second opinion that could drift from the sampler, and the NEGATIVE is printed (`no kept member descends from a rejected one`) because this increment's own result is one. It builds no config and loads no graph, so it works after a run that could not start. **The stepping stone did not pay, and this time the statistic had power.** ADR 0160's A/B kept nothing in either arm, so distinct-kept-trees was 0 on both sides and could not discriminate; its raw data was re-read first (15 turns, every one `score 0.5 incumbent 0.5` with G2 refusing a traded scenario) and the rig moved to `agents/demo`, where the staircase was **measured** — 3/3 → 0.5000, 4/3 → 0.5000, 3/4 → 0.5000, **4/4 → 0.6667**, 5/5 → 0.8333 — so a one-constant candidate is neutral, G3 rejects it, and it is a stepping stone by construction. Four arms; **16 live calls** for the LLM arms (the demo corpus replays offline, so a turn costs the proposer's call and nothing else) and **0** for the rule-based ones. Both LLM arms kept exactly one candidate, on turn 1, from the root; the other fourteen scored **exactly** the incumbent 0.8182. **`stepping_stone_keeps` = 0 in every arm, with the denominator stated**: greedy proposed from a rejection 0 times in 8 turns, sampling **5 times**, three generations deep, and all five descendants were rejected too. *The archive is owner-visible; nothing has yet been shown to grow from a rejected member.* The rule-based arms show the ceiling mechanically — `cycle` takes `proposals[0]` and `find_constants` returns `RETRY_BUDGET` first, so 4/4 is unreachable under any parent policy. **A defect found by running**: the lineage fold took the last record wholesale and a resumed root's closing record carries `parent_ref=None`, so one invocation erased the parentage of the branch the loop had kept (a4d0bfe4 ← c01e9b06 at 0.818); `archive.fold_lineage` is now the one fold and repairs files already on disk. 4 mutations, 4 caught; 18 tests; registered in `make measure` as `j2b-archive`, 6 rows, zero live calls. Remaining: one agent, one corpus, 8 turns per arm, and a proposer that plateaued after one idea in both arms — the two points left need a kept descendant of a rejection, and a rig that can produce one needs a proposer that can take the second step |
| 8 | 5/5 | N10 (ADR 0199): J0b's deduction — *"the Codex path is never exercised against the real CLI — its only live test is one of the three skips"* — was two problems. ADR 0131 did run it; no CI job ever set the opt-in, so on every push the path ran **nowhere**. Run here on `codex-cli 0.153.2`, unmodified: `1 passed in 4.87s`. And the half that decides an adopter's containment story: every earlier assertion that `PromptAgentNode` records `user_turn_persona` was against a `_Declaring` fake with a hard-coded frozenset, which proves the node reacts to a declaration and not that the real provider makes one. A real graph, executor and `CodexProvider` over the real binary now assert it end to end — `user_turn_persona` and `read_only_fs` present, **`no_tools` and `single_turn` absent** (the two `claude_code` earns and this argv does not send), the `prompt_agent.persona_in_user_turn` warning, and `state.errors == []` because it is the provider's property and never the run's error. `ci.yml`'s `live-harness` job runs the live tests wherever a CLI and a credential exist — `vars.AEF_LIVE_RUNNER` aims it, detection asks two questions (CLI on PATH; a credential as a repository secret **or** the CLI's own auth file), the guard is on the TARGET LIST so codex-without-claude still runs codex, and the empty case prints why. Exercised under four runner shapes; the test extracts the shell FROM the workflow, and its own first draft skipped without a real `codex` — reproducing the deduction inside the fix, caught by M7 not firing. 3 mutations, 3 caught. Remaining: one model (`gpt-5.5`), one prompt shape, and a credential check that knows two forms and deliberately does not claim to see a macOS Keychain login |
| 5 | 10/10 | N5 (ADR 0196): `make measure` exists, and the deduction it closes was named word for word — *"no re-runner that regenerates the tables in CI"*. `docs/research/measure.py` drives eleven runners through one shared `--verify` mode (committed JSON/JSONL, or a cassette at `on_miss="fail"`; **zero live calls in every mode**), re-derives 53 pinned rows across 13 measurements, and diffs each against **the ADR paragraph that publishes it** rather than a constant — so perturbing a number in an ADR fails exactly like perturbing the data, which `test_a_perturbed_published_number_is_caught` runs against a symlinked shadow tree (`0.9176` → `0.9999` ⇒ `DRIFT (b) raw records, mean: ADR says '0.9999', re-derived '0.9176'`). CI runs `make measure-ci`, which fails when the drift set **changes** in either direction — a new drift, or a recorded drift that healed and left a stale note. **The first run found four things nothing else could see**: ADR 0155's runner had raised `FileNotFoundError` since its raw JSON was flattened (fixed; the table then reproduces exactly); ADR 0175's step-0 runner does not import at all (`write_check_failure_record`, removed by ADR 0180); ADR 0159's oracle-B block is stale (18/18 published, 10/18 re-derived — ADR 0186 disclosed this for v2 and missed v1); and `report-tally.txt` is a pre-`_tally`-correction artefact. And it fails, by name and with both numbers, on **ADR 0184's seed** — the measurement ADR 0191 withdrew a `+2` for, a night late, by hand. `make measure` is red on this tree on purpose: an explained drift is still a number nobody should cite. Remaining, stated: 53 rows is a sample of what those ADRs publish, not all of it, and CI's fast subset excludes the two cassette-replaying seeds — so the 0184 row is checked by `make measure`, not by every push |
| 2 | 14/20 | N2 (ADR 0193): the four arms re-run on the corpus that exists now, which is what ADR 0191 withdrew S1c's +2 for wanting. **The seed step is re-run first and its diff stated**: same script, same single entry, same signature — 7 distinct train runs instead of 3, confidence 0.6364 instead of 0.4286, lesson text `observed 37 words, 233 chars`, and 8 owner-check negatives in validation instead of 4 (ADR 0186's re-recording), so `NEGATIVES` is now DERIVED from each scenario's own trace and asserted rather than carried forward. **The knob's own A/B, which had never run, is answered at zero cost**: arm (c) at the shipped `knowledge_boost=0.0` sends arm (b)'s prompts BYTE FOR BYTE on all 17 (one sha256, not a similar one) — the entry ranks 20–23 of 28 and never reaches the five bullets `render_retrieved_context` renders — so (b)'s three live repeats ARE (c)@0.0's, and (c) ran at **8.0**, the smallest value on a 1.0 grid that keeps the lesson in the model's prompt in all 17 scenarios under the WORST-case dry trajectory. 119 live calls on `claude-opus-5[1m]`, equal repeats this time: (a) 0.9529 ×1 · (b) **0.9373** ×3, spread 0.0118 · (c) **0.9843** ×3, spread 0.0353. Pre-registered rule, evaluated by script: (c) − (b) = **+0.0470** against a 0.0353 bar and **+0.1167** on the eight negatives against 0.0250 — first branch fires, 1.33× rather than S1c's 1.5×. 0 leaked output windows in 119 live lesson blocks. **(b) − (a) = −0.0156**: raw retrieval remains unshown to help, fourth measurement, third sign change. Defect 2 of ADR 0184 fixed: `knowledge_boost`, `staleness_half_life` and `knowledge_min_occurrences` are now `aef.yaml` fields, unset meaning the retriever's own measured default (one copy, asserted); 4 mutations, 4 caught. **`knowledge_boost` stays 0.0** — 8.0 was chosen to guarantee engagement on a store holding exactly ONE entry, and ADR 0110 measured a raised boost displacing a precisely-relevant record when there are many. Remaining: one corpus, one agent, one failure family (a word cap); the delta clears this rig's bar and not S1b's 0.0857; and the layer is INERT at the shipped default on this corpus |
| 2 | 12/20 | Final seam hunt (ADR 0191): S1c's +2 (ADR 0184) is WITHDRAWN, not disproved — its seed step (1 entry from 3 runs, lesson text ending "208 chars") no longer reproduces on `main` after ADR 0186 re-recorded the corpus (7 runs, different text). A measurement that cannot be re-run on the current tree is asserted, by the rubric's own first rule. The runner and data stand as history; the next dim-2 increment is the same four arms on the one-model corpus |
| 2 | 14/20 | S1c (ADR 0184): ADR 0175 measured (c) ≤ (b) and named the two defects that made the number unreadable — a lesson quoting the model's own over-long summary back at it inside a lesson about being shorter (ADR 0162 measured that shape making at-cap runs LONGER), and a producer wired into `bootstrap` alone, so arm (c) wrote `"failures": {}` and the entry walked from rank 0 to rank 25–39 while the two late negatives never saw it. ADR 0180 removed both; this re-runs the arms. Seed identical but for the quotation (1 entry, 3 distinct runs); **0 twelve-character windows of any run's output** in the 23 records or in any of the 85 live lesson blocks — the first version of that detector fired 28 times on the arm with no retrieve node at all (`' for the first time '`) and was corrected to scan only the lesson block. Producer on the scored split: entry **rank 0 at scenario 1 and rank 0 at scenario 17**, in the model's prompt **15/17** (S1b: 10/17), `runs_since_last_seen` resetting on a reproduced failure instead of climbing to 17. Six live arm-repeats on `claude-opus-5[1m]`, 102 calls, arm (d) cut on ADR 0175's proof that it sends (c)'s prompts byte for byte: (a) 0.9176 · (b) **0.9059** over three repeats, byte-identical prompts, same-prompt spread **0.0353** with 9/17 scenarios churning · (c) at boost 3.0 **0.9588** over two. Pre-registered rule, evaluated by script: (c) − (b) = **+0.0529** on the mean against a 0.0353 bar, **+0.0917** on the four owner-check negatives — first branch fires. The third (b) repeat was added after seeing the first two and moved both numbers against the claim (bar 0.0000 → 0.0353, delta +0.0647 → +0.0529); it survived. Remaining: the delta does NOT clear S1b's 0.0857 same-prompt band measured on a 7-scenario subset (stated, with why a 17-scenario mean's spread is the right instrument); (c) has two repeats to (b)'s three; the excerpt fix and the staleness fix were removed together so neither is attributed; one corpus, one agent, one failure family. **`knowledge_boost` stays 0.0** — its own A/B ((c)@0.0 vs (c)@3.0) has never been run, and `ContextConfig` cannot express it |
| 1 | 13/20 | J0b (ADR 0188): the shipped nightly cycle omits `--graph-id`; against this repo's own two-graph corpus it exits 1, which the workflow reads as "the system working". Reproduced by the reviewer. Fixed (exit 3, flag pinned) — the row stays until a blind review sees it |
| 4 | 15/15 | N3 + N4 (ADRs 0194, 0195): **both** halves of J0b's deduction, which it stated as two facts. (a) *"adversarial rounds exist as a document I was not allowed to read, not as an executable red-team suite"* — `tests/adversarial/`, **18 modules, one per attack**, each of which BUILDS the hostile input, RUNS it through the real control, and then MUTATES the control away and asserts the attack LANDS. That third step is the claim: a test passing because the exploit was never viable is indistinguishable from one passing because the defence worked, so every module removes its own defence and watches the exploit succeed. The seven the trust case named plus **eleven with a known-good exploit** — a `bless` archiving **16 bytes of `../real/graph.py`** as the baseline (0149), bytecode taking a one-line change **over** the 0.500 drift budget (0142), `../escape` writing **above** the archive root (0168), the reward hack the `MUST_FAIL` tripwire catches (0060/0061), a placebo drawn from the treatment's own lesson (0170), a candidate killing the provider on the scenarios it loses (0185), a provider property proposed into the persona verbatim (0179), an adopter's quoted marker pair (0172), a candidate granting itself the harness login (0181). **18 attacks, 18 mutations, 18 caught**, plus three removed by editing the shipped file with sha256-verified restores (one of which failed 1 of 2 — `_segments` refuses `..` before any zone question, defence in depth measured rather than assumed). `pytest -m adversarial`; the marker is applied by the directory's conftest so a new module cannot forget it. **The grep test that stood for this record is REPLACED** by one asserting bidirectionally that every attack the document names has a module and every module has an attack in the document. (b) *"`aef loop digest` printed `Halt channel configured: NO`"* — reproduced (`external file exists: False`; `HaltNotifier.webhook_url` existed and **nothing in `aef/` ever called `notify`**), then closed with `halt_channel:`, an argv template of the same shape as `impl: command` — no dependency, no guess about how the owner is paged — invoked on halt with the reason and the ledger's last entry, **read from the base ref** so a candidate cannot silence its own halt (A18, with its mutation), bounded by a timeout, recorded in the HALTED entry, and reported by the digest along with the last N notifications. Proven both ways with `/bin/sh -c 'cat >> file'`, zero network. **Two attacks are recorded as getting through on purpose** and two limits are stated where the owner reads them (trust case §2.0): a mutation proves a control load-bearing against *the exploit that module builds*, and the round is still written by the party that wrote the defences. Remaining, and named in ADR 0195: the channel has never fired on a real regression |
| 4 | 13/15 | J0b (ADR 0188): adversarial rounds are a document, not an executable red-team suite; `aef loop digest` prints `Halt channel configured: NO`. Containment confirmed by running 64 container tests against a real daemon |
| 5 | 9/10 | J0b (ADR 0188): the reviewer found every measurement's runner and raw data one directory from the claim without reading an ADR. Remaining: no `make measure` regenerating the tables in CI |
| 6 | 7/10 | J0b (ADR 0188): ran `aef loop run` — a stepping stone archived into a persistent hash-chained lineage store, sampled as a parent. Remaining: no measured run where a stepping stone produced a better descendant; no owner-facing lineage listing |
| 7 | 3/10 | J0b (ADR 0188): 0 scenarios with `source=harvest`; every summary passage is author-written and answered live — live model calls on synthetic inputs are not live traffic. M6's pilot harvests real runs; S7 may re-earn on it |
| 3 | 8/10 | S6 / J4 (ADR 0162): the self-preference control — J0's third and last named gap on this row — now exists, because the thing it needs now exists. Nothing here had a judge ranking MODEL OUTPUTS against each other (`LLMJudge` grades one state, so it cannot prefer its own writing), so `PairwiseRanker` was built with the control ON by default. Rig: 11 Opus-recorded validation scenarios, two summaries each of the same passage under a byte-identical recorded request — the cassette's (`claude-opus-5[1m]`) and a live one (`claude-haiku-4-5-20251001`) — ranked by BOTH those models and by a **disinterested third** (`claude-sonnet-5`, wrote neither), position-swapped, blind to authorship (the prompt carries neither the label nor the model, asserted against the issued requests). 77 committed judgments. **Self-preference measured PRESENT and attributed**: prefers-the-Opus-text 0.714 / 0.444 / 0.455, so opus +0.260 and haiku +0.010 against the disinterested reference; the disinterested judge also never flipped with position (0/11 vs 4/11) and agreed with the owner's checks 5/5 vs 2/3. Mitigation implemented and re-measured, not asserted: `allow_self_ranking=False` REFUSES (ADR 0105's reasoning, not a warning), guarded twice because `model: ""` hides the name until the call answers, and suffix-normalised so `claude-opus-5[1m]` cannot slip past. Shipped prompt proved byte-identical to the measured one (sha256 pinned). 5 mutations, 5 caught; +15 tests. Remaining: n=11 with a discriminating subset of 5 and one task family; self-preference and position-instability are confounded in this design; and nothing in `run_loop` calls the ranker yet — the control exists where candidates are compared, not yet where they are chosen |
| 3 | 7/10 | S3b (ADR 0171): ADR 0159 could not grade a judge because the corpus had no true negatives — its only three were case-sensitive `contains` defects, and correcting them left it 20/20 pass, AUC 0.322. Corpus rebuilt: 3 case defects corrected, 57 more of the same shape found latent and corrected, 20 word caps moved to `max_words`+`min_words`, and 19 scenarios recorded live on `claude-opus-5[1m]` against inputs chosen to be handled badly (negation, superseded figure, similar names, dropped unit, conditionality, direction-of-change, caps 12–38). **7 fail an owner check, 4 of them in validation.** The A/B re-run on the enriched validation split, same script, same model, 34 calls: rule-based 4/17 (constant-fail), **LLM 16/17 against a 13/17 constant baseline, AUC 1.000** (every owner-fail state 0.275–0.635, every owner-pass state 0.850–0.910), position delta max 0.17. All three pre-registered conditions fired. Remaining: all 7 negatives are word-cap overruns, so AUC 1.000 is perfect separation of ONE failure family with n=4; no self-preference control (J0's third gap, still open) |
| 6 | 6/10 | S4 / J2 (ADR 0160): J0's four clauses, each on an artifact — `aef loop run --sample-parents/--seed/--no-lineage` tested through parser AND handler; `<state>/lineage/<graph-id>.jsonl`, digest per record, a second invocation resumes it and proposes from a parent the first kept; REJECTED candidates are members with their verdict, sampleable iff G3 scored them and weighing zero if a cheap gate stopped them first; duplicate detection and the rejected-tree stop run over the persisted set. 7 mutations, 7 caught. **The live A/B did NOT earn the second point**: 8 turns per arm on the summary corpus, Opus, 18 live calls — distinct kept trees **0 in both arms** because neither arm kept anything, so the specified statistic could not discriminate. Sampling proposed from **5 distinct parents** against greedy's 1, two of them rejected stepping stones, and every G5 drift rejection (0.511/0.515/0.547 vs the 0.500 budget, budget NOT raised) was in that arm — an archive that keeps stepping stones spends a cumulative drift budget faster than a ladder. The archive is now real and persistent; it has not yet been shown to buy anything. `sample_parents` stays off and is NOT deleted, on evidence that differs from ADR 0121's |
| 4 | 15/15 | S5 / J3 (ADR 0161): the 14 read "one point off: shadow containment is opt-in". Reproduced by running — on a box WITH docker and the worker image, `ShadowRunner(incumbent, candidate)` refused exactly as it does with neither, and the one-line way past it was `uncontained=True`; the escaping node's write landed on the host. `shadow_for` now resolves a runtime, verifies isolation both directions and returns a contained runner: same node, same box, no arguments, host marker **False**. `shadow.containment: auto` is the default and **refuses rather than falling back** (an automatic fallback would be weaker than ADR 0105's refusal); `fallback`/`off` are owner statements in `aef.yaml`, announced on stderr and written to the ledger as `EventKind.CONTAINMENT` with `security_event: True`, which `build_digest` counts. J0's deduction answered in both halves: the renamed `test_the_in_process_bypass_exists_only_when_an_owner_opts_out_and_is_logged` keeps proving the bypass real under opt-out and adds that the default cannot reach it, and the adversarial-round record's two BROKE-IT attacks are named at the tests that re-execute them. 5 mutations, 5 caught; +15 tests |
| 1 | 15/20 | J0 (ADR 0151): the repo's own scheduled `aef loop cycle` passes no `--memory` and exits 0 every night with `no memory store configured … no candidate`; the adopter's rendered workflow has no cycle step. Verified. "Automatically" is unmet |
| 2 | 12/20 | J0 (ADR 0151): `make_retrieve_node` writes `retrieved_context` and no prompt reads it — `draft_node` builds from `working_memory` alone; the knowledge A/B measures a retrieval-coverage proxy, never a task score. Verified. I12 as designed would have measured four identical arms |
| 3 | 6/10 | J0 (ADR 0151): the 3/18 vs 9/18 judge A/B exists as a docstring citing an ADR — no test, script or committed data; no judge scored against a real model in-repo; no self-preference control. Verified |
| 5 | 8/10 | J0 (ADR 0151): archive sampling, `reflection.impl: llm` and `proposer: llm` are "off by measurement" with the measurement in prose only — no runnable script, no committed data; nothing measures end-to-end benefit. Verified |
| 6 | 5/10 | J0 (ADR 0151): lineage archive is in-memory per `run_loop` call, no CLI flag (`grep sample_parents aef/cli/` empty), only kept candidates enter it, nothing persists across invocations. Verified |
| 7 | 4/10 | J0 (ADR 0151): no live signal has ever entered harvest; the cron's `--runs` is populated by no step; corpus is self-generated. Verified — one point below our own row |
| 8 | 4/5 | J0 (ADR 0151): the Codex path's live test is a skip from code alone; six manual obligations remain. J0 scored 4.5; rows are integers; the lower integer |
| 8 | 5/5 | I15 (ADR 0131): the Codex CLI was 18 versions behind and could not parse its own server's catalogue; upgraded, `CodexProvider` answered UNMODIFIED on the first live attempt (argv, `--output-last-message` reply, `turn.completed` usage in 19,975 / out 26). `tests/providers/test_codex_live.py` keeps it runnable, opt-in. Two backends measured, none assumed |
| 1 | 19/20 | I11 (ADR 0123): a content task a model can fail — `agents/summary`, 20 recorded scenarios with owner checks (12/6/2), `CassetteProvider` replays every pinned model call with no credential, miss FAILS by default; cassette score train 0.9792 / validation 0.9167, repeat spread 0; three real content failures (0.75); planted prompt regression → 0.0000 under the default with no live call. Remaining: the live noise floor did not get measured (three attempts past a ten-minute wall); no turn kept/reverted on this suite yet (I12) |
| 1 (I10) | 18/20 | I10 (ADR 0122): `run_loop` gave every turn one workdir and G1 refuses a non-empty one, so no real loop had ever gated a second candidate behaviourally — fixed per turn, asserted in the acceptance test (M48); on `agents/demo` the loop now keeps a coherent two-constant change the rule-based proposer could not make. Remaining: a corpus of real tasks (I11) |
| 3 | 8/10 | I11 (ADR 0123): I3's judge A/B re-run on the 18 summary states (36 calls): rule 3/18, LLM 9/18 agreement with the checks, disagree with each other on 10/18, position delta up to 0.2 — the corpus where the judges disagree now exists, and it shows neither judge's evidence contains the answer. Remaining: a judge that reads `working_memory`; self-preference control |
| 6 | 8/10 | I10 (ADR 0122): `LLMProposer` — model writes prose + whole file, code computes citations (train only, via `_check_citations`) and validates against G0's imported allowlist/line budget and G4's owner-only fields, falls back to rule-based with the reason; `LoopConfig.proposer`, `--proposer llm`. Live (26 calls): 4/4 distinct candidates per run, 16/16 validated, demo kept 1 vs 0, flaky 1 = 1; kept diversity still 1 everywhere; one G5 halt → off by default. Remaining: kept diversity > 1 needs an agent with more than one repairable failure |
| 6 | 6/10 | I6 (ADR 0121): G3 scores reach the driver; archive of kept members with lineage; `sample_parents` by sigmoid(score)/(1+children), seeded; kept branch = best member; duplicates skipped. Measured on the real cycle: zero diversity gain (deterministic proposer) → off by default. Remaining: a proposer with a repertoire |
| 5 | 10/10 | I8 (ADR 0120): `tests/test_prompt_surface.py` pins required blocks, forbidden text, kept verification lines; every detector proved against a planted fault; found a broken regex and a missing reproduce-first line in the shipped CLAUDE.md |
| 7 | 5/10 | I5 (ADR 0119): harvest redacts the input, re-executes, admits only if behaviour is unchanged, scans the output; on by default; 4 mutations detected. Remaining: has never run against a real tenant — owner decision |
| 2 | 17/20 | I9 (ADR 0118): `make_retrieve_node` — the retriever's first production caller, budget enforced in a real run; `retrieved_signatures` on reflection; helpful/harmful tallies per entry, agent-scoped, surfaced in metadata + drafts; A1 closed. Remaining: ranking on the tally (no rig where harmful ≠ live); text curation |
| 2 (I7) | 15/20 | I7 (ADR 0117): `aef loop skills` drafts one SKILL.md per well-evidenced entry under a proposals dir; refuses harness dirs, never overwrites, every field computed; 11 tests, 3 mutations detected. Remaining: no measurement that an adopted skill helps; no retrieved→outcome signal |
| 2 (I4) | 12/20 | I4 (ADR 0116): `runs_since_last_seen` recomputed per consolidation; retriever demotes stale entries by half-life, deletes nothing; live-lesson coverage 2→3 of 3 at budget 400, 6/6 at 2000; default 5 by sweep. Remaining: no retrieved→outcome signal (no retrieval node), no curation of lesson text, no skill layer |
| 3 | 7/10 | I3 (ADR 0115): `LLMCritic`/`LLMJudge` on `impl: claude_code`; citations, arithmetic, omission=0, clamping and the position-swap control all in code; live A/B 11/11 agreement, delta 0.0, ~10 s/judgment; off by default (parity on this corpus). Remaining: no corpus where the judges disagree; no self-preference control (nothing compares model outputs yet) |
| 1 | 17/20 | I2 (ADR 0114): `run_loop` keeps on a local branch, never main; proposes from the kept state so improvements stack; bounded by turns, budget, halt, no-candidate, repeated-rejected-tree. Real cycle: kept 1, reverted 1, stopped on its own evidence. Remaining: corpus is 11 demo scenarios; proposer knows numeric constants + one structural transform |
| 1 (I1) | 13/20 | I1 (ADR 0113): owner-declared `checks` + `budget_ms` on scenarios, one `score_scenario()` in both scoring paths, `aef loop score` with CI and repeat-spread. Planted clean-run-wrong-content fault moved train 0.5000→0.1667. Still missing: keep/revert on the metric (I2); corpus is 11 demo scenarios, 5 checked |

Other dimensions unchanged from baseline.

## Baseline — 2026-09-03 — **51 / 100**

*Corrected from 50 (ADR 0127): the rows below always summed to 51. Every
increment's delta was taken from the wrong base, which is how one point
travelled through nine increments in the document whose first rule is that
a self-graded number is the failure the trust case warns about.*

| # | Score | Evidence for the number |
|---|---|---|
| 1 | 8/20 | G3 improvement gate vs a null cohort exists (`aef/harness/gates/g3_improvement.py`, ADR 0051) and train/val/holdout separation is enforced in `proposer.py`. But the corpus is synthetic scenarios, there is no task benchmark an agent is scored on, and nothing is kept automatically — Tier-1 auto-merge is off on the trust case's recommendation |
| 2 | 8/20 | Consolidated knowledge exists and is measured (ADR 0110, I4 A/B: coverage 6 vs 1 under budget). Retrieval is lexical overlap. No playbook curation, no skill layer (`aef/evolution/` hard-disabled, constraint #7), no harness self-modification by design |
| 3 | 3/10 | `RuleBasedCritic`/`RuleBasedJudge` only (ADR 0046). `Critic`/`Judge` LLM interfaces raise `NotImplementedError`. Runnable now via `impl: claude_code` (ADR 0112) but not implemented |
| 4 | 14/15 | Checkpoint/replay (ADR 0023/0031), deny-by-default `PolicyEngine`, HITL (ADR 0032), G0–G5, container (ADR 0105), canary with keyed tenant hash (ADR 0106), six adversarial rounds, `docs/trust/promotion-trust-case.md`. One point off: shadow containment is opt-in |
| 5 | 9/10 | I4 A/B, `knowledge_boost=0.0` swept and left off, LLM summariser measured and left off, ADR per decision, planted-fault detector checks. One off: no prompt eval exists, so prompt changes are unmeasured |
| 6 | 3/10 | `ledger.py` records candidates; lineage is linear; no archive sampling, no diversity pressure |
| 7 | 2/10 | Trust case §: criteria 1 and 6 "have never run against the live traffic and real tenants their text names". Corpus built from `GraphExecutor` runs of shipped examples |
| 8 | 4/5 | `aef adopt` scaffold, cross-tool entry files (ADR 0040), harness provider with no key (ADR 0112). One off: Codex path unconfirmed |

**Reading the number** (written at the baseline, when the corrected total
was 51). As a governed harness this is ~90. As a *learner* it is
~35: the machinery to judge a change is stronger than the machinery to
produce one, and nothing it learns yet moves a task metric. The field's
strongest loops (DGM, autoresearch) are the mirror image — strong learning,
no governance. The gap to 90 is dimensions 1, 2, 3, 6, 7, and every one of
them needs a task metric first, because without one the rest cannot be
measured and would be scored on vibes.
