# Upgrade loop — report, 2026-09-04 → 2026-09-05

Input: `UPGRADE_LOOP.md`. Two goals, stated so each could fail on its own:
**M** — copying this repo into an *existing* agentic repo gives it a
self-learning loop over its prompt-file agents, gated, under Claude, Codex
or Grok; **S** — above 95 on the rubric, or an honest statement of the
number the measurements support.

**Outcome in one line each.** M: true for three of the owner's real repos
(a marlin clone end to end, keystone and datamining offline), with one
opt-in that makes a prompt candidate acceptable on live evidence for the
first time; not yet true for a third party. S: **69 / 100** by a second blind review — one
increment measured after it was withdrawn when its seed stopped reproducing — the score barely moved and the
system did, because most of the night went into making claims true rather
than making new ones.

Every number below is on `main`; every command was run; every falsification
that fired is named. Pending increments at the time of writing are marked.

---

## 1. What was measured before the loop was written

Read-only survey of `/Users/raptor/*` (git repos with an agentic surface):

| repo | `.claude/agents` | skills | CLAUDE.md | AGENTS.md | `.codex` | py | **SDK call sites** |
|---|---|---|---|---|---|---|---|
| datamining | 13 | 6 | Y | Y | – | 362 | **0** |
| marlin | 8 | 5 | – | Y | Y | 39 | **0** |
| keystone | 7 | 4 | – | Y | Y | 67 | **0** |
| dataco | 5 | 3 | Y | – | – | 2000+ | **0** |
| databento_ingestion | 3 | 3 | Y | – | – | 82 | **0** |
| Promethesus | 0 | 18 | Y | Y | – | 43 | **0** |
| Raptor (trading — off limits) | 26 | 18 | Y | Y | Y | 2000+ | 7 |

**Every eligible repo has zero SDK call sites.** Their agents are prompt
files the harness runs. `aef migrate` found nothing in any of them; `aef
adopt` on a marlin clone said `detected framework: none` and skipped the
`AGENTS.md` its agents read. That sentence in `CLAUDE.md` — "there is no
call site to convert and this runtime has nothing to attach to" — was the
gap, and it is retired (ADR 0158).

Harnesses on the box: `claude`, `codex`, `grok` 1.0.5 (interface
near-identical to Claude's); Copilot's CLI not installed.

## 2. Thread M — the upgrade, increment by increment

Each row names the ADR and the command that proves it on the marlin clone.

| inc | what | proof (run, not read) |
|---|---|---|
| M1 (0152) | a prompt-file agent is a graph: `migrate` discovers `.claude/agents/**/*.md` recursively, one graph per persona, `--agent-root .claude/agents` opt-in widens Zone A | `found 8 prompt agent(s)` → 8 graphs; `aef run … --config` returned persona-grounded text; bootstrap recorded 2 scenarios with the persona verbatim as system message |
| M2 (0153) + H1 (0172) | adopt into a populated repo: `prompt_files (8 agents, 5 skills, AGENTS.md, .codex)`; append inside **signed** markers; bytes in/bytes out, the file's own line ending, an executable never-destroy check | `AGENTS.md` original bytes at offset 0; `git diff --numstat` insertions only; second run byte-identical on 260 files |
| M3 (0154) + G2 (0169) | Grok provider; generic `CommandProvider` (Copilot is config the owner writes); providers **declare** isolation from their own argv, the node records it per run | `grok` answered `OK` through the provider; `CommandProvider` reproduced it from config alone; **`grok --tools ""` suppresses nothing (read a canary file); `claude --tools ""` does** (invented an `ls` listing, `num_turns` 1) |
| M4 (0157) | `RuleBasedPromptProposer` — ACE's method: the highest-recurrence lesson as one bullet under `## Lessons (aef)` | 4 lines, drift 0.007/0.500, the failing scenario 0.0 → 1.0 paired live, twice — with the caveat that the bullet then carried the check's literal |
| M4b (0174) + K2 (0180) | a failed **owner check** becomes failure memory (signature `check:<path>:<op>` so it recurs); no output excerpt in the lesson; a third tally outcome | memory → 1 entry from 2 runs → candidate → gates; 33 twelve-char windows of model output in a record → 0 |
| M4c (0170) | the gates can judge prose: G2 no longer dies on the workspace G1 built; G3 has a placebo cohort with a leak refusal | both outcomes constructed offline and asserted against the ledger |
| M5 (0158) | the acceptance test on a real prompt-file repo, offline half in CI | adopt → migrate → bootstrap → bless → doctor → cycle; a candidate editing one `.md`; every gate ran; verdict in the ledger; ADR 0139's shape gone |
| K1 (0181) | **live model calls inside the gates are an explicit opt-in**, `gates.live_model_calls: false` by default, read from the base ref; `USER` is the one variable the login needs | a prompt candidate reached **G3 live** on the clone: 12 misses served in the sandbox, drift 0.007, rejected on p95 — a judgement of the prompt, not of a worker that could not log in |
| M7 (0183) | the documents say what is now true | 14 false sentences retired; the kit's prompt-file sequence **could not propose on any repo** (three missing flags) — fixed; 55 rendered commands parse |
| M6 (0163) | **the pilot on the clone** — real objectives, real harness, harvest with redaction, one cycle | five real objectives from marlin's own `AGENTS.md` answered live (453–949 words), containment recorded, redaction scan 0/0/0 with a planted-fault control 5/5; **harvest rejected all five** — `aef run --record-runs` stores no model calls, and the re-execution has no provider so the containment fact differs and the byte-for-byte comparison rejects: no run of any generated prompt-agent graph had ever been harvestable. **Fixed (ADR 0190):** `aef run --record-runs` goes through the one recording cassette and stores the provider's declared isolation; the re-check replays it through a provider whose `complete` raises. M6's five real runs, re-recorded through the fixed path: **5 of 5 promoted**, at zero live cost — where the old recorder's files went 5 of 5 rejected. Bootstrap populated the corpus instead; one live cycle proposed a 4-line bullet with no excerpt and G3 rejected it as a regression the lesson caused — G2 passed while G3 failed, ADR 0162's two signals separating in the wild. 41 calls |
| M8 (0187) | the sequence on keystone and datamining clones, offline | 7/7 and 13/13 personas; every step's exit checked; a candidate proposed and gated on both. **F-M8-1:** datamining's default branch is not `main` → `cycle` exited 0 having done nothing, blaming a persona that exists (L1 fixing). Three repos, one owner — what that cannot detect is a convention all three share |

**What an adopter with an existing prompt-file repo gets today**, in the
words `FIRST_DAY.md` now carries: `adopt` appends the contract into the
entry file their agents already read without touching a byte outside the
block; `migrate` registers every persona as a gated graph; `bootstrap
--memory --config` records real runs and writes failure memory for a run
that answered wrongly; `bless`, `doctor` (all six obligations, the
persona path resolving to its generated graph); `cycle --proposer
rule_based_prompt --agent-root .claude/agents … --cassette-miss live` with
`gates.live_model_calls: true` — and every gate run records that the
login was spent. Exit codes 0/1/2/3 each mean one thing and the nightly
workflow fails the job on 2 and 3 with the right remedy.

**What it does not get:** a third party's data; Copilot without installing
its CLI; a lesson that has been shown to *help* (§4).

## 3. The acceptance test's output

Offline half, synthetic marlin-shaped repo, `impl: command` with `/bin/echo`
(from ADR 0158):

```
adopt     prompt_files (3 agents, 1 skill, AGENTS.md, .codex); diff 41/0 and 5/0
migrate   3 graphs incl. the nested persona; all printed run commands parse
bootstrap 2 failed an owner check; two check-derived failure records, one signature
bless     entry.json: agent_root .claude/agents; six digests under it
== proposed  {"paths":[".claude/agents/accela-agent.md"]}
== gated     7 corpus pass(es) (21 executions): candidate + incumbent + 5 controls
  G0 pass  G1 pass  G4 pass  G5 pass  drift 0.012/0.500
  G2 fail  3 previously-passing scenario(s) no longer pass   (cassette-miss artifact)
== rejected
```

Live half with the opt-in (from ADR 0181), on the clone:

```
gated: reject — G3 rejected it: candidate does not beat the p95 of the random control cohort
live_model_calls: True   drift 0.007/0.500   14 executions, 12 live misses served
```

## 4. Thread S — every measurement, every falsification

| inc | claim | result | delta |
|---|---|---|---|
| S0 / J0 (0151) | blind re-score | **68.5 vs our 86** — every deduction verified | 86 → 68 |
| S5 / J3 (0161) | containment on by default | it already was, by refusal; `auto` now contains when a runtime exists, refuses otherwise, opt-out ledgered | d4 +1 |
| S3 / I14 (0159) | judge A/B as an artifact | LLM 15/18 vs rule 3/18 — **and the corpus is 15/18 pass, AUC 0.322: the corpus could not grade a judge** | 0 |
| S3b (0171) | a corpus with true negatives | 19 new scenarios, 7 owner-check negatives; AUC **1.000** on them; 57 latent case-sensitivity checks found and fixed | d3 +1 |
| S2 / I13 (0156) | the live noise floor | **mean 0.7639, spread 0.1666** (Opus, 3×6); planted regression moved the mean 0.0278 — undetectable by the mean, visible per scenario; **found the ReDoS** that had been read as a throttle | 0 |
| S1 / I12 (0155) | retrieval → prompt, four arms | one SHA-256 across all four arms before wiring; after: (c) ≤ (b) fired; **zero entries formed** — the split could not recur | 0 |
| S1b (0175) | four arms on a corpus that recurs | entry formed; boosted into 10/17 prompts; **(c) worse than (b)** on mean and negatives — ADR 0110's coverage proxy **disproved** as a task predictor here | 0 |
| S4 / J2 (0160) | the archive earns its keep | CLI flag, persistence, rejects as members; live: 0 kept in both arms — sampling controls *distinct parents* (5 vs 1), and stepping stones spend the drift budget faster | d6 +1 |
| S6 / J4 (0162) | self-preference; harmful-lesson rig | Opus prefers its own text **+0.26**, Haiku +0.01, a third judge attributes it; mitigation shipped. Harmful rig: **could not be built honestly** — and found `_tally` counting a harmful run as helpful | d3 +1 |
| S0b / J0b (0188) | second blind re-score | **69 vs our 72**; a live defect found by running the nightly command; dims 5 and 6 raised by the reviewer on artifacts it found | 72 → 69 |
| S1c (0184) | four arms with K2's fixes | **(c) beat (b)**: +0.0529 mean vs a 0.0353 same-prompt bar, +0.0917 on the negatives vs 0.0500; a third (b) repeat added against the claim widened the bar 30× and the claim survived. **Then WITHDRAWN (ADR 0191):** S3c re-recorded the corpus under S1c's feet in the same hour, and the seed step (1 entry from 3 runs) no longer reproduces on `main` (7 runs, different lesson text). A measurement that cannot be re-run on the current tree is asserted. The runner and data stand as history; re-running the four arms on the one-model corpus is the next dim-2 increment | d2 +2, then −2 |
| S2b (0185) | G3 under live noise | a dead call is retried once and excluded symmetrically (candidate, incumbent, controls); refusal above 25%; **excluding a dead control RAISES the bar** (tested both ways); paired line reported beside p95, never deciding. Re-aggregated floor with the dead call excluded: mean 0.8083, spread 0.0417 — S2's regression would have been 2.1× the spread | 0 |
| S3c (0186) | one model across the corpus | 20 Fable recordings, not 18 — two in the holdout, left by decision (spending the holdout is the owner's act); 18 re-recorded on Opus with every cassette key unchanged; 8 of 18 verdicts moved, all length, zero content | 0 |
| S7 / J1 (0164) | real signal, bounded | the pre-registered branch "if harvest refuses every run, claim +0" fired: real runs were made and were NOT admitted; the cycle's evidence came from bootstrap, which the claim excludes by name. The last +3 stays unclaimed: marlin is the owner's repo, not a third party. Re-measurable now that ADR 0190 landed — M6's five runs promote through the fixed path; a candidate proposed FROM harvested evidence (clause b) is the next S7 | 0 |

**The rubric, re-summed** (guarded by `tests/test_rubric_arithmetic.py`):

| dim | weight | J0 | now | why |
|---|---|---|---|---|
| 1 closed loop | 20 | 15 | 13 | J0b: nightly cycle exit 1 on a two-graph corpus read as healthy (fixed after) |
| 2 what is learned | 20 | 12 | 12 | S1c measured +2 on the pre-0186 corpus and it no longer reproduces; withdrawn, not disproved |
| 3 reflection | 10 | 6 | 8 | S3b + S6 |
| 4 safety | 15 | 14 | 13 | S5 +1; J0b −2 (no runnable red team; no halt channel) |
| 5 evidence | 10 | 8 | 9 | J0b found the artifacts |
| 6 archive | 10 | 5 | 7 | S4 + J0b |
| 7 real signal | 10 | 4 | 3 | 0 harvested scenarios (M6 pending) |
| 8 adoptability | 5 | 4 | 4 | Codex live test is a skip from code alone |
| | | **68** | **69** | |

**The ceiling, recomputed from 69.** Reachable in this repo on the evidence
now in hand: d1 +2 (the nightly loop closing unattended with a real
memory file), d2 +2 more only if the knob's own A/B and a second corpus agree with S1c, d3 +1 (a judge in the choosing path), d4 +2 (a runnable red team; a
halt channel), d5 +1 (`make measure`), d6 +2 (a stepping stone producing a
better descendant), d7 +3 (M6's real signal; the last +3 needs a third
party), d8 +1 (Codex live in CI). **82 without a third party, 85 with
one.** Above 95 is not reachable from what this repo can measure about
itself; the number past ~85 is a claim about someone else's traffic.

## 5. Adversarial rounds

Five seam hunts on tonight's own merges: **16, 8, 8, 8, 8 findings**; every
one reproduced with a command, every one fixed or in a fix branch. The
load-bearing ones, each of which shipped green:

- `--mcp-config {}` is invalid; **`ClaudeCodeProvider` had been dead for hours** through four merges (0150).
- The repo's own scheduled `loop cycle` **exited 0 doing nothing every night** since the workflow was written (0165); `loop run` was its unfixed twin (0167).
- `retrieved_context` was written by the retrieve node and **read by no prompt** (0155); then reached no *generated* graph (0179).
- A word-cap regex **did not terminate** on any answer one word over the cap — I11's three "ten-minute walls" (0166).
- `grok --tools ""` suppresses nothing; the containment sentence stamped into every generated module was true for one harness of three (0169).
- Adopt's append **rewrote every line of a CRLF file** and **deleted the adopter's prose** between a quoted marker pair, printing "your bytes are unchanged" both times (0172).
- One loader of three learned the file-path form; the other two fed G2, which reported the import error as **"the candidate regressed"** (0177).
- The gates' worker had **no login**, so a prompt candidate could be rejected but never accepted (0158 → 0181).
- The nightly command omits `--graph-id` and its refusal exited 1 — "the system working" (0188).

Twelve workers reported a mutation that survived a first pass and fixed
the test rather than dropping the mutation. Three ADR claims written
tonight were found false by later rounds and carry errata rather than
rewrites (0152, 0153, 0165, 0167, 0168, 0169, 0174, 0176, 0178).

## 6. Calls made

| worker | live calls | model |
|---|---|---|
| S1 | 90 | Opus |
| S2 | 41 | Opus |
| S3 | 36 | Opus |
| S3b | 54 | Opus |
| S1b | 120 | Opus |
| S4 | 18 | Opus |
| S6 | 90 | Opus + Haiku + Sonnet |
| M1 / M3 / M4 / M4b / M5 / K1 | 4 / 12 / 15 / 3 / 15 / 18 | Opus (Grok for M3) |
| G2 / orchestrator canaries | 3 / 5 | Grok / Opus |
| pending (M6, S1c, S3c) | ≤ 80 / ≤ 110 / ≤ 24 | Opus |

Every measurement re-ran its baseline arm on the same model; no Fable
number was reused. The Opus session limit was hit once at 02:30 EDT; six
workers died within a step of done and were resumed from their worktrees
with nothing lost.

## 7. What still requires the owner

1. **A real checkout.** Everything ran on clones in scratch. Nothing under
   `/Users/raptor/*` except `aef-core` was written. The first real adoption
   is the owner's, on a repo that is not the trading system.
2. **A third party.** Dimension 7's last three points and the trust case's
   "real tenant" criterion cannot be earned from the owner's own repos.
3. **`gates.live_model_calls: true`** is the owner's opt-in per repo; it
   lets a candidate's code spend the harness quota, and every gate run says
   when it did.
4. **Copilot's CLI** (`impl: command`, configured when installed).
5. **A halt channel** — the digest says `Halt channel configured: NO`.
6. **A redaction pattern for this repo's own identifiers** — marlin's subscription UUID is not matched by `opaque_secret` (ADR 0126 removed `-`); the pilot's scan was clean for that reason as much as any other.

## 8. Pending at the time of writing

L3 (the final hunt's two highs: `--cassette-miss live` with no `--config` turns every miss into an excluded dead call; `--graph-id` scopes evidence for one proposer only). The final seam-hunt found eight; F3/F4/F5/F8 are closed on `main`. Each lands as
its own ADR and a row in this file's tables when merged; the rubric heading
is recomputed from the rows on every merge.

Then stop.
